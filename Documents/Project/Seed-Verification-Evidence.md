# Windvale Seed verification evidence

- Evidence date: 2026-08-02
- Milestone: Windvale Seed
- Qualified hosts: Windows x64 and Debian Linux x64
- Latest cross-host Seed qualified implementation commit: `ea1aa89ba204ead633f8340c61b2bacc716881fd`
- Latest pinned Windvale OS qualified implementation commit: `ea1aa89ba204ead633f8340c61b2bacc716881fd`

## Requirement evidence

| Requirement | Evidence |
| --- | --- |
| C# Stage 0 toolchain | Pinned .NET SDK, solution, compiler, bytecode, runtime, object-model oracle, WVA assembler, linker oracle, CLI, and test projects build with warnings treated as errors. |
| Source-to-bytecode path | `Sum-Data.wv`, `Hello-Windvale.wv`, `Read-Wvb-Header.wv`, `Wv-Dump-Core.wv`, `Wvo-Object-Core.wv`, `Wva-Assembler-Core.wv`, and `Wv-Linker-Core.wv` compile through syntax, semantic WIR, lowering, canonical encoding, and mandatory verification. |
| Deterministic portable modules | Repeated compilation and codec round trips compare complete bytes; fixed SHA-256 identities are checked for every qualified Windvale example and composed tool artifact. |
| Module, assembly, object, and link CLI | The native verifiers invoke `compile`, `assemble`, `link`, `inspect`, `verify`, `run`, `object-inspect`, and `object-verify` against generated artifacts. |
| Reference runtime | Portable .NET interpreter executes only `Verifiedˉmodule`, with checked signed and unsigned arithmetic, array and byte-range bounds, step, call-depth, capability, and host-result controls. |
| Explicit capabilities | Hosted arguments, file-byte input and output, standard output, line output, and diagnostics must be declared, supported by the selected host, and granted separately; refusal and success are tested. |
| Hosted resource boundary | Arguments are immutable and UTF-8 bounded, native file reads stream into bounded immutable bytes, each exact resource name has one first-successful-read snapshot per context, at most 64 distinct snapshots are retained, file writes replace one bounded whole value and durably flush, normal and diagnostic sinks are separate, and stable missing/invalid/oversized/bad-host/snapshot-limit cases are exercised. |
| Useful diagnostics | Language compiler, assembly parser/semantics, binary readers, link resolution/layout/relocation, verifiers, and runtime failures expose stable codes; source errors retain one-based line and column. |
| Source conventions | U+02C9 identifiers and exported `Main` compile and execute; immutable `let` locals and parameters reject assignment; mutable `var` locals accept it; malformed and confusable separators are rejected. |
| Bounded static source modules | Explicit `import` graphs are resolved only from supplied strict-UTF-8 sources, bounded to 64 modules and 16 MiB of aggregate characters, rejected when missing, duplicate, cyclic, unreferenced, non-portable, or outside the first function-only shape, and composed deterministically regardless of dependency argument order. Dependencies are semantically isolated from root and sibling declarations, dependency functions are internalized, and only root exports remain WVB exports. |
| First two-consumer Foundation module | `Foundationˉmachineˉcontracts` owns exact bounded WVO alignment and whole-value machine-name predicates, is imported by both the Windvale assembler and linker, and preserves diagnostic ownership plus every canonical object, image, map, and failure boundary. |
| Shared ordinal byte-span ordering | `Foundationˉbyteˉordering` owns unsigned lexicographic comparison for validated spans of one immutable byte value and is consumed by the Windvale WVO object core, assembler, and linker without allocation, text decoding, or raised instruction ceilings. |
| Foundation binary and text primitives | `u8`, `u32`, immutable `bytes`, zero-copy slices, immutable concatenation, signed and unsigned little-endian reads and fixed-width construction, exact SHA-256 identity over sequences and slices, explicit byte widening, strict UTF-8 validation/encoding/decoding, and ASCII-safe quoting are type-checked, verified, inspected, executed, and covered by deterministic trap and size-limit tests. |
| Windvale-written compiler lexer | `Compilerˉsourceˉlexer` streams the complete implemented Seed keyword/operator surface over strict UTF-8 bytes, preserves Stage 0 token identities and UTF-16-compatible positions, classifies bounded integer literals, validates string escapes and surrogate pairs, and reports deterministic bounded failures without allocating a token collection. |
| Windvale-written compiler parser | Qualified declaration and body passes reproduce the complete implemented top-level, statement, and expression grammar as bounded immutable source views while retaining no token, declaration, statement, expression, or syntax-tree collection. |
| Canonical compiler source set | WVSS 1 carries one root and as many as 63 canonically ordered dependencies in one validated immutable envelope. The portable Windvale module enforces structure, every qualified frontend pass, unique names, and dependency profile/shape rules without admitting paths, timestamps, host objects, or host collection ordering. |
| Windvale-written compiler import graph | `Compilerˉsourceˉgraph` resolves exact declared module-name spans, rejects duplicate and missing imports, computes the complete root closure, rejects unreachable supplied modules, and proves acyclicity with deterministic bounded traversal over one immutable state byte per module. |
| Windvale-written declaration/signature binding | `Compilerˉsourceˉsymbols` enforces namespaces and capability policy, resolves visible nominal signature types, assigns canonical nominal identities, and publishes independently validated `WVSD 1` plus transitive-visibility evidence. |
| Windvale-written body binding | `Compilerˉsourceˉbindings` assigns canonical parameter/local slots and lexical scopes, binds reads, assignments, constructors, functions, capabilities, and Foundation intrinsics, checks arity, and publishes independently validated `WVLB 1` evidence. Its parameter and finalization phases let typed lowering construct the same local evidence without a second successful-path body traversal. |
| Canonical typed source IR | `Compilerˉsourceˉwir` performs complete implemented expression typing, field/operator/call validation, explicit basic-block and temporary construction, return/reachability proof, and publication only after independent validation of all five packed `WVIR 1` sections and their semantic relationships. It carries function-private binding state through the same statement traversal and preserves independent binding diagnostics on failure. |
| Windvale-written WVB backend | `Compilerˉsourceˉwvb` resolves global WVSD identities through their owning WVSS source and statically flattens the complete validated graph into one WVB 1.6 module. It preserves root profiles, capabilities, data, and exports; internalizes dependency functions and nominal types; canonically remaps every function/data/type/capability identity; lowers the complete implemented primitive, nominal, capability, and Foundation surface; computes exact branch offsets and maximum stack in a separate pass; and rejects invalid graphs before publication. |
| Reproducible compiler bootstrap | The exact 12-module, 677,073-source-byte inventory is compiled by Stage 0 into a verified 599,868-byte Stage 1 Windvale compiler. Stage 1 compiles that same explicit inventory in 6,700,562,174 verified VM instructions into a separately verified, byte-identical Stage 2 compiler with SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066`. Dedicated Windows and Linux verifiers fail on build, compile, runtime, publication, verification, or byte-identity differences. |
| Shared native WVB execution slices | Cross-host-qualified portable modules lower into typed locals, typed numbered values, canonical blocks, explicit terminators, explicit instruction charges, and independently decoded x86-64 fragments. Constants, checked arithmetic, control flow, calls through the prior four-register convention, shared instruction/depth budgets, immutable data, borrowed and arena-backed values, twelve native services, W^X JIT, and WVO-linked AOT agree on Windows and Debian x64. Qualified ABI 16 retains the first four registers and independently verifies stack cells through the language's 64-parameter bound; maximum-width scalar, descriptor, and void calls pass focused differential coverage. Unsupported WVB or hostile fragment bytes fail with stable diagnostics and no fallback. |
| First shared native WVB in Windvale OS | One ordinary portable module compiles to canonical verified WVB and the same ABI-5 WVO backend, then executes calls, a bounded loop, and immutable data on the kernel-owned OS stack. Exact result 29 gates continued version-7 boot under pinned QEMU; the guest does not yet load or verify WVB. |
| Immutable nominal records | Record declarations, positional construction, named field reads, nominal function signatures, canonical WVB schemas, verifier rejection cases, runtime values, and inspector output are exercised end to end. |
| Nominal enums and bounded formatting | Explicit enum declarations, exact nominal equality, enum-valued record fields, member naming, invariant `i32`/`u8`/`u32` formatting, bounded text concatenation, verifier rejection cases, and deterministic runtime output are exercised end to end. |
| Windvale-written inspection | `Wvˉdumpˉcore` reads an explicit real file through hosted resources while pure Windvale functions validate all seven envelopes, decode every declaration payload and value shape, walk every instruction, reject malformed lengths/UTF-8/opcodes without escaping diagnostic boundaries, and emit a versioned ASCII-safe line report. |
| WVO 1.0 object model | The C# oracle canonically encodes and strictly verifies x86-64-first sections, symbols, imports/exports, ranges, machine names, zero relocation placeholders, and non-overlapping `absolute-u32`/`relative-i32` relocations. |
| Windvale-written object production | `Wvoˉobjectˉcore` constructs and structurally validates a 189-byte representative object in pure Windvale, persists it only through `file.write_bytes`, and matches the C# oracle byte for byte. |
| WVA 1 Stage 0 assembler | The bounded parser enforces canonical declarations, definition ownership, contexts, integer widths, and references; the x86-64 encoder covers the complete initial code/data subset, derives ranges, creates both relocation kinds, and returns only independently verified WVO bytes. |
| Windvale-written WVA assembler | `Wvaˉassemblerˉcore` validates source size, strict UTF-8, physical-line bytes, line endings, token boundaries, the complete grammar, declarations, definitions, integer widths, contexts, limits, ownership, and references over immutable bytes without host text parsing. It measures the complete result, derives definition ranges, encodes every WVA 1 instruction/data statement, and constructs canonical WVO sections, symbols, and relocations. |
| Windvale linker object boundary | `Wvˉlinkerˉcore` validates the complete WVO 1.0 header, limits, names, sections, symbols, relocations, canonical order, definition ownership, placeholder zeros, and exact consumption in verified bytecode, then exposes immutable offset-based views for later link passes. Representative objects, 128 deterministic one-byte mutations, and 128 bounded random byte values are differentially classified against the independent C# WVO oracle. |
| Windvale linker resolution and layout | The same verified module enforces link-wide aggregate limits, merge-checks canonical export ranges, resolves every import with exact kind matching, selects an exported-function entry, places sections by kind/input/source order using actual-address alignment, bounds the image and `u32` address space, and recomputes every defined-symbol address. |
| Windvale linker image and relocation | The module constructs exact padding, materialized data, and zero-fill as one immutable persistent byte value; resolves local/export/import relocation targets; checks full `absolute-u32` and `relative-i32` arithmetic without truncation; patches exactly four bytes immutably; and emits the exact Stage 0 image digest. |
| Windvale linker independent reconstruction | A separately structured path recomputes placement, rescans all exports, derives symbol addresses independently, applies relocations in reverse traversal order with separate arithmetic, and compares the complete reconstructed image byte by byte. A mismatch returns `WVL1011`, clears candidate bytes, and leaves the writer unreachable. |
| Windvale linker canonical map and publication | After independent image agreement, the Windvale linker constructs the complete path-free ASCII/LF map with exact byte accounting and a 1 MiB limit, publishes the image with exactly one `file.write_bytes` call, and emits that map through standard output. Deterministic failures cannot reach the writer and preserve an existing output; host write failures remain a separate runtime boundary. |
| Stage 0 linker oracle | Windvale Linking 1 validates complete WVO inputs and aggregate limits, keeps locals object-private, resolves every import to one same-kind export, requires an exported-function entry, lays out actual addresses by section kind/input/source order, materializes zero padding and BSS, applies both relocation kinds with checked arithmetic, and independently reconstructs the complete image before returning bytes. |
| Canonical link evidence | The path-free ASCII/LF map records input digests, section placements, definitions, import providers, patch and target addresses, relocation values, entry, and image digest in deterministic order. It is byte-limited, locale-independent, and compared as a complete value across hosts. |
| Representative programs | The portable sum returns `29`; the hosted example prints `Hello from Windvale`; the Foundation header returns `1`; both Foundation boundary demos return `0`; WvDump emits the complete golden Sum report; the WVO core writes its exact object; the hosted Windvale assembler writes the 218-byte canonical `Hello-Object.wvo`; and both the Windvale linker and Stage 0 link it with the 91-byte `Console-Provider.wvo` into the same independently verified 24-byte image and 1,721-byte map. |
| Malformed-input coverage | Structured adversarial cases plus deterministic bounded random Windvale source, WVA source, module, object, Windvale WVO-scan, and link input exercise diagnostic and rejection boundaries. Link coverage includes invalid and oversized objects, invalid entry names, duplicate exports, undefined imports, kind mismatches, non-function entries, aggregate section limits, map limits, unaligned bases, all section kinds, BSS/padding zeros, image/u32 overflow, absolute and relative relocation overflow, 200 hostile objects, locale changes, semantic input reordering, and missing/unchanged output cases. |
| Cross-host coverage | The same 60-test suite passed on Windows and Debian Linux, including native interpreter/JIT/AOT differential execution through each host's W^X adapter. Their normalized contracts compare equal, all 62 current portable WVB, WVO, image, and map artifacts are byte-identical, and both hosts retain the exact qualified Stage 1/Stage 2 compiler identity and portable instruction count. |
| Scope control | The qualified compiler supports bounded static nominal source composition through independently validated typed `WVIR 1`, direct canonical flattening of one complete validated graph into a portable, hosted, or system WVB, and exact bytecode self-reproduction. It covers imports, dependency functions and nominal types, root primitive data/functions, immutable records, nominal enums, and declarations/calls from the complete seven-entry Seed capability catalog; four focused Foundation modules and the qualified Windvale and Stage 0 linkers remain available. The shared native backend covers one exported portable `Main() -> i32`, internal i32/bool functions with as many as four parameters, constants, checked i32 arithmetic, comparisons, Boolean operations, forward/backward control flow, loops, returns, immutable i32 array data, and dynamic positive instruction/depth maxima. Capabilities, general collections, text/bytes, mutable globals, aggregates, allocation/reclamation, a runtime module linker, package manager, executable-container adapter, broad native instruction coverage, native compiler execution, directly executable host program, Windvale-written VM/JIT, .NET retirement, functioning kernel runtime, driver, and complete OS implementation are not included. |

## Verified on the implementation host

```powershell
pwsh -NoProfile -File Tools/Verify/Verify-Seed.ps1 -Level Qualification
```

The verifier performs a Release build, runs the 60-test conformance suite, produces a Windows report, publishes the CLI as framework-dependent `linux-x64`, and exercises real source composition, project selection, Foundation/compiler module composition, the complete compiler frontend through typed WVIR and the qualified canonical function/data/nominal/capability WVB backend, shared native fragments, Windvale-owned runtime stencils, module, Windvale/Stage 0 assembly, Windvale WVO scan, complete Windvale and Stage 0 link, object, inspect, verify, and runtime CLI paths. It checks dependency-order independence, semantic and binary-evidence rejection, output preservation, capability refusal and authorized execution, deterministic file snapshots, opt-in executed-instruction reporting, SHA-256 sequence/slice behavior, byte-for-byte compiler and assembler equality, independent complete-image reconstruction, exact Windvale/Stage 0 image and map equality, maximum image and map-limit cases, publish-after-success behavior, no file for rejected source/link or a missing parent, and preservation of existing outputs when validation fails. The current WVB 1.6 Windows report SHA-256 is `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`.

The expensive compiler convergence proof remains separate from the ordinary suite:

```powershell
pwsh -NoProfile -File Tools/Verify/Verify-Bootstrap.ps1
```

It builds Stage 1 from the exact inventory with the reference/recovery compiler, runs Stage 1 to build Stage 2, independently verifies Stage 2, and requires complete byte equality. Linux uses `./Tools/Verify/Verify-Bootstrap.sh` with the same inventory and ceiling.

## WVB 1.6 linker-prerequisite qualification

Candidate commit `348c82a` was archived and transferred with matching SHA-256 `985410209aee1cc54753aecd12d8d73658b7f3ffa646a129a4f490bec520817c`, then verified in `/tmp/windvale-prerequisites-348c82a-20260730` on the isolated [E-Worker](https://eworker.ca) QA host. The host ran Debian GNU/Linux 12 x64 with .NET SDK `10.0.302`; the reports identify .NET `10.0.10`. The exact archive passed the Release build with zero warnings/errors, all 31 tests, and the complete native CLI verifier. The Debian report was retrieved with SHA-256 `cec2e640181eb7bf5bca91fd7890aecd93137c6f594ca6af0bac873e8c98d3c0`. The report comparator confirmed that the Windows and Debian normalized contracts match.

Both hosts produced WVB format `1.6` with these module identities:

- `Sum-Data.wvb`: `6f3a272d37dd8893995c7f85c236414ed2864bf59de2f3775c08afd426013f8c`.
- `Hello-Windvale.wvb`: `bcf6597a27384661d2796f1dd8ee6e24cce8e6c7cb84def3b7826a564acb7d54`.
- `Read-Wvb-Header.wvb`: `72ae31559bb3335b320328c26e70518b6a0f3e617d099d41b328b066bb3784c7`.
- `Wv-Dump-Core.wvb`: `38af93371f5ed737946092092c67f6c363b340c7b2a2e8d0588c05a3e94b730b`.
- `Wvo-Object-Core.wvb`: `76f5a414bdc8feab35cedb28ecfc56d0ed24b0abcfc3c5c128e4f71fd0e5232b`.
- `Wva-Assembler-Core.wvb`: `9fe0e79a4895281908df13b31f127dd9dd019282263da71874bbefb7d9d3cb3a`.

The directly retrieved Debian `Wv-Dump-Core.wvb` and `Wva-Assembler-Core.wvb` matched their Windows artifacts byte for byte. Every pre-existing WVO, WVA, image, and map identity remained unchanged, including the 24-byte linked image `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a` and 1,721-byte map `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`. Qualification specifically exercised opcode `0x7D` through compiler lowering, verifier stack typing, runtime SHA-256 over a non-zero-offset slice, inspector output, and the Windvale-written WvDump decoder. It also proved same-name snapshot reuse without a second adapter call, snapshot sharing across capability hosts for one context, the 64-distinct-snapshot ceiling, and the 67-argument launcher ceiling. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0012 and the prerequisite portion of roadmap gate 6G. It does not qualify the Windvale-written linker itself.

## Balanced persistent-byte qualification

Candidate commit `89ce80b` was archived with SHA-256 `a0b6620227b04315936e103a1e42e4d99c14649cc3de1e1e3b8fd30895619452` and verified from that exact archive in `/tmp/windvale-persistent-bytes-89ce80b-20260730` on the Debian QA host. Windows and Debian both completed a zero-warning Release build, all 32 tests, and the complete native CLI verifier. The added Windvale test performs 65,536 one-byte concatenations, validates both endpoints, creates and reads a four-byte patch across persistent slice/concat boundaries, and proves that the original sequence remains unchanged. Both reports retained SHA-256 `5711f3025ea56edcc3af017505604c3c4153ad38e5858dedfea73b0d121366e3` on Windows and `cec2e640181eb7bf5bca91fd7890aecd93137c6f594ca6af0bac873e8c98d3c0` on Debian because the normalized portable contract and every canonical artifact stayed unchanged. The comparator confirmed equality, and the resolved exact QA directory plus transferred archive were removed and confirmed absent.

This qualifies Decision 0013 as an implementation-semantic prerequisite for 6G. It does not add mutable buffers, change WVB, or qualify the Windvale-written linker.

## Windvale linker object-scanner qualification

Candidate commit `3eb331a` was archived with SHA-256 `af8c34b304faf258cad811fa3828b127faa99a801cdf3a0a3f3e6f6bd8d63c64`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-linker-scanner-3eb331a-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 33 tests, and the complete native CLI verifier. The QA report identifies .NET `10.0.10`.

The suite compiled and bytecode-verified `Wvˉlinkerˉcore`, checked its exact SHA-256 `ac00a5b702f2a4ef185bd5f021ec2611bd8a335d1937804ceeb30f28cc1b8ded`, exercised embedded valid and malformed WVO cases, accepted representative objects covering all section representations and both relocation kinds, and differentially compared the Windvale scanner with the Stage 0 object oracle for 128 deterministic one-byte mutations plus 128 bounded random byte values. The real CLI verifier proved capability refusal, no hosted read during self-test, acceptance of the Windvale-written 218-byte assembler object, and deterministic rejection of non-WVO source.

The Debian report was retrieved with SHA-256 `824eac7247d83a2d790aba87a80c583b67155cc056a48e5fe73a133bd81d1e67`. Its normalized contract matched the Windows report and included `wvLinkerCoreResult` `0` plus exact hosted output `object status=Valid sections=2 symbols=3 relocations=2 offset=218` followed by LF. The directly retrieved Debian `Wv-Linker-Core.wvb` matched the Windows artifact byte for byte. Every previous module, object, image, and map identity remained unchanged. After evidence retrieval, the previously resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0014 and the WVO parsing/view portion of roadmap gate 6G. It does not qualify symbol resolution, layout, relocation application, independent image verification, map construction, hosted image publication, the complete Windvale linker, or Phase 6.

## Windvale linker resolution/layout qualification

Candidate commit `709ccb3` was archived with SHA-256 `28740cfcf8d97b54619235b433b4c404ac74fc1d2d56e77e6bbb6e026e8656fd`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-linker-layout-709ccb3-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 34 tests, and the complete native CLI verifier.

The new tests compare Windvale analysis with the Stage 0 oracle for canonical and reversed input order, aligned and deliberately unaligned bases, code/read-only-data/writable-data/zero-fill placement, exact aggregate counts, final image length, entry address, invalid objects and requests, aggregate overflow, duplicate exports, undefined imports, symbol-kind mismatch, missing entry, and address-space overflow. The five-object 320-section aggregate fixture also exposed and then verified removal of a cubic section-name scan; the accepted implementation walks record cursors directly and remains within the normal 20,000,000-instruction verifier budget. Snapshot evidence confirms that repeated semantic passes invoke the native reader once per exact resource name. No success or rejection path writes the reserved image output in this analysis-only slice.

The Debian report was retrieved with SHA-256 `2d02a8af949b80df0778fdf2d486967cd0174b19c4789da390e89b73778e8f28`. Its normalized contract matched the Windows report and included exact Windvale analysis output `link status=Valid inputs=2 sections=3 symbols=4 relocations=2 image-bytes=24 entry-address=1048576 input=4294967295` followed by LF. The directly retrieved Debian `Wv-Linker-Core.wvb` matched the Windows artifact byte for byte at SHA-256 `2a4c24d1330ffbfc6d7253f16978fe5a86264c5118d8bb3e20473d35be023707`. Every previous object, image, and map identity remained unchanged. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0015 and the multi-object resolution/layout portion of roadmap gate 6G. It does not qualify image construction, relocation application, independent image reconstruction, canonical map construction, hosted image publication, the complete Windvale linker, or Phase 6.

## Windvale immutable-image/relocation qualification

Candidate commit `ec9c980` was archived with SHA-256 `a1252652bfef44abedb477f684c265ff222d40a5d24f1a4c7d1da33a1491d6ca`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-linker-image-ec9c980-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 34 tests, and the complete native CLI verifier.

Windvale constructs the canonical 24-byte candidate from two Windvale/Stage 0-identical WVO inputs, including 15 bytes of alignment padding, materialized code and read-only data, an imported relative call target, and an absolute data relocation. The emitted SHA-256 is the Stage 0 oracle identity `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`. Additional oracle comparisons cover reversed semantic input order, deliberately unaligned bases, writable data, BSS, local/export targets, negative relative values, and both relocation kinds. Pure Windvale self-tests cover exact positive and negative signed limits, valid negative addends, absolute underflow, and relative overflow; hosted tests require `WVL1009` and `WVL1010` with no writer invocation.

The Debian report was retrieved with SHA-256 `04fceb5c29de93a707cbdbbab9900eb0dc687525f9c054d2df72e8cfe36f02cc`. Its normalized contract matched the Windows report, including the complete two-line Windvale image report. The directly retrieved Debian `Wv-Linker-Core.wvb` matched the Windows artifact byte for byte at SHA-256 `87fb5974d989d7b7870dab9a6fb4e1bb1bae8549bb90edf78f7bdfdf1824b822`. Every Stage 0 object, image, and map identity remained unchanged. The reserved Windvale output remained absent on success and rejection because independent reconstruction is not yet implemented. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0016 and the candidate-image/relocation portion of roadmap gate 6G. It does not qualify independent image reconstruction, canonical map construction, hosted image publication, the complete Windvale linker, or Phase 6.

## Windvale independent-image-reconstruction qualification

Candidate commit `d8008e3` was archived with SHA-256 `9f1d73966581f1181c69144dca1503830e0d83f65f587eca53f5ee580fb6eb12`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-linker-verify-d8008e377e7b-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 34 tests, and the complete native CLI verifier. The QA report identifies .NET `10.0.10`.

The independently structured Windvale verifier rebuilt section padding, materialized data, BSS, provider resolutions, defined-symbol addresses, and both relocation kinds without calling the production image builder. It scanned all symbols for exports, applied relocations in reverse input/source order, used separate signed-magnitude arithmetic, and compared every byte. Embedded fault injection required `WVL1011` plus an empty result for a corrupted reconstruction. A valid 4 MiB code-plus-BSS image exercised the accepted maximum image size under the explicit 200,000,000-instruction ceiling while still invoking the reader once per input and the writer zero times.

The Debian report was retrieved with SHA-256 `eeb5c34026dbec92d20e3cf26e5c486fcb062774039414c4998b109687c96e0e`. Its complete normalized contract matched the Windows report, including the exact two-line canonical analysis output and image identity `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`. The directly retrieved Debian `Wv-Linker-Core.wvb` matched the Windows artifact byte for byte at SHA-256 `0b8d4ce09a043a675e64018c02fac94740b0b878a74801fc622ce7703e956b35`. Every previous object, image, and map identity remained unchanged. The reserved Windvale output remained absent on success and rejection because canonical map construction is not yet implemented. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0017 and the independent complete-image reconstruction portion of roadmap gate 6G. It does not qualify canonical map construction, hosted image publication, the complete Windvale linker, or Phase 6.

## Complete Windvale linker / Phase 6 qualification

Candidate commit `40ac57d` was archived as `artifacts/windvale-linker-complete-40ac57d1b2ff.tar.gz` with SHA-256 `2f8e0e570370cb91a3e35f16ae3341095f6963ecc23eaaaf41f7fb83d0a629f3`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-linker-complete-40ac57d1b2ff-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 34 tests, and the complete native CLI verifier. The QA report identifies .NET `10.0.10`.

The complete Windvale linker accepted the same two canonical inputs as Stage 0, independently reconstructed every image byte, built canonical map version 1, invoked the writer exactly once only after complete deterministic success, and produced the exact 24-byte image SHA-256 `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a` plus the exact 1,721-byte map SHA-256 `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`. The qualified WVB SHA-256 is `8d3cb567f6985077b3ad487627bf77a20326b4bc02bcab8d938354f48d339cfd`. The accepted 4 MiB image completed under the explicit 200,000,000-instruction ceiling. Four valid objects containing 16,384 short-name definitions reached the exact canonical-map limit path, returned `WVL1012`, and invoked no writer under the same ceiling.

The Debian report was retrieved with SHA-256 `64798286563e2d626f4e7d4fca2fffbe0feed5417dcf7e0abf9e3d14776ff3ca`; the Windows report SHA-256 is `496ab9faae9b62da3d733bdb62da7bd20fb8980e165a1f6c6b6be3d32d4b221a`. Their normalized contracts matched, including exact equality between the Windvale `wvLinkerMapOutput` and Stage 0 `linkMap` values. The directly retrieved Debian WVB, linked image, and map matched their Windows artifacts byte for byte. The native success path wrote once; every deterministic rejected link invoked no writer and preserved an existing image; and a missing output parent remained the separate host diagnostic `WVR3022`. After evidence retrieval, the previously resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0018, roadmap gate 6G, and Phase 6. It does not qualify Phase 7 Foundation modules, a self-hosted compiler, native lowering or executable containers, directly executable host tools, firmware, a kernel, drivers, or an operating system.

## Bounded source-module composition / Phase 7 enabling qualification

Candidate commit `df80f91` was archived as `artifacts/windvale-source-modules-df80f91.tar.gz` with SHA-256 `d69de6acee577c2396edd047ec1e09b776ba4e3fb512f087fd2f3e2a3d1c6b8c`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-modules-df80f9154c45-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 35 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

The qualified compiler resolves a bounded explicit source set, rejects duplicate/missing/cyclic/unreferenced graphs and unsupported dependency shapes, semantically validates each portable dependency against only its own transitive closure, then internalizes dependency functions into one ordinary verified WVB 1.6 module. Reversing the two explicit dependency arguments produced the same exact 339-byte module. Its SHA-256 is `5d27c9667eb66e1abbf46b40d02ab3d4e01b94a421a93bffd0375a550440a612`, its only WVB export is root `Main`, and portable execution returned `42` on both hosts. The real CLI missing-transitive-import case returned `WVC0007`, created no new output, and preserved an existing output unchanged.

The Debian report was retrieved with SHA-256 `4be571b8f73e15b9b1f01302435f89459405ca48e83dacd3c93c22ae1c46686f`; the Windows report SHA-256 is `f943887d661732b9a81ce35723c730430a8e61e90f88771ce8e3046e4d8f21cf`. Their normalized contracts matched, including `sourceCompositionSha256` and `sourceCompositionResult`, and the directly retrieved Debian composition module matched the Windows artifact byte for byte. Every previously qualified module, object, image, and map identity remained unchanged. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0019 and the source-module prerequisite for Phase 7. It does not yet qualify a reusable Foundation API because no shared source module is consumed by both the assembler and linker. It also does not qualify imported data or nominal types, runtime WVB linkage, package discovery, the self-hosted compiler, native tooling, or any OS layer.

## First two-consumer Foundation-module qualification

Candidate commit `d46af86` was archived as `artifacts/windvale-foundation-machine-d46af86.tar.gz` with SHA-256 `d0691dcdf22a0cc18e6fcb594174d21900b93c37f2e290d2416f114143222b0f`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-foundation-machine-d46af863420e-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 36 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

Both hosts produced the exact 2,465-byte `Machine-Contracts.wvb` SHA-256 `9f909a4c47d6f7fb41570b58615a533e79e0219a780c686a64995826b322219a` and 3,538-byte `Machine-Contracts-Demo.wvb` SHA-256 `b505d3335fa5a4b1dabe2d5e64e4c7a557e0028666cbebe1e2557a0255772f1a`; the demo returned `0`. The composed assembler is 94,739 bytes with SHA-256 `5043d1b47686af72c04209875845021862dc664f960dae0a2c1d7babe051d842`, and the composed linker is 109,419 bytes with SHA-256 `ffedbf10782850c3cd693c4d8dc2b88b67b33a19714a0d8a489adb9fb728c96e`. Directly retrieved Debian copies of all four WVB files matched their Windows artifacts byte for byte.

The shared predicates did not change accepted tool products. Both hosts retained the 218-byte canonical object SHA-256 `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`, 24-byte image SHA-256 `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`, and 1,721-byte map SHA-256 `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`; directly retrieved Debian copies matched Windows byte for byte. The Debian report SHA-256 is `5fcd3c65bf873ad090ee98241b5b6200b80ec3356573e90c3c24d2714b23959c`, and the Windows report SHA-256 is `104694c0bc45edbaa4e26db4d9b850d194cb4750b65dc15e63d2082bca2868b3`. Their normalized contracts matched. After evidence retrieval and one full attached confirmation rerun, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0020 and Windvale's first real Foundation module. It does not complete Phase 7 or qualify a collections layer, imported source data or nominal types, runtime WVB linkage, package discovery, the self-hosted compiler, native tooling, or any OS layer.

## Shared ordinal byte-span ordering qualification

Candidate commit `4fdea22` was archived as `artifacts/windvale-byte-ordering-4fdea22.tar.gz` with SHA-256 `7526c21e766fcfc0301dcc17747fff1a2a9bca5236c7d86abe09e0f0d3e7912e`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-byte-ordering-4fdea22b1208-20260730` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 37 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

Both hosts produced the exact 989-byte `Byte-Ordering.wvb` SHA-256 `194e4b5c4eb7f4641a39098abce3dabb93187af7149e184b56b76f978ed2f4f1` and 2,445-byte `Byte-Ordering-Demo.wvb` SHA-256 `0b41e8f615630e0734812ba8cd8e7c06e975592b86327c2fe8220f5e29c10cab`; the demo returned `0`. The three composed consumers were also exact: the 29,863-byte object core SHA-256 `e35939e46ca63f6c284ae457be12de23bb6bc8cb28fac52ce76c833d5fe6bb74`, 94,616-byte assembler SHA-256 `92eb62466747e6747f2052efb46a7e5164ba472f2638cc014a4b6b202596b57f`, and 109,437-byte linker SHA-256 `88a828ee110d20dcb110a39849bbb9b19b56f746d7084041d5ecbaa3532a894c`. Directly retrieved Debian copies of all five WVB files matched their Windows artifacts byte for byte.

The shared comparison preserved every qualified product and boundary. The 189-byte representative WVO remained SHA-256 `006fd80183da7fbc71d3c6d63b65e6f3551765508fe9dba6f38ba80e002eb28a`; the directly compared 218-byte assembled object remained `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`; the directly compared 24-byte image remained `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`; and the directly compared 1,721-byte map remained `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`. The maximum image and definition/map-limit cases still completed under their existing 200,000,000-instruction ceiling, and all rejected-output preservation checks passed.

The Debian report SHA-256 is `a400178dc71bafa0d5cfd549098741807dc60914fbf834610dfe04f54c905618`, and the Windows report SHA-256 is `1c674f9a78e1a074ca95a942d54ea914da484aca2feb9bb425ccb525e7077a33`. Their normalized contracts matched, including the new Foundation identities/results and all three consumer identities. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0021 and Windvale's second real Foundation module. It does not complete Phase 7 or qualify a general collection, arbitrary cross-value ordering, structured result layer, imported source data or nominal types, runtime WVB linkage, package discovery, the self-hosted compiler, native tooling, or any OS layer.

## Static nominal contracts and bounded decimal parsing qualification

Candidate commit `6d2a351` was archived as `windvale-nominal-decimal-6d2a351.tar.gz` with SHA-256 `e8c4c7253bac7135e43944b4f6279c66807e9b2da6f7f04436f098ff54bf4c43`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-nominal-decimal-6d2a351` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 38 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

Static composition now internalizes dependency records and enums as well as exported functions while retaining explicit bounded inputs, portable-only dependencies, root-only WVB exports, deterministic dependency ordering, and semantic validation against only each module's transitive closure. Both hosts produced the same 714-byte transitive nominal fixture SHA-256 `0980b7178943be516cd9b6924f179d5977ca147e11bf105c5063ea078c645b60`; it returned `42`, and reversing the dependency arguments produced identical bytes. The suite also rejected a dependency attempting to use a nominal type from an unimported sibling.

Both hosts produced the exact 1,697-byte `Decimal-Parsing.wvb` SHA-256 `39f6c1c3d5a2233d5296e777e798450571c5f4ba837120a25a6487bf8014ee1f` and 3,778-byte `Decimal-Parsing-Demo.wvb` SHA-256 `16a20ee595eb708095f6e8c38c809a24774989110780dbefbacbc36ee468e695`; the demo returned `0`. Its structured result and total span checks covered zero, `u32` maximum, overflow, empty and overlong spans, nondigits, leading zeroes, non-zero-offset parsing, and invalid ranges. The composed 95,124-byte assembler SHA-256 is `d7a9aecb64b99f5ea2b4d61f8900390e06de9708c219164cc1e3d8bb702de416`; the composed 109,867-byte linker SHA-256 is `c3d3e17bf1aa55c692fac2749aa6b960b60226469de5b0687af2d93ec54762d2`.

The shared parser preserved every qualified product and boundary. The directly compared 218-byte assembled object remained SHA-256 `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`; the directly compared 24-byte image remained `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`; and the directly compared 1,721-byte map remained `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`. The maximum image and definition/map-limit cases still completed under their existing 200,000,000-instruction ceiling, and all deterministic no-write and existing-output preservation checks passed.

The Debian report SHA-256 is `83099f01b9135beb5228b67a3e55ae6ade66201be601e827ecf0eb3b6c05bec2`, and the Windows report SHA-256 is `c0069140db06504ce1ab3513e8fd331c37471be16b69d40f14f524e2575e1909`. Their normalized contracts matched. Directly retrieved Debian copies of the nominal composition fixture, decimal module and demo, assembler, linker, canonical object, image, and map matched their Windows artifacts byte for byte. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decisions 0022 and 0023 and Windvale's third real Foundation module. It does not complete Phase 7 or qualify imported source data, private dependency declarations, runtime WVB linkage, package discovery, a general numeric library, the self-hosted compiler, native tooling, or any OS layer.

## Bounded immutable byte-construction qualification

Candidate commit `26e2fd1` was archived as `windvale-byte-construction-26e2fd1.tar.gz` with SHA-256 `452ff47101e978156aa6aa7897bd74b0de10b41c2604acf709cf0e671708810d`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-byte-construction-26e2fd1` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 39 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

Both hosts produced the exact 2,000-byte `Byte-Construction.wvb` SHA-256 `6f26865069333c02b15ab83d48f2a0cb0e3a05db98bcd841f31e232485b76207` and 5,068-byte `Byte-Construction-Demo.wvb` SHA-256 `a9b577dc08ac6e4a0d786f04d6667eb0347c57a0c1abbd81f3481fb0e0bc6c29`; the demo returned `0`. It exercised empty and unsigned-byte repetition, the exact 4,194,304-byte maximum, rejection above the maximum, middle replacement with a length change, end insertion, invalid offsets/ranges, and final-size overflow. Repeat construction uses logarithmic immutable concatenation, and replacement validates every range and final size before slicing.

The two composed consumers were exact across hosts: the 96,781-byte assembler SHA-256 is `a5f4e913078295a323eac315f9df818877ac519de97028e581cab8577f1dd150`, and the 110,840-byte linker SHA-256 is `091383174f0ca6e535881f31949c65d46542f8b452905f0a82c713707cada1aa`. The assembler replaced its linear long-line fixture construction; the linker replaced its page filler and duplicate immutable patch algorithms.

Every qualified product and boundary remained intact. The directly compared 218-byte assembled object remained SHA-256 `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`; the directly compared 24-byte image remained `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`; and the directly compared 1,721-byte map remained `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`. The maximum image and definition/map-limit cases still completed under their fixed 200,000,000-instruction ceiling, and all deterministic no-write and existing-output preservation checks passed.

The Debian report SHA-256 is `2b09b06452690d7076fd807209ab23b8a25eeeaea0aa021c12b90820f0b2a81f`, and the Windows report SHA-256 is `25911b0601018b794872b66bbdc69f5f35a0fa2f79acb7eb47a288cf07122d33`. Their normalized contracts matched. Directly retrieved Debian copies of the Foundation module and demo, assembler, linker, canonical object, image, and map matched their Windows artifacts byte for byte. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0024 and Windvale's fourth real Foundation module. It does not complete Phase 7 or qualify a mutable builder, packed/general collection, streaming API, compiler frontend, self-hosted WVB encoder, native tooling, or any OS layer.

## Windvale bootstrap source-lexer qualification

Candidate commit `d91dbfb` was archived as `windvale-source-lexer-d91dbfb.tar.gz` with SHA-256 `22be575414cc29ee6d6348ca1b80cde51d77cc37fa8f59f548325f6fc04e3194`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-lexer-d91dbfb` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 40 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

The lexer is portable verified bytecode and imports only `Foundationˉdecimalˉparsing`. It streams one flat token with its next byte cursor and UTF-16-compatible line/column position, preserving all 55 Stage 0 token-kind identities without retaining a token sequence. The fixed demo covers all 28 keywords, all 22 punctuation/operator forms, U+02C9 identifiers, `i32`/`u8`/`u32` values and overflow, Unicode whitespace, comments containing non-BMP text, strict UTF-8 rejection, cursor validation, supported and unsupported escapes, Unicode-escape shape, surrogate pairing, unexpected characters, and token/limit boundaries. It returned `0` on both hosts under the 10,000,000-instruction ceiling.

Both hosts produced the exact 33,770-byte `Source-Lexer-Core.wvb` SHA-256 `0a9d5ff05afbe8598491ca636029fdfc7577dda754a048b93b0529d549019b04` and 39,020-byte `Source-Lexer-Demo.wvb` SHA-256 `32429c56b1b027fc440de14487ac0b5c628cec3c9bded1a98c1c21e6cbeed05a`. Directly retrieved Debian copies matched the Windows artifacts byte for byte.

Every qualified downstream identity remained exact across hosts. Direct comparisons retained the 96,781-byte assembler SHA-256 `a5f4e913078295a323eac315f9df818877ac519de97028e581cab8577f1dd150`, 110,840-byte linker SHA-256 `091383174f0ca6e535881f31949c65d46542f8b452905f0a82c713707cada1aa`, 218-byte assembled object SHA-256 `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`, 24-byte image SHA-256 `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`, and 1,721-byte map SHA-256 `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`.

The Debian report SHA-256 is `1bfb71d91229560810be446a770accc7664ab4fe497b5ef1ab2aaee81732a55f`, and the Windows report SHA-256 is `568aa1632d0a58c617b552dd8186f9a1e9196ea17cb4447acfe0b706b9c6568a`. Their normalized contracts matched, including both lexer identities and result. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0025 and the first slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify decoded string construction, a token collection, a parser, syntax tree, recoverable diagnostic collection, semantic model, WIR producer, WVB encoder, bootstrap closure, native tooling, or any OS layer.

## Windvale bootstrap declaration-parser qualification

Candidate commit `fc87a3e` was archived as `windvale-declaration-parser-fc87a3e.tar.gz` with SHA-256 `4e59e942b9665df1350ac0cd90c90b0e93d2c7cc3aef4ea78d57bdb27de6cf61`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-declaration-parser-fc87a3e` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 41 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

The portable declaration pass validates the complete source once, exposes streaming immutable header/declaration views, and retains no token or declaration collection. It covers module profiles; imports; qualified capabilities; text, integer-array, and byte data; records; enums; function signatures; balanced body spans; declaration ordering; explicit item/depth/source/token ceilings; and fail-fast lexical/parser evidence. The portable demo returned `0`. Through the explicit hosted file boundary, the tool reported the real 39,210-byte lexer as 4,715 tokens, 1 import, 2 records, 3 enums, and 14 functions under 30,000,000 instructions. It reported its own 64,950-byte source as 8,876 tokens, 1 import, 4 records, 4 enums, and 24 functions under 45,000,000 instructions.

Both hosts produced the exact 95,703-byte `Source-Declaration-Parser.wvb` SHA-256 `b09be82c374636bf0b75a0dcea21afa648d89676e0fb0ffedcef68f9e958ee61`, 99,847-byte demo SHA-256 `82dd2f72d2b2d148289353045fda861e07638e8fac8ba97164642d185c3b8e9a`, and 97,660-byte hosted tool SHA-256 `36406acea0ccab9cf9f91cc9723638ae133daa1d5893dcf64454a983427a520c`. Directly retrieved Debian copies matched the Windows artifacts byte for byte.

Every directly checked dependency and downstream product also remained exact across hosts: the 33,770-byte lexer SHA-256 `0a9d5ff05afbe8598491ca636029fdfc7577dda754a048b93b0529d549019b04`, 39,020-byte lexer demo SHA-256 `32429c56b1b027fc440de14487ac0b5c628cec3c9bded1a98c1c21e6cbeed05a`, 29,863-byte object core SHA-256 `e35939e46ca63f6c284ae457be12de23bb6bc8cb28fac52ce76c833d5fe6bb74`, 96,781-byte assembler SHA-256 `a5f4e913078295a323eac315f9df818877ac519de97028e581cab8577f1dd150`, 110,840-byte linker SHA-256 `091383174f0ca6e535881f31949c65d46542f8b452905f0a82c713707cada1aa`, 218-byte assembled object SHA-256 `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`, 24-byte image SHA-256 `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`, and 1,721-byte map SHA-256 `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`.

The Debian report SHA-256 is `69e324108712136dc689eb23aaa063edf2c852d35bf76e4ed4164bf49ac1deac`, and the Windows report SHA-256 is `b4b249a7e9505a4b26b71f23a677bf73073334af02a02f78c794557b59f80cd3`. Their normalized contracts matched. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0026 and the declaration pass of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify statement/expression parsing, decoded string construction, a token/declaration/syntax collection, recoverable diagnostic collection, semantic binding, WIR production, WVB encoding, bootstrap closure, native tooling, or any OS layer.

## Windvale bootstrap body-parser qualification

Candidate commit `ddfa9e3` was archived as `windvale-source-body-parser-ddfa9e3.tar.gz` with SHA-256 `6ebc36462fc4ed0655b97d8c10450d81c8e28a3d0d9f305543fd6b4a65e3bda4`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-body-parser-ddfa9e3` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 42 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

The portable body pass implements every Stage 0 statement and expression form over the declaration parser's exact body spans. It returns flat immutable views and retains no token, declaration, statement, expression, or syntax-tree collection. The tests cover all statement/expression forms and precedence, exact child and body spans, lexical/declaration failure propagation, and the hard boundaries for 64 call arguments, expression and statement nesting, 4,096 statements per function, and aggregate expression nodes. The portable demo returned `0` on both hosts under the 30,000,000-instruction ceiling.

Both hosts produced the exact 164,806-byte `Source-Body-Parser.wvb` SHA-256 `bb04309dfd4b037c05a4f0d52903d937336e90e64077fbc1b78cf5ea88c1de5f`, 168,718-byte demo SHA-256 `5c479f4e922852043696a599a7832a4111d326ef54ce8222166caf3570ec28ba`, and 165,942-byte hosted tool SHA-256 `761887d3674833854d976dd394ad3f83f27d2c74748b6dd0f296c97b117140ca`. Directly retrieved Debian copies matched the Windows artifacts byte for byte.

At the conservative qualification ceilings, the hosted tool produced these identical reports on both hosts:

```text
source bodies status=Valid functions=14 top-level=138 statements=510 expression-nodes=1432 statement-depth=17 expression-depth=5 offset=39211
source bodies status=Valid functions=24 top-level=232 statements=527 expression-nodes=2135 statement-depth=5 expression-depth=3 offset=64951
source bodies status=Valid functions=38 top-level=234 statements=519 expression-nodes=2500 statement-depth=5 expression-depth=3 offset=69023
```

These cover the real lexer at 100,000,000 instructions, declaration parser at 160,000,000, and body parser at 160,000,000. The qualified body module contains 23 module-defined types and 38 WVB exports; Decision 0027 records why the present source-composition subset mechanically exposes retained dependency helpers.

Every directly checked dependency and downstream product also remained exact across hosts: the 95,703-byte declaration parser SHA-256 `b09be82c374636bf0b75a0dcea21afa648d89676e0fb0ffedcef68f9e958ee61`, 99,847-byte declaration demo SHA-256 `82dd2f72d2b2d148289353045fda861e07638e8fac8ba97164642d185c3b8e9a`, 97,660-byte declaration tool SHA-256 `36406acea0ccab9cf9f91cc9723638ae133daa1d5893dcf64454a983427a520c`, 33,770-byte lexer SHA-256 `0a9d5ff05afbe8598491ca636029fdfc7577dda754a048b93b0529d549019b04`, 39,020-byte lexer demo SHA-256 `32429c56b1b027fc440de14487ac0b5c628cec3c9bded1a98c1c21e6cbeed05a`, 29,863-byte object core SHA-256 `e35939e46ca63f6c284ae457be12de23bb6bc8cb28fac52ce76c833d5fe6bb74`, 96,781-byte assembler SHA-256 `a5f4e913078295a323eac315f9df818877ac519de97028e581cab8577f1dd150`, 110,840-byte linker SHA-256 `091383174f0ca6e535881f31949c65d46542f8b452905f0a82c713707cada1aa`, 218-byte assembled object SHA-256 `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`, 24-byte image SHA-256 `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`, and 1,721-byte map SHA-256 `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`.

The Debian report SHA-256 is `f5bd4c12c841683a289213e89491f81d0ec17dc80e81e1dc5cbf384af6f64e7d`, and the Windows report SHA-256 is `eb6c2546bd07017bb815fcd40699f8c887fd6871091601fcfef5a146245e944e`. Their normalized contracts matched. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0027 and the complete syntax-parser portion of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify decoded string construction, a token/declaration/syntax collection, recoverable diagnostic collection, semantic binding, WIR production, WVB encoding, bootstrap closure, native tooling, or any OS layer.

## Canonical packed compiler source-set qualification

Candidate commit `00ef0b1` was archived as `windvale-source-set-00ef0b1.tar.gz`, 315,314 bytes with SHA-256 `0628a6666d8afdb27db963930c7aba2bb02c8430f40d9eacea7e5607e652f53a`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-set-00ef0b1` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 43 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

The portable source-set module validates the complete WVSS 1 envelope before exposing any source slice; accepts one through 64 nonempty sources; rejects bad headers, truncation, noncanonical layout, trailing bytes, source or body syntax failures, duplicate declared names, nonordinal dependencies, and invalid dependency profiles/shapes; and keeps import-graph and symbol binding in their later compiler phases. Tests include the exact 64-module boundary and prove that repeated hosted resource names use one first-successful-read snapshot. The real compiler frontend set produced this identical report on both hosts under the explicit 800,000,000-instruction ceiling:

```text
source set status=Valid modules=5 source-bytes=192171 imports=4 records=16 enums=11 functions=86
```

Its root is the source-set core followed by the body parser, declaration parser, lexer, and decimal parser in canonical declared-name order. The five source payloads total 192,171 bytes. Both hosts produced the exact 177,057-byte `Source-Set-Core.wvb` SHA-256 `c03b3e9daa5b20fc2f77a0d1dd15cb1fdc1728e2a6eda021aa766b19b1bfa2b8`, 186,030-byte demo SHA-256 `0054138c6e39f3c99e5cd4751c796cd599b495880d7db174323342fb7b687488`, and 181,286-byte hosted tool SHA-256 `dc290826985f66f80d469b99235ca290dc617997edee0aab2ea0d4227984aab6`.

Directly retrieved Debian copies of 14 dependency/downstream artifacts also matched Windows byte for byte: the body parser/core/demo/tool, declaration parser/core/demo/tool, lexer core/demo, object core, assembler core, linker core, 218-byte canonical object, 24-byte image, and 1,721-byte map. Their retained identities include parser core hashes `bb04309dfd4b037c05a4f0d52903d937336e90e64077fbc1b78cf5ea88c1de5f` and `b09be82c374636bf0b75a0dcea21afa648d89676e0fb0ffedcef68f9e958ee61`, lexer hash `0a9d5ff05afbe8598491ca636029fdfc7577dda754a048b93b0529d549019b04`, object/assembler/linker hashes `e35939e46ca63f6c284ae457be12de23bb6bc8cb28fac52ce76c833d5fe6bb74`, `a5f4e913078295a323eac315f9df818877ac519de97028e581cab8577f1dd150`, and `091383174f0ca6e535881f31949c65d46542f8b452905f0a82c713707cada1aa`, plus product hashes `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`, `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`, and `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`.

The Debian report is 14,017 bytes with SHA-256 `2690398c4aaedf1b376010ca9126ee5a5adfe2c96d18095ab4e11fb532462e2c`; the Windows report is 14,092 bytes with SHA-256 `213fd24ec0cf764b696e382d52af4153594d317bbf5b359e730ac9418b007e59`. Their normalized contracts matched. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0029 and the canonical multi-module input boundary for roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify semantic import graphs, reachability or cycle checks, symbol/type/control-flow binding, WIR production, WVB encoding, closure of the current 4 MiB versus 16 MiB source-envelope gap, bootstrap closure, native tooling, or any OS layer.

## Portable compiler import-graph qualification

Candidate commit `09c6f54` was archived as `windvale-source-graph-09c6f54.tar.gz`, 324,949 bytes with SHA-256 `79c5f2efa7c915f7c263c7b2b125a716b54c11d7f5e01482a9b3f5b59bb0e08a`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-graph-09c6f54` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 44 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302` and .NET `10.0.10`.

The portable graph phase first requires a completely valid WVSS value, then resolves imports by exact declared module-name bytes, rejects repeated and missing targets, expands the root closure, rejects the first canonically supplied unreachable module, and proves acyclicity through deterministic zero-incoming removal plus an actual closing-edge witness. Tests cover a valid diamond, transitive reachability, duplicate/missing imports, direct and self cycles, stable module/target/location evidence, source-set failure propagation, and the exact 64-module/63-edge chain. Traversal owns exactly one immutable state byte per module through the qualified byte-construction API and retains no host object or general graph collection.

The hosted tool validated the 214,394-byte real seven-module closure—graph core, body parser, declaration parser, lexer, source-set core, byte construction, and decimal parsing—with this identical output on both hosts under the explicit 1,500,000,000-instruction ceiling:

```text
source graph status=Valid modules=7 imports=6 reachable=7
```

Both hosts produced the exact 193,522-byte `Source-Graph-Core.wvb` SHA-256 `1617419c838effd80e4ab3f167912f47f4959002a77b0b166970b1d8f30f3133`, 199,922-byte demo SHA-256 `53c976f867dccf60bf26aa74e3942cf877b048405f57dd42e462dbe0b63c9073`, and 197,203-byte hosted tool SHA-256 `75fdf22e93f154599cdf4530ebcf828eec061458c73f6ab09b00d0765e3ebdc1`.

All 19 directly retrieved dependency/downstream artifacts also matched Windows byte for byte: source-set core/demo/tool, body-parser core/demo/tool, declaration-parser core/demo/tool, lexer core/demo, byte-construction and decimal-parsing cores, object/assembler/linker cores, the 218-byte canonical object, 24-byte image, and 1,721-byte map. Their retained product identities are `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`, `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`, and `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`.

The Debian report is 14,428 bytes with SHA-256 `4d9b1f6276bcaf25a2799c33acebd68e03a4c852d79964bcd94d96004a94b1cb`; the Windows report is 14,508 bytes with SHA-256 `e7689ae4d5f71e55e03b07093a978bbb9ef10f38910fb95d5414f769d7c03eea`. Their normalized contracts matched. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0030 and the import-topology slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify declaration namespaces, nominal layout, function signatures, body/local/call binding, expression types, control flow, typed WIR production, WVB encoding, closure of the 4 MiB versus 16 MiB source-envelope gap, bootstrap closure, native tooling, or any OS layer.

## Portable declaration and signature binding qualification

Candidate commit `d57a6d8` was archived as `windvale-source-symbols-d57a6d8.tar.gz`, 355,035 bytes with SHA-256 `258c3af559b65e9eb45251ff3b68087a959d58f2d53b5eac5b0f96340f6d3c90`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-symbols-d57a6d8` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 45 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302`.

The portable symbol phase validates the qualified graph, global namespace and capability rules, record/enum/function signature structure, transitive nominal visibility, canonical record-then-enum indices, and stable failure evidence. It publishes a `WVSD 1` fixed-entry declaration directory and a bounded visibility matrix only after independently validating the directory against every accepted source. Tests cover valid cross-module signatures; every namespace/constructor/capability refusal family; empty and duplicate record, enum, and parameter cases; unknown, inaccessible, and nested-record field types; graph failure propagation; canonical nominal lookup; and corrupted directory magic, counts, entries, and trailing bytes.

The first bounded-rescan implementation exhausted 4,000,000,000 instructions after 248.1 seconds on the real compiler closure. Decision 0033 replaces that path with the independently validated directory and once-computed visibility matrix. The retained tool validated the 283,765-byte, eight-module closure under its explicit 4,000,000,000-instruction ceiling with this identical output on both hosts:

```text
source symbols status=Valid modules=8 capabilities=0 data=0 records=24 enums=14 functions=131 fields=289 members=181 parameters=582 directory-bytes=4072 visibility-bytes=64
```

Both hosts produced the exact 252,266-byte `Source-Symbols-Core.wvb` SHA-256 `79a60d3734c8c128af327b3c9e015bfa1f5b2c9d7b87abf4fb3fc2428d8bac3a`, 264,958-byte demo SHA-256 `476551cc0990588c3e782f45be83baebbcf3cd519cc0fe8dc17ccd67a7aa3714`, and 256,260-byte hosted tool SHA-256 `852dae1fe1962351e46a70e70bbdd4547814de17d75a8409e56aa0f94ccd7a4d`.

The Debian report is 14,964 bytes with SHA-256 `bd514ce1a9ba154cde689e4b1cf4cac23f0b5c50d21b5e58e40074022a04e5dd`; the Windows report is 15,049 bytes with SHA-256 `2f9f0f62b8a98e411500ef34fe697936808c74b39bcfd915c6de780f6fffd1ff`. Their normalized contracts matched. All 42 directly retrieved top-level verifier artifacts, totaling 3,102,891 bytes, matched Windows byte for byte; these include the complete syntax/source-set/graph/symbol chain, Foundation dependencies, object/assembler/linker cores, canonical objects, images, maps, and existing golden modules. After evidence retrieval, the resolved exact QA directory and transferred archive were removed and confirmed absent.

This qualifies Decision 0033 and the declaration/signature slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify body/local/call binding, expression types, control flow, typed WIR production, WVB encoding, closure of the 4 MiB versus 16 MiB source-envelope gap, bootstrap closure, native tooling, or any OS layer.

## Portable body, local, and call binding qualification

Candidate commit `9185b28af7a7a7802d8606162ef7a5413d82dc33` was archived as `windvale-source-bindings-9185b28.tar.gz`, 2,583,180 bytes with SHA-256 `4f6c8d12ea80a52ad9d9cf989b80ac1e6cb1d2f9d266ad9e71effa3afb81c2ef`, transferred with the same digest, and verified from that exact archive in `/tmp/windvale-source-bindings-9185b28` on the isolated Debian QA host. Windows and Debian GNU/Linux 12 x64 both completed the Release build with zero warnings/errors, all 46 tests, and the complete native CLI verifier. The QA host used .NET SDK `10.0.302`.

The portable binding phase validates the qualified source-symbol result, assigns deterministic parameter/local slots and source scopes, binds local/global reads and assignments, resolves Foundation intrinsics, record constructors, functions, and declared capabilities through the visibility matrix, checks call arity, and returns stable failure evidence. It publishes `WVLB 1` only after independently validating its header, exact length, canonical per-function ranges, declaration ownership, binding order/kind/slot, shape identities, identifier spans, scope spans, and trailing data.

A separate collect-then-bind prototype repeated body parsing and directory reconstruction and exceeded the 4,000,000,000-instruction ceiling. A combined prototype that still rebuilt the growing binding directory per statement failed after approximately 276 seconds; direct payload access reduced that failure to approximately 264 seconds but still retained per-symbol source slicing. The accepted packed-span implementation passes raw binding ranges through local lookup and compares global names against absolute WVSS source offsets. It completed the real nine-module closure below the unchanged ceiling with identical output on both hosts:

```text
source bindings status=Valid modules=9 functions=177 parameters=777 locals=896 reads=7937 assignments=602 calls=1344 directory-bytes=62044
```

Both hosts produced the exact 321,127-byte `Source-Bindings-Core.wvb` SHA-256 `e9f15ed16a627ae2f96feee001dd0dd7272d744566022e9b353aa79a351ed7d4`, 328,438-byte demo SHA-256 `d0007e74e697398d3a4cf52a5ee3143a5f624036f3665f8e2d610674b26eb72e`, and 324,035-byte hosted tool SHA-256 `dc3911680d5ea22890adfad9c3cf7156c386824591d16c9c39ada677c2dfd8d8`.

Windows Qualification completed in approximately 1,005.8 seconds with a 526.833-second suite; Debian completed in approximately 1,066.4 seconds with a 538.947-second suite. The focused source-binding test took 2.877 seconds on Windows and 2.691 seconds on Debian. The Windows report is 15,561 bytes with SHA-256 `cb17433f772f0336208e39623be65726b3b9d57525afa5fc7a53a80f93af9972`; the Debian report is 15,471 bytes with SHA-256 `f25adeb591fa8547ffadbd38c61f1a1f667cbbf57f1e909e9e48a520fb3e54e8`. Their normalized contracts matched.

All 45 directly retrieved top-level verifier artifacts, totaling 4,076,491 bytes, matched Windows byte for byte. The transferred Debian artifact bundle was 1,188,300 bytes with SHA-256 `a91577a01962854ee7f0f5f806f860adfd77698c43ecea2c32cd8e03c098b7b5`. These artifacts include the complete lexer/parser/source-set/graph/symbol/binding chain, Foundation dependencies, object/assembler/linker cores, canonical objects, images, maps, and existing golden modules. After evidence retrieval, the exact resolved QA directory, source archive, and artifact bundle were removed through `/bin/sh` and independently confirmed absent.

This qualifies Decision 0034 and the body/local/call slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify complete expression types, field/operator validation, control-flow proof, typed WIR production, WVB encoding, closure of the 4 MiB versus 16 MiB source-envelope gap, bootstrap closure, native tooling, or any OS layer.

## Canonical typed source-IR qualification

Candidate commit `bf77f70b08f332deda9ea3a1691e262e1426c1c1` was archived as `windvale-source-wir-bf77f70b08f3.tar.gz`, 2,612,325 bytes with SHA-256 `b9cf019a5f568a7843a2190af2abcf8013cd270a019874f14cf6ded530ce460e`. The same archive digest was verified before extraction on Windows and after transfer to `/tmp/windvale-source-wir-bf77f70b08f3` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 47 tests, and the complete native verifier.

The portable phase consumes validated WVSS, WVSD, and local WVLB evidence; performs complete implemented expression typing, field/operator/data/call checks, explicit block and temporary construction, and return/reachability proof; and publishes five packed `WVIR 1` sections only after independent structural and semantic validation. Failure precedence reuses full body binding as a diagnostic oracle without imposing repeated global binding work on the successful focused path. The hosted control-flow fixture produced the same output on both hosts:

```text
source wir status=Valid modules=1 functions=8 blocks=11 operations=44 temporaries=36 operands=29 directory-bytes=3200
```

Both hosts produced the exact 490,693-byte `Source-Wir-Core.wvb` SHA-256 `ebea7beb54939e9f3b396905859ca25bef49c5765bebccd645860fb1741debfe`, 496,484-byte demo SHA-256 `7d7699cc110284274fb17fa7fd095d7566660f037ad3e3a067ebdeb68e5dd5f3`, and 492,635-byte hosted tool SHA-256 `c6a1939d2199fa62606f1351beaed3c96a8360ccc20b032b54760ee51f6974a8`.

Windows Qualification completed in approximately 1,090.7 seconds; Debian completed in approximately 1,118.8 seconds with a 600.373-second suite. The Windows report is 15,561 bytes with SHA-256 `fc619e2a1588413ae4110426f9c8d43eacb5bf70a91d1ec5a9bc22c94007c971`; the Debian report is 15,471 bytes with SHA-256 `e7499f6baa56af09bbe9fcf7416218680310f3d424758228c12f8eba87695e1c`. Their normalized contracts matched.

All 48 directly retrieved portable artifacts, totaling 5,624,431 bytes, matched Windows byte for byte. The transferred Debian evidence bundle was 1,644,339 bytes with SHA-256 `9b34cc9206645a4fe78ec4f7331140d129f3fe354f37c84f8a0bb9dc5b2cb5c7`. These artifacts include the complete lexer/parser/source-set/graph/symbol/binding/WVIR chain, Foundation dependencies, object/assembler/linker cores, canonical objects, images, maps, and existing golden modules. After evidence retrieval, the exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0035 and the expression/type/control-flow/WVIR slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify the later Decision 0036 function-only WVIR-to-WVB candidate, execution of WVIR, optimization, closure of the 4 MiB versus 16 MiB source-envelope gap, bootstrap closure, native tooling, or any OS layer. The ten-module self-lowering performance target remains separate under the unchanged instruction ceiling.

## Initial Windvale-written WVB backend qualification

Candidate commit `ca5699617d713b4da3689cd69b0b165abc9c090e` was archived as `windvale-source-wvb-ca5699617d71.tar.gz`, 2,634,435 bytes with SHA-256 `cefcfaaf355f4578c1e180363a4c65ff8efcf1defba5c3080ec5b8692b00906a`. The same digest was verified before extraction on Windows and after transfer to `/tmp/windvale-source-wvb-ca5699617d71` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier. The later rebase dropped two duplicate verifier-maintenance commits already present upstream; published implementation commit `d65d286` and the qualified candidate share the exact Git tree `bfe879b67d5f6f197b1ace0cdd9b0ad221f94906`.

The backend consumes independently validated `WVIR 1`, accepts one portable ordinal function-only module over `void`, `i32`, `u8`, `u32`, and `bool`, assigns each temporary a WVB local, measures every block and function before emission, and publishes a complete canonical seven-section WVB 1.6 value. The four-function differential fixture exercises calls, mutable locals, control flow, arithmetic, comparisons, and boolean negation. Windvale and Stage 0 both produced the exact 815-byte module SHA-256 `9ccfed0509e84bfc63979c6dc13170c14762efbdaa448b4c5894325f31aa7761`; the mandatory verifier accepted it and runtime execution returned `6`.

Both hosts produced the exact 517,874-byte `Source-Wvb-Core.wvb` SHA-256 `d4846b2c0eed11e35a3f715e61efd84c676c1055c340c08acf582a2558bca9db`, 519,338-byte demo SHA-256 `d2477d6de0e90753c3f93b9ffc9db71da02a30472aca0a813ee4b6bf3ef5ec16`, and 519,455-byte hosted tool SHA-256 `58a337338aa98a225c563a49cfdffc9133988d332c1000391b99c0ef31e2edac`.

Windows Qualification completed in approximately 950.4 seconds with a 493.395-second suite; Debian completed in approximately 949 seconds with a 498.272-second suite. The focused backend test took 2.875 seconds on Windows and 2.629 seconds on Debian. The 15,561-byte Windows report has SHA-256 `a61946c88aee3c2509b5300af938a7503c9af11bd024ceb1aa2ae5edc881c450`; the 15,471-byte Debian report has SHA-256 `6f1c3002b5f4937cd50127a6c2fe1cd366f8a4a4063001dc12b5fa8cec5ba409`. Their normalized contracts matched.

All 53 directly retrieved portable artifacts, totaling 7,199,036 bytes, matched Windows byte for byte. The transferred Debian evidence bundle was 2,105,495 bytes with SHA-256 `d4490dbe5368d1c8b4b43f9b8b734980700f743e13238be0fb85e0938da94357`. These artifacts include the complete lexer/parser/source-set/graph/symbol/binding/WVIR/WVB chain, Foundation dependencies, object/assembler/linker cores, canonical objects, images, maps, and existing golden modules.

This qualifies Decision 0036 and the first executable code-generation slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify data or nominal metadata lowering, canonical declaration/index remapping, capabilities, imports, full compiler bootstrap closure, native tooling, or any OS layer. Those boundaries remain explicit deterministic rejections in this backend version.

## Canonical remapping and static-data backend qualification

Exact candidate commit `636627cfd6d5e81094c0b8b076c0391ee00eb228`, tree `04905d85d364dd248408375f18f4cf618290c226`, was archived as `windvale-wvb-data-636627cfd6d5.tar.gz`, 2,643,142 bytes with SHA-256 `2509c28c7b0f680e4ca3f4bbb17604df88ec18b68c0a589a7bf97f7ede54ceab`. The digest was retained across transfer and the exact archive was extracted to `/tmp/windvale-wvb-data-636627cfd6d5` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The extended backend translates canonical WVSD declaration identities to independently ordered WVB function and data indices; accepts interleaved declarations, `[i32]`, text, bytes, text/bytes shapes, and primitive Foundation intrinsics; decodes strict escaped Unicode including surrogate pairs; and interns literal values deterministically with collision-free synthetic names. The original fixture remains the exact 815-byte WVB module SHA-256 `9ccfed0509e84bfc63979c6dc13170c14762efbdaa448b4c5894325f31aa7761` and executes with result `6`. The deliberately unsorted and interleaved data/text fixture produces the exact 1,651-byte WVB module SHA-256 `5d0779925bee06b8e27afb5ccedd995fc83cbd6aa71954911a644cf078c71704` from both Stage 0 and Windvale and executes with result `13`.

Both hosts produced the exact 554,525-byte `Source-Wvb-Core.wvb` SHA-256 `c410f775e6c6e5a8a40678a5caf4e7a07a37c4dcf711b2f272f11cc1796d5d8d`, 555,160-byte demo SHA-256 `d376b66312dc9005540482f3adfe6be10b6ec8a2fbd9fcbb86c3a412e70e75fa`, and 555,049-byte hosted tool SHA-256 `364c47c70f04f0133a35ce07dcdfeb5eedbcaaf8acbedd8e002c8c6d93fa867f`.

Windows Qualification completed in 902.4 seconds with a 457.742-second suite and 4.585-second focused backend test. Debian Qualification completed in 916.4 seconds with a 471.285-second suite and 4.701-second focused backend test. The 15,561-byte Windows report has SHA-256 `a61946c88aee3c2509b5300af938a7503c9af11bd024ceb1aa2ae5edc881c450`; the 15,471-byte Debian report has SHA-256 `6f1c3002b5f4937cd50127a6c2fe1cd366f8a4a4063001dc12b5fa8cec5ba409`. Their normalized contracts matched exactly with canonical SHA-256 `c7889bc7535d1cba8363589ff8f61db4fe3aded469161b82fbf818071b4a8015`.

All 55 directly retrieved portable artifacts, totaling 7,310,405 bytes, matched Windows byte for byte. The transferred Debian evidence bundle was 2,146,953 bytes with SHA-256 `b3019c8a5062c1f5566c83b218aecae828d9c257b95389a80df10c27e0b4925e`. After retrieval and comparison, the resolved exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0037 and the primitive static-data/canonical-remapping slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify nominal metadata, capabilities, imports, multi-module backend translation, full compiler bootstrap closure, native tooling, or any OS layer. Those boundaries remain explicit deterministic rejections.

## Nominal types backend qualification

Exact candidate commit `f39ff73913177de9e0f03896074262001d4eee00`, tree `dda7b94a33474f2f78970b4617dded119dd30cd4`, was archived as `windvale-wvb-nominal-f39ff7391317.tar.gz`, 2,648,719 bytes with SHA-256 `6dcc9ac0e80764a130e99c3d76600cba6fb00bf9eccc91a7150cbaaac6f12b4c`. The digest and size were retained across transfer, the Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-wvb-nominal-f39ff7391317` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The backend now emits canonical record-then-enum Types metadata, preserves field/member declaration order, encodes nominal parameters/results/locals/temporaries, and lowers immutable record construction/field access plus enum constants, equality, inequality, and names through the existing WVB 1.6 operations. The deliberately interleaved fixture produces the exact 1,781-byte WVB module SHA-256 `1366b543a28a1921aca6198bca9eaaf5eeeb97766405d5efcdeff9d27cfca57a` from both Windvale and Stage 0, passes mandatory verification, and executes with result `11`. The preceding 815-byte primitive fixture and 1,651-byte data/text fixture also retain exact Stage 0 equality and results `6` and `13`.

Both hosts produced the exact 564,844-byte `Source-Wvb-Core.wvb` SHA-256 `fc08eb2cf97b9e67ca7393300fb2e0e3e743d65d12c01642dd650c669abfcbb4`, 565,332-byte demo SHA-256 `28370090024ee90672b1270e2392cacbbfb7b26c5e449285ecc49a2af2c575ad`, and 565,221-byte hosted tool SHA-256 `433f4f5249c7995a2582d8e8c89cc4289b1eec539929a21c9cf7882ecbc9816f`.

The focused backend test took 7.382 seconds on Windows, and the complete 48-test Standard suite took 434.111 seconds. Windows Qualification completed in 906.282 seconds with a 458.742-second suite; Debian Qualification completed in 913.811 seconds with a 465.247-second suite. The 15,561-byte Windows report has SHA-256 `a61946c88aee3c2509b5300af938a7503c9af11bd024ceb1aa2ae5edc881c450`; the 15,471-byte Debian report has SHA-256 `6f1c3002b5f4937cd50127a6c2fe1cd366f8a4a4063001dc12b5fa8cec5ba409`. Their normalized contracts matched exactly with canonical SHA-256 `c7889bc7535d1cba8363589ff8f61db4fe3aded469161b82fbf818071b4a8015`.

All 57 directly retrieved portable artifacts, totaling 7,344,630 bytes, matched Windows byte for byte. The transferred Debian evidence bundle was 2,157,969 bytes with SHA-256 `6d9bbb0f72ca111e65526cb9e2cefb4ec15113531e504aaa80ba3ae314dbbcaf`. After retrieval and comparison, the resolved exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0038 and the nominal record/enum slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify capabilities, imports, multi-module backend translation, closure of the source-envelope or repeated-body-traversal limits, full compiler bootstrap closure, native tooling, or any OS layer. Those boundaries remain explicit deterministic rejections or separately measured closure work.

## Capability/profile backend qualification

Exact candidate commit `98117c15255ce5a95d41ca13e43f92a4af77ef98`, tree `9127f1f7ef47d0c14b1bc06d012919eef20f0a2b`, was archived as `windvale-wvb-capabilities-98117c15255c.tar.gz`, 2,653,455 bytes with SHA-256 `2e5171f40d027f5bfd9b8c79d5fa5b8f5a3ac745720bdcf80a0302c5ada9f126`. The digest and size were retained across transfer, the Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-wvb-capabilities-98117c15255c` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The backend now preserves every validated portable, hosted, or system profile, emits declared capabilities in canonical ordinal order from the existing seven-entry Seed catalog, translates WVSD capability identities to WVB indices, and lowers WVIR `Callˉcapability` to WVB `call.capability`. The deliberately unsorted hosted fixture declares and calls all seven capabilities, covers every catalog parameter/result signature, passes mandatory verification, exposes canonical indices `0` through `6`, and executes its authorized no-argument path with result `0` and no file mutation. Windvale and Stage 0 both emit its exact 849-byte WVB module with SHA-256 `1df4503a21abf5f2c0b0307ac2dc79402bc8550ec5e4a016df43fdeb8197d528`.

Both hosts produced the exact 567,387-byte `Source-Wvb-Core.wvb` SHA-256 `4f8738e60e152e8cb20b8aa85792536303c5a05c42636205b426d826f65f3aa6`, 567,964-byte demo SHA-256 `34c08767a264b75bf552583dd868720306a04b99a03e7324b93c96bc6046eead`, and 567,620-byte hosted tool SHA-256 `3862a74e7f0b1a3fc42dc043a6dcbe14651bec15b264fe7b4c65574f1a4c16c7`.

The focused backend test took 8.695 seconds on Windows. Standard verification completed in 445.034 seconds, with 441.254 seconds in the complete 48-test suite. Windows Qualification completed in 908.170 seconds with a 458.146-second suite; Debian Qualification completed in 926.189 seconds with a 474.315-second suite. The 15,561-byte Windows report has SHA-256 `a61946c88aee3c2509b5300af938a7503c9af11bd024ceb1aa2ae5edc881c450`; the 15,471-byte Debian report has SHA-256 `6f1c3002b5f4937cd50127a6c2fe1cd366f8a4a4063001dc12b5fa8cec5ba409`. Their normalized contracts matched exactly with canonical SHA-256 `c7889bc7535d1cba8363589ff8f61db4fe3aded469161b82fbf818071b4a8015`.

All 59 directly retrieved portable artifacts, totaling 7,353,902 bytes, matched Windows byte for byte. The transferred Debian evidence bundle was 2,160,660 bytes with SHA-256 `773b5694ae9085cebe26a4093db22f8296f418bc4e79040c6cf259ef2a02a5d5`. After retrieval and comparison, the resolved exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0039 and the capability/profile slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not qualify imports, multi-module backend translation, closure of the source-envelope or repeated-body-traversal limits, full compiler bootstrap closure, native tooling, or any OS layer. Those boundaries remain explicit deterministic rejections or separately measured closure work.

## Static multi-module backend qualification

Exact candidate commit `cb1db235ef1ecf9697693f260516d0e241ced012`, tree `32fb95efd3ff656b846c92c843973bacafbf9de1`, was archived as `windvale-wvb-modules-cb1db235ef1e.tar.gz`, 2,658,772 bytes with SHA-256 `dc8ae327e7ca56f7f0d4025ee4b7fa09dfc8e64b51f05d972ec735becac8cd97`. The digest and size were retained across transfer, the Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-wvb-modules-cb1db235ef1e` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The backend now lowers all one to 64 modules admitted by the existing WVSS/WVSD/WVLB/WVIR contracts. The three-module differential fixture covers cross-module calls and nominal values, a dependency-owned text literal, root explicit data, synthetic-name collision avoidance, global canonical function/data/type order, and root-only exports. Windvale and Stage 0 both emit its exact 1,030-byte WVB module with SHA-256 `7279011a12f3d2becc1e9775fb92bd7c74b8760b2c94f13a282d71c0849f8e6f`; it passes mandatory verification, exposes only `Main`, and executes with result `42`. Reversing the canonical dependency order returns `Sourceˉwir` and result `1` without creating an output file.

Both hosts produced the exact 571,555-byte `Source-Wvb-Core.wvb` SHA-256 `b00677d82b90c7aa5dfe486cf7c8675658fd37a9fc560a3631004056f28b5cbf`, 573,282-byte demo SHA-256 `19da326967e17a06f149efbb9cbd35c89ad9ec156f7f74833ea8287141cb419e`, and 572,786-byte hosted tool SHA-256 `ce3ae94685c07b025e8cd8ea95f53df01819cb1946eaf3cf442e3fa38ad8cb5d`.

The focused backend test took 10.354 seconds on Windows. Standard verification completed in 441.8 seconds, with 435.850 seconds in the complete 48-test suite and 8.956 seconds in the backend test. Windows Qualification completed in 916.6 seconds with a 465.074-second suite and 9.203-second backend test; Debian Qualification completed in 927.5 seconds with a 477.445-second suite and 9.682-second backend test. The 15,561-byte Windows report has SHA-256 `a61946c88aee3c2509b5300af938a7503c9af11bd024ceb1aa2ae5edc881c450`; the 15,471-byte Debian report has SHA-256 `6f1c3002b5f4937cd50127a6c2fe1cd366f8a4a4063001dc12b5fa8cec5ba409`. Their normalized contracts matched exactly with canonical SHA-256 `c7889bc7535d1cba8363589ff8f61db4fe3aded469161b82fbf818071b4a8015`.

All 61 directly retrieved portable artifacts, totaling 7,370,614 bytes, matched Windows byte for byte. The transferred Debian evidence bundle was 2,165,744 bytes with SHA-256 `7aa65463230eca2d418efa8b05c57acee6bcdc6226f1da55019de8929011b7c0`. After retrieval and comparison, the resolved exact Debian QA directory, source archive, failed-candidate directory/archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0040 and the static multi-module backend slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8 and does not close the 4 MiB WVSS envelope, repeated local/body traversal costs, full compiler bootstrap closure, native tooling, or any OS layer. Those remain explicit later slices.

## Fused local-discovery and typed-WVIR qualification

Exact candidate commit `b1241157310bc597dbdf0d24146f4d81f0128712`, tree `eda80c98a2706ddb22abcc50deb5e60961cd2981`, was archived as `windvale-wir-fusion-b1241157310b.tar.gz`, 2,662,478 bytes with SHA-256 `0505bf1a5b1259e2625f2bb12c8f13975f94a22eb03580f6673ebfae7114fb8c`. The digest was retained across transfer, the Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-wir-fusion-b1241157310b-20260731` on the isolated Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The successful typed-WVIR path now prepares parameters, lowers each initializer before making its local visible, carries the growing function-private WVLB payload through nested blocks, and publishes independently validated WVLB and WVIR directories after one statement traversal. The complete standalone binding API remains qualified, and typed-lowering failures still run the local-only and full binding diagnostic oracles. The CLI's opt-in `--report-steps` reports `203` executed instructions for `Sum-Data.wvb` without changing default output.

Both hosts produced the exact 337,586-byte `Source-Bindings-Core.wvb` SHA-256 `5440db53fa42f819321d91f67a2440cf128a8685e564b057d61d1b7ee9a2e1c3`, 344,471-byte binding demo SHA-256 `1dabd3cb09339e63f2d8495eb27258fce1054c5163f92a5afd3258995d5bb8e5`, and 340,068-byte binding tool SHA-256 `47dab8589a4ca5a7c460be981fc183a5efcdb551174e1bb734b415f4f4198cb8`. They also produced the exact 501,133-byte `Source-Wir-Core.wvb` SHA-256 `95b63f627f05efbdc95d65da7654a69dde89a7e7ec38e2ed80d3f5cfbff5c17f`, 506,834-byte WVIR demo SHA-256 `23da120afffa4f450f703fc17a9719f1ed6c9a080ced67fbcd23bb7aa232f42e`, and 502,985-byte WVIR tool SHA-256 `2d8cbae0f87e2043eb18e43b6e6659044ada2f8c453cc17ca1b406efb6df517e`.

The downstream backend remained semantically identical while its embedded dependency changed: both hosts produced the exact 579,187-byte `Source-Wvb-Core.wvb` SHA-256 `bb259fe85c6593e3e13091faf77252468fd85ad5d0d2f73e4d6f450bacbd83a6`, 580,914-byte demo SHA-256 `a873e870d005fb49cd48043d5f8d3f54c33d889bc2ab5f118f7c09b0f90f5fa0`, and 580,418-byte tool SHA-256 `b85ff9d1be9815304617be13d532636ab0a5a3ce2d018a610565ca067e52245c`. All five differential source-to-WVB fixtures retained their exact Stage 0 equality and runtime results.

The focused 24-test compiler area passed in 60.194 seconds. Standard verification completed in 455.1 seconds with a 446.139-second complete suite. Windows Qualification completed in 896.1 seconds with a 450.790-second suite; Debian Qualification completed in 908.2 seconds with a 458.664-second suite. The 15,561-byte Windows report has SHA-256 `3b17531a91951f1d3b3234740c984020c58e00f7aa8587d7960a76ca1b040329`; the 15,471-byte Debian report has SHA-256 `e2bbbb13a51da4d5fb3492b618cd92cc3cd9b7845b7ad8066ac08e8532e3a8a0`. Their normalized contracts matched exactly with canonical SHA-256 `3582a9c6bbe2b156af3657daec307bfaddbab7bf844fe5c48f0cd9465dbf788d`.

All 61 directly retrieved portable artifacts, totaling 7,418,376 bytes, matched Windows byte for byte. The Debian evidence bundle was 2,175,902 bytes with SHA-256 `ecdc2168a4d17a72a32520399b07d5629b6fd062bf45f90bb686588c28d16b27`. After retrieval and comparison, the resolved Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

The exact ten-module typed-IR experiment still reaches bounded runtime diagnostic `WVR3011` at the unchanged 4,000,000,000-instruction ceiling; a retained-host run took 209.130 seconds versus 222.393 seconds for the separate-pass baseline, but elapsed time is not portable progress because both execute the full ceiling. This qualifies Decision 0041's traversal fusion, not full compiler self-hosting, closure of the WVSS source envelope, native tooling, or any OS layer.

## Bounded lexical dispatch and function profiling qualification

Exact candidate commit `5d67463d42fc81ca82825da7c3894e16193391f7`, tree `bee0468d61f7d700989b16ffabcc180a1e961b0d`, was archived as `windvale-lexer-dispatch-5d67463d42fc.tar.gz`, 2,669,416 bytes with SHA-256 `e0cd211f9d85b61cead81362d847ff505f74ef7e10104d5907f64b82469d9256`. The digest was retained across transfer, the Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-lexer-dispatch-5d67463d42fc-20260731` on the isolated E-Worker Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The lexer now dispatches the 28 exact keywords by bounded byte length and first ASCII byte, classifies ordinary ASCII identifier continuation directly, and enters full Unicode-whitespace classification only for byte prefixes that can begin an accepted scalar. The original fixed lexer workload falls from 822,959 to 590,813 instructions, a 28.2% reduction with identical result. The focused typed-WVIR fixture falls from the qualified 8,074,045-instruction baseline to 5,735,695 instructions, a 29.0% reduction. The expanded lexer fixture covers every dispatch bucket, identifier length boundaries, near-keywords, and representative accepted Unicode whitespace; it executes 1,438,364 instructions and returns `0`.

The C# reference runtime and CLI now expose opt-in deterministic per-function instruction evidence without allocating counters or changing output by default. The exact `Sum-Data.wvb` profile totals 203 instructions: 163 in `Main` and 40 in `Add`. Collection resets for each run and retains partial counts after bounded failure. On the real compiler input, the profile identifies repeated symbol-directory decoding, ordinal span comparison, and remaining lexer entry work as the next structural costs.

The real nine-module binding closure remains valid at 2,972,056,275 instructions and reports 189 functions, 827 parameters, 945 locals, 8,335 reads, 612 assignments, 1,479 calls, and 65,704 directory bytes. The exact ten-module typed-WVIR input still returns bounded diagnostic `WVR3011` at the unchanged 4,000,000,000-instruction ceiling. This milestone improves and measures the compiler; it does not claim compiler self-hosting.

The focused 24-test compiler area passed in 49.846 seconds. Standard verification completed in 381.4 seconds with a 376.454-second complete suite. Windows Qualification completed in 795.6 seconds with a 396.973-second suite; Debian Qualification completed in 788.7 seconds with a 404.135-second suite. The 15,561-byte Windows report has SHA-256 `c5f327cb1b16507dd6199dfaff2ec99d1ac7479f92b6a01834e4f44ac01b1dd1`; the 15,471-byte Debian report has SHA-256 `9b59d87ec1de2a29755c12cf8e50128791bf7c8b7fc5f795e071b18e3bab686b`. Their normalized contracts matched exactly with canonical SHA-256 `e708c79eb98e8d7caf826479a96dfcef09385c116b69f2af90240af41dc07a15`.

All 61 directly retrieved portable artifacts, totaling 7,496,881 bytes, matched Windows byte for byte. The Debian evidence bundle was 2,209,127 bytes with SHA-256 `2f204dbbc367b6eafe3d4c0af528bf7d153df4f9a8f8c7080729055c181ba29d`. After retrieval and comparison, the exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0042. It does not complete Phase 7 or Phase 8, close the full compiler source envelope, complete the exact typed-WVIR input below its fixed ceiling, qualify compiler self-hosting, produce native Windvale tools, or implement any Windvale OS layer.

## Compiler implementation role-layout qualification

Exact candidate commit `4fdc6bfcb864251ab18584933319abe837703612`, tree `8637171764f2c70c290fa547fd9aba0bb8c474cf`, was archived as `windvale-compiler-roles-4fdc6bfcb864.tar.gz`, 2,670,922 bytes with SHA-256 `da9477cc36f0bc66a03c8f03bc3e6529ec9785c5c2e4603c978e58c4dcd222f8`. The digest was retained across transfer, the updated Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-compiler-roles-4fdc6bfcb864-20260731` on the isolated E-Worker Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The candidate moves the nine Windvale compiler sources from `Compiler/Bootstrap` to `Compiler/Windvale` and the eleven C# compiler project files from `Compiler/Windvale.Compiler` to `Compiler/Reference`. Nineteen implementation files are byte-identical moves; the project file changes only its descriptive text. The public C# project, assembly, and namespace remain `Windvale.Compiler`. Solution entries, project references, embedded resources, verifier paths on both hosts, source-name evidence, example commands, specifications, and verification-routing fixtures all follow the new ownership paths. No old live path remains.

The focused 24-test compiler area passed in 46.028 seconds. Standard verification completed in 369.1 seconds with a 364.917-second complete suite. Windows Qualification completed in 764.3 seconds with a 382.984-second suite; Debian Qualification completed in 749.2 seconds with a 371.853-second suite. The 15,561-byte Windows report has SHA-256 `c5f327cb1b16507dd6199dfaff2ec99d1ac7479f92b6a01834e4f44ac01b1dd1`; the 15,471-byte Debian report has SHA-256 `9b59d87ec1de2a29755c12cf8e50128791bf7c8b7fc5f795e071b18e3bab686b`. Their normalized contracts matched exactly with canonical SHA-256 `e708c79eb98e8d7caf826479a96dfcef09385c116b69f2af90240af41dc07a15`.

All 61 directly retrieved portable artifacts, totaling 7,496,881 bytes, matched Windows byte for byte and retained the exact Decision 0042 identities. The Debian evidence bundle was 2,209,135 bytes with SHA-256 `9d33a469a2cca655a98caca850b56e245d73f16eb9b551e1ade9923ea149d838`. After retrieval and comparison, the exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0043 and the role-based repository layout. It does not qualify compiler self-hosting, close the source envelope or four-billion-instruction exact workload, produce native Windvale tools, advance the native backend gate, or implement any Windvale OS layer.

## Bidirectional nominal symbol-index qualification

Exact candidate commit `e37204ffcdf17b39a486466cc13f35d8ee00b4b4`, tree `8b2fa783637e46872cc783dd22f3f8ee975f4e7f`, was archived as `windvale-symbol-index-e37204ffcdf1.tar.gz`, 2,676,371 bytes with SHA-256 `c85ec6bc7d8e1a052669130455f347da57dcb5affd5eb1ddf0afc6c77dab6682`. The candidate carried provisional Decision number 0044; integration retained concurrently published OS Decisions 0044 through 0048, left 0049 available to that active sequence, and renumbered this documentation-only identity to Decision 0050. The digest and size were retained across transfer, the Linux verifier passed `/bin/sh -n`, and the exact archive was extracted to `/tmp/windvale-symbol-index-e37204ffcdf1-20260731` on the isolated E-Worker Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

The public `WVSD 1.0` declaration directory remains byte-for-byte unchanged. Private `WVSI 1.1` adds a canonical-nominal-to-directory reverse map and a directory-to-canonical-nominal forward map after the existing first-byte bucket payload. Type binding, record/enum shape lowering, and independent WVIR validation use those maps directly. Hot scans read packed directory fields without constructing rejected match records, and equality-only lookup paths reject unequal UTF-8 lengths before ordinal comparison. The symbol demo verifies the exact 4,244-byte fixture index, version 1.1, both map directions, and total construction for rejected duplicate inputs.

The real nine-module binding closure remains valid while growing to 189 functions, 827 parameters, 986 locals, 8,451 reads, 643 assignments, 1,508 calls, and 67,180 directory bytes. It completes in 2,600,859,185 instructions, down 371,197,090 or 12.5% from Decision 0042. The focused typed-WVIR fixture falls from 5,735,695 to 5,715,847 instructions. The exact ten-module typed-WVIR input still returns bounded diagnostic `WVR3011` at the unchanged 4,000,000,000-instruction ceiling; a diagnostic-only six-billion run also reached its temporary ceiling. Profiling removes directory-entry materialization and ordinal nominal ranking from the dominant costs and identifies repeated lexical/parser traversal as the next structural target.

Both hosts produced the exact 269,799-byte `Source-Symbols-Core.wvb` SHA-256 `063a9ff3cb37196b1e6d9c8fb5be39916fe9fc21fab8032b0415d5f8ab0677d2`, 283,038-byte symbol demo SHA-256 `9e7826d354d80d06702555e857f348e08a6e396cca64b11c5eda4895e7294a25`, and 273,580-byte symbol tool SHA-256 `54289a4ebfe778c6c2b6ebfb7cb7afaf3d4899af41e68a989eaa26b9dd2f28c4`. The binding artifacts are 345,074, 351,959, and 347,556 bytes with respective SHA-256 values `2c253a188c96d5bbdf7e8b81d44dbd776c27c043ba9df7cc6230972c278335e5`, `11621f87f3fe198c97ea141f41ac1a4d69bd34e03f10c17d4bd23083b3087b18`, and `2e7f2885da27a5ba12f15a831bb2429a21acb7e23ad46061009b4b2209b713f4`. The WVIR artifacts are 507,772, 513,473, and 509,624 bytes with respective SHA-256 values `e11a017226c8b357732f02f5bdc6ff581c876276165e267b1875a2daa247cef0`, `1af3a5d7523ce5ace0091e4892e35c54a4835487186c4e49e653a8af98ecc721`, and `3096f68bd7c1ab9e08b5112937d75ef6481b724ba84a9571fd4ce9076156cfa2`. The WVB backend artifacts are 585,826, 587,553, and 587,057 bytes with respective SHA-256 values `32c739a08fd70e3df8551a4c15571f5f53da8d661300f55500eb15cc2c909468`, `426fcdba5267db8390dfa301d16ea93f5391f25a4fd139ba17268736aefb306e`, and `d68581ed1e89e22eee9c59d051cd1e79e2e75104287f401697528380198c0527`.

The focused 24-test compiler area passed in 47.920 seconds. Standard verification completed in 375.1 seconds with a 369.044-second complete suite. Windows Qualification completed in 724.8 seconds with a 366.914-second suite; Debian Qualification completed in 738.0 seconds with a 379.640-second suite. The 15,561-byte Windows report has SHA-256 `e84d06f54ffc60931aed454615f45a5d57d930ea232208df98328667091f3498`; the 15,471-byte Debian report has SHA-256 `099873d6b05f2f371125e4e756952c55a28ee2ef5e6c040325b85061378aefb1`. Their normalized contracts matched exactly with canonical SHA-256 `165612d4325d8d53066ae9b7aeb6f42557aae8a884deb031a45becdcb52322a8`.

All 61 directly retrieved portable artifacts, totaling 7,546,823 bytes, matched Windows byte for byte. The Debian evidence bundle was 2,229,929 bytes with SHA-256 `7f85f60758a62659cd568bf05a92657ce994e8fc2bed8ec72b8a33dcbc99fdec`. After retrieval and comparison, the resolved exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent.

This qualifies Decision 0050 and completes the structural symbol-directory/name-ranking slice of roadmap Phase 8. It does not complete Phase 7 or Phase 8, close the 4 MiB source envelope, complete the exact typed-WVIR input below its fixed ceiling, qualify compiler self-hosting, produce native Windvale tools, advance the native backend gate, or implement any Windvale OS layer.

## Validated scan reuse and ten-module closure qualification

Decision 0055 retains checked standalone lexer/parser entry points while letting compiler loops consume already established full-source scan and cursor evidence. Source-set validation reuses its declaration summary for the body pass. A bounded byte scanner skips lexically valid function bodies while tracking strings, escapes, comments, braces, line/column coordinates, and the existing depth limit; rejected or ambiguous shapes fall back to the qualified token scanner. The checked body-span boundary adds an iterative nesting preflight so adversarial depth returns `Nestingˉlimit` without host-stack exhaustion. Nominal lookup uses the existing WVSI reverse table and first-byte canonical ranges.

The focused typed-WVIR fixture falls from Decision 0050's 5,715,847 instructions to 3,626,693, a reduction of 2,089,154 instructions or 36.55%. The focused 24-test compiler area passed in 37.927 seconds. The complete candidate Windows Standard suite passed all 48 tests in 246.417 seconds, and focused nesting-containment and golden-contract reruns also passed.

The exact ten-module typed-WVIR input now completes below the unchanged 4,000,000,000-instruction ceiling:

```text
source wir status=Valid modules=10 functions=315 blocks=5658 operations=27377 temporaries=25094 operands=21869 directory-bytes=1720804
Result: 0
Instructions: 3912239584
```

The retained margin is 87,760,416 instructions. This clears the performance entry gate for Stage 0 → Stage 1 → Stage 2 convergence but does not itself prove compiler self-hosting, byte-for-byte bootstrap convergence, closure of the 4 MiB source envelope, completion of Phase 8, native tooling, or any OS layer.

Exact candidate commit `1a4fca7e295545b3b815bbf187fc048f1a885c74`, tree `00eef5249581f48e57e20a92b4524af1e2b54420`, was archived as `windvale-scan-reuse-1a4fca7e2955.tar.gz`, 2,744,521 bytes with SHA-256 `767906a4f4e114c595ae92bfab3dbb6caf1914dc29ec25b213eec6413c092158`. The same digest was verified before extraction on the isolated E-Worker Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, all 48 tests, and the complete native verifier.

Windows Qualification completed in approximately 449.0 seconds with a 220.663-second suite; Debian completed in approximately 469.5 seconds with a 227.434-second suite. The 15,563-byte Windows report has SHA-256 `24a1b6eb0096e9fba642b0f1284f287e3d43c1d0e4a49f27157b71bae04f7efa`; the 15,473-byte Debian report has SHA-256 `2fa2b75b8e6def4183925850fb0a4df8b9da751e0d966f147b5609e1afc3f4f3`. Their normalized contracts matched exactly with canonical SHA-256 `3e0d97b8a3d68c150545f69e7c7761be61bd39443dde27b83d8288f9e7dd93d5`.

All 61 directly retrieved portable artifacts, totaling 7,733,603 bytes, matched Windows byte for byte. The Debian evidence bundle was 2,287,803 bytes with SHA-256 `7aa03b7145ca5777ab646d4a7043ea4c391ecf81638fea7a32f1722b6e2bb760`. After retrieval and comparison, the exact Debian QA directory, source archive, and evidence bundle were removed and confirmed absent. This qualifies Decision 0055 and makes Stage 0 → Stage 1 → Stage 2 convergence the next Phase 8 slice.

## Reproducible compiler bootstrap convergence qualification

Decision 0058 replaces repeated WVB symbol reranking with immutable per-kind entry/rank tables, uses reverse exact equality on equality-only validated source spans, and avoids rescanning accepted module prefixes merely to reconstruct emission coordinates. Public WVSS, WVSD, WVLB, WVIR, and WVB formats and the five differential fixture outputs remain unchanged.

The canonical inventory contains one tool root plus eleven explicit dependencies, 12 modules and 677,073 source bytes in total. Stage 0 produces a verified 599,868-byte Stage 1 compiler. On both hosts, Stage 1 compiles the same inventory in exactly 6,700,562,174 VM instructions and produces a separately verified, byte-identical 599,868-byte Stage 2 compiler. Both stages have SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066`. This proves bytecode compiler self-reproduction; it does not claim native compiler execution, a Windvale-native VM/JIT, or Decision 0057's .NET-retirement gate.

Exact candidate commit `5c1654726f0288808d2c1d3918d65ec1c59b4635`, tree `b00666be6a8b31d8868934e66751d8c6d8011cc7`, was archived as `windvale-bootstrap-5c1654726f02.tar.gz`, 2,764,889 bytes with SHA-256 `b8c7cc0193b149d5fab84b60c1d751f74f495a77090994fea6a57e4524b73051`. The digest matched after transfer, the Linux verifier was executable, and the exact archive was extracted to `/tmp/windvale-bootstrap-5c1654726f02-20260731` on the isolated E-Worker Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning builds, all 48 tests, the complete native CLI verifier, and the dedicated bootstrap verifier. Both reports identify .NET `10.0.10`.

Windows Qualification completed in 427.3 seconds with a 210.018-second suite; its bootstrap verifier completed in 386.8 seconds. Debian Qualification completed in 442.4 seconds with a 223.751-second suite; its bootstrap verifier completed in 423.3 seconds. The 15,563-byte Windows report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f`; the 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The Debian evidence bundle is 2,293,322 bytes with SHA-256 `ce0555ded58b2dd1abcd3c4301dca7b45461efb0fd3d842d31fbb19d5cccdbbc`; the separately retrieved Stage 2 compiler also matched its Windows artifact and canonical digest. After retrieval and comparison, the resolved exact QA directory, source archive, and evidence bundle were removed and confirmed absent. This qualifies Decision 0058 and completes roadmap Phase 8's bytecode self-hosting gate. The 4 MiB WVSS envelope is proven sufficient for the real compiler closure; broader source-envelope parity remains a separate future decision.

## First shared native WVB slice qualification

Decision 0059 adds compiler-owned native machine IR, an x86-64 selector, a bounded fragment verifier, a WVO sink, and a runtime-owned Windows/Linux W^X execution adapter. The backend accepts only an already verified WVB module and, for this first slice, only one exported portable `Main() -> i32` returning a constant through the canonical four-instruction WVB shape. The reference interpreter, in-memory fragment, and WVO-linked image all return `42` on both hosts. Two compilations and WVO serializations are exact; the code is `B8 2A 00 00 00 C3`, and the 79-byte WVO has SHA-256 `d69ab30a34a7281ff9911ab89220b405ad0944ede5130dd6f07c44baac1b9d6a`.

Exact candidate commit `962bb854fd8af195ec859c89355bae0e6f85ff33`, tree `f617e322bd01d533f3e139131f9dfded74037ccb`, was archived as `windvale-native-962bb854fd8a.tar.gz`, 2,776,515 bytes with SHA-256 `8f8641289dcbd00092e598b6f5977e0a0eb0ef70e9957c5a5d792c92bc205c3f`. The digest matched after transfer, and the exact archive was extracted to `/tmp/windvale-native-962bb854fd8a-20260731` on the isolated E-Worker Debian QA host. Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` both completed zero-warning Release builds, the focused native test, all 49 tests, and the complete native CLI verifier.

Windows Qualification completed in 432.4 seconds with a 219.563-second suite; Debian Qualification completed in approximately 464.3 seconds with a 235.420-second suite. The 15,563-byte Windows report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f`; the 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The Debian evidence bundle is 2,292,959 bytes with SHA-256 `7c71c21f79b32e684ddc930cb3f580645bd7000301d05e2df2abe9eca8ef4649`. After retrieval and comparison, the resolved exact QA directory, source archive, and evidence bundle were removed and confirmed absent. This qualifies the first Decision 0059 slice and advances roadmap Phase 9 without claiming arithmetic, control flow, calls, data, capabilities, traps, native compiler execution, a Windvale-written native runtime, or .NET retirement.

## Checked native i32 arithmetic and trap qualification

Decision 0060 advances the experimental shared target to `x86-64-wvb-baseline-v2` and ABI 2. Verified straight-line WVB constants and single-assignment i32 temporaries lower to numbered native values, checked add/subtract/multiply/negate operations, and one return. The x86-64 selector uses a bounded one-page frame, emits `jo` after every checked operation, and returns a packed 64-bit value/status word so every successful `i32` bit pattern remains representable. Status 1 becomes recoverable Windvale trap `WVR3007`. The fragment verifier independently decodes the entire constant or arithmetic shape, including the minimal frame, contiguous initialized slots, exact instruction encodings, overflow targets, balanced epilogues, packed status, symbol partition, and complete byte consumption before either WVO or W^X publication.

The differential computation `-(((2 + 2) - (7 * 6)) - 4)` returns `42` in the reference interpreter, W^X JIT fragment, and WVO-linked AOT image. Its 239 code bytes have SHA-256 `9ab91125f773fc56f6aafc4ac66ebb4596d2dc93f7eec713d13aaf1053558bf6`; its deterministic 341-byte WVO has SHA-256 `ef8bb7e4322ef7bce4ad9de0710a441f617bfb6dbc5e7d05b3e883fce1319cbc`. The full `i32` result range includes successful `int.MinValue`; separate add, subtract, multiply, and negate programs trap in the interpreter and native path, the add trap survives WVO linking, and five structural corruption families are rejected as `WVN3030` before execution.

Exact candidate commit `84dd9086bef352d964b6c44db7e6a25deae43c37`, tree `e3f9feb2ba50650f7e61b86f9cb4fdeecb59a2b6`, was archived as `windvale-84dd9086bef352d964b6c44db7e6a25deae43c37.tar.gz`, 2,784,775 bytes with SHA-256 `bb5e51c7fabbb23bedcd42d162b1c387751946b09ba4d9e9b785a150b77dc2da`. The digest matched after transfer to the isolated E-Worker Debian QA host. Its focused native case passed through Linux `mmap`/`mprotect`; Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` then completed zero-warning Release builds, all 49 tests, and the complete native CLI verifier.

Windows Qualification completed in 474.1 seconds with a 223.137-second suite; Debian Qualification completed in 489.4 seconds with a 235.710-second suite. The 15,563-byte Windows report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f`; the 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The Debian evidence bundle is 2,292,642 bytes with SHA-256 `6f70dde11fb4d28b7d8ced13d761c0e33905afa8d3d485e3151805b77abcc3db`. After retrieval and comparison, the resolved exact QA directory, source archive, and evidence bundle were removed and confirmed absent. This qualifies Decision 0060 and advances Phase 9 to comparisons and structured control flow without claiming calls, data, capabilities, a general native runtime, Windvale-written native execution, native self-hosting, or .NET retirement.

## Typed native blocks and forward control-flow qualification

Decision 0061 advances the experimental shared target to `x86-64-wvb-baseline-v3` and ABI 3. The machine IR owns typed i32/bool locals, typed numbered values, canonical basic blocks, and explicit jump, branch, and return terminators. The qualified lowering covers constants, mutable locals, checked i32 arithmetic, all six signed comparisons, bool equality/inequality/negation, forward branches, and early returns while requiring an empty operand stack at control boundaries. The x86-64 selector zero-initializes the complete bounded frame and resolves all internal relative targets; the independent decoder admits only exact semantic instruction groups, in-frame aligned accesses, overflow targets, forward group-boundary branches, reachable code, balanced exits, and complete byte consumption.

The primary differential program returns `42` through the reference interpreter, Windows/Linux W^X JIT fragment, and exact WVO-linked AOT image while exercising every admitted comparison and nested forward control. Its 2,428 code bytes have SHA-256 `55f7ec906db8c8e06dc89ba0a55556cd1248d3a5491585134bbacbeaa6400d44`; its 2,530-byte WVO has SHA-256 `7442f46a0a03b4870150b34ad75724e44cd9ded8b73e875db84130bb6c42f8f2`. A second differential program proves the false path. Corrupt zero initialization, comparison encodings, mid-group branches, and backward targets fail before publication; verified WVB loops remain explicitly rejected until native execution budgeting exists.

Exact candidate commit `f0a53a99b25638cd02acafba81c066be931ef5e4`, tree `e425e9d24f373a1d09694a38910b85a98359725c`, was archived as `windvale-native-control-f0a53a9.tar.gz`, 2,794,632 bytes with SHA-256 `5448e3eab034997b877ab05c7b61dc8337adc7ec78b4f095f54f6b8e898b0603`. The digest matched after transfer to the isolated E-Worker Debian QA host. Its focused native case passed through Linux `mmap`/`mprotect` in 166 milliseconds; Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` then completed zero-warning Release builds, all 49 tests, and the complete native CLI verifier.

Windows Qualification completed in 469.086 seconds with a 224.296-second suite; Debian Qualification completed in approximately 489.6 seconds with a 243.020-second suite. The 15,563-byte Windows conformance report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f`; the 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The Debian evidence bundle is 2,292,663 bytes with SHA-256 `afe0d6a69b3a67ddb1af6e7b0214b9899cc18dcd3078b1e7245292439a42196e`. After retrieval and comparison, the resolved exact QA directory, transferred source archive, and evidence bundle were removed and confirmed absent. This qualifies Decision 0061 and advances Phase 9 to execution-budgeted backward control without claiming native loops, calls, static data, capabilities, native compiler execution, Windvale-written native execution, native self-hosting, or .NET retirement.

## Dynamic native instruction-budget and backward-control qualification

Decision 0062 advances the experimental shared target to `x86-64-wvb-baseline-v4` and ABI 4. Every WVB instruction lowers with one explicit native charge; explicit terminators consume a charge while implicit fallthrough does not invent one. The executor supplies `(0, budget, budget)`, giving the same dynamic positive maximum in `RDX` under Windows and System V x64; the fragment reserves `R11`, permits exactly that many charges, and returns packed status 2 as `WVR3011` before the next instruction. The independently decoded fragment requires exact budget initialization, strict charge/semantic alternation, limit targets, both trap epilogues, control targets on charge boundaries, complete cyclic reachability, minimal initialized frames, and complete byte consumption.

A finite loop returns `42` through the reference interpreter, Windows/Linux W^X JIT fragment, and exact WVO-linked AOT image at its exact 157-instruction requirement; all paths reject 156 as `WVR3011`. A genuinely nonterminating verified loop is bounded at 50 instructions in both the interpreter and native path. The deterministic loop fragment is 1,178 bytes with SHA-256 `8b4cde486874a5dcd7eded18e0d3fcb208e244de0499e868dc8e50501bd21139`; its 1,318-byte WVO has SHA-256 `7b0d7898a83eb87818b08dfcb4081ce6ff05e91f5dd2958ba0e869ebcae735ac`. Corrupt budget sources, charge amounts, limit targets, limit statuses, and non-charge branch targets fail before publication.

Exact candidate commit `2b67c8ac9cceefa3aa78d2e93955b8987a845baa`, tree `ee00205acd5902833bb122681fb7efa629ce0dfb`, was archived as `windvale-native-loop-2b67c8a.tar.gz`, 2,800,895 bytes with SHA-256 `7ce11c11a92d5f048f737402e90b9a0467407c2092db24c68254d68b8530b7cc`. The digest matched after transfer to the isolated E-Worker Debian QA host. Its focused native case passed through Linux `mmap`/`mprotect` in 162 milliseconds; Windows x64 and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` then completed zero-warning Release builds, all 49 tests, and the complete native CLI verifier.

Windows Qualification completed in 439.535 seconds with a 216.829-second suite; Debian Qualification completed in approximately 457.5 seconds with a 233.088-second suite. The 15,563-byte Windows conformance report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f`; the 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The Debian evidence bundle is 2,292,662 bytes with SHA-256 `15856f84dcc916adec24e63b992cfe2c55af023081fa51c8bbd6dc7994cb5678`. After retrieval and comparison, the resolved exact QA directory, transferred source archive, and evidence bundle were removed and confirmed absent. This qualifies Decision 0062 and advances Phase 9 to shared-budget native calls and static data without claiming calls, data, capabilities, native compiler execution, Windvale-written native execution, native self-hosting, or .NET retirement.

## Shared-budget native calls and static-data qualification

Decision 0063 advances the experimental shared target to `x86-64-wvb-baseline-v5` and ABI 5. A six-argument host bridge supplies identical instruction and call-depth maxima under Windows and System V x64; `R11` and `R10` carry them through the complete internal function graph. As many as four i32/bool parameters use a shared volatile-register convention. Calls preserve exact instruction accounting, return one packed value/status, propagate traps, and bound recursion as `WVR3004`. Immutable i32 array data uses unsigned bounds checks and exact RIP-relative patches; failures map to `WVR3005`; the WVO sink separates `.text` and `.rodata` while its linked image remains byte-identical to the flat JIT fragment.

The focused case covers zero- through four-parameter functions, Boolean results, nested and recursive calls, noncommutative argument order, local preservation, exact depth exhaustion, direct and function-mediated data access, data loops, exact WVB instruction accounting, bounds traps, WVO relocation/link equality, and hostile-patch rejection. A representative program loops over immutable data, calls a two-parameter `Add`, and returns `29` in the reference interpreter, W^X JIT fragment, and WVO-linked AOT image.

Exact candidate commit `1af2eca1e3ef8443daa3354ddfe03935821ae23d`, tree `bcdc8a138767a34866514720bc721032759a2dc3`, was archived as `windvale-native-calls-1af2eca.tar.gz`, 2,810,114 bytes with SHA-256 `07163bdb75b6573bd5c9a67dde6390430502bd2e39552e1073b611f90d8578ee`. The digest matched after transfer to the isolated E-Worker Debian QA host, where the focused native case passed through Linux `mmap`/`mprotect` in 219 milliseconds with a zero-warning build.

Windows Qualification completed in 490.3 seconds with a 251.091-second suite; Debian Qualification completed in 481 seconds with a 236.437-second suite. Both hosts used .NET SDK `10.0.302`, passed zero-warning Release builds, all 49 tests, and the complete native CLI verifier. The 15,563-byte Windows conformance report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f`; the 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The 2,298,177-byte Debian evidence bundle has SHA-256 `32dc646999aca8925730d2840db8c389040bdc15982224f5976f8fd26d48173e`. After retrieval and comparison, the resolved exact QA directory, transferred source archive, and evidence bundle were removed and confirmed absent. This qualifies Decision 0063 and advances Phase 9 to first-tool adoption without claiming capabilities, broader values/data, native compiler execution, Windvale-written native execution, native self-hosting, or .NET retirement.

## First shared native WVB in Windvale OS qualification

Decision 0064 reuses the qualified ABI-5 selector, fragment verifier, and WVO sink in the first downstream Windvale OS consumer. `Native-Wvb-Probe.wv` is an ordinary portable module: it loops across immutable i32 data, calls `Add`, and returns `29`. Its canonical WVB is 502 bytes with SHA-256 `1f384f77c4e1c718a331aaa1a3c1f1e4173bbae9d870ec9023d70c7b15c1f7ef`; the 2,296-byte WVO has SHA-256 `338d05395502cc34dc5ac1a99626e0507faf3503ae7d5a3016c52ac140139ee5`. An independently verified 269-byte bridge object with SHA-256 `b345f42813fb5a20829a28882e03820a05e815982689478dc2b17ac593dca88d` supplies exact instruction/depth budgets 203/2 and accepts only packed result 29 before the existing special system-profile Main can run.

Exact candidate commit `708242e2ad84f5fe9337588de5c2d02bc3f50a38`, tree `7ce94ee13aa0110d616a2adb2767c5b8e9ac0da4`, passed a zero-warning Release build, all 15 OS tests, all 48 Development tests, and all 49 Standard tests including the qualification-only golden contract. The pinned environment check accepted QEMU `11.0.0`, Q35/TCG, and the exact OVMF code/variables digests. A real boot produced the deterministic 9,728-byte PE32+ UEFI application with SHA-256 `16c225916be855ca0aa27bcdacb56e38c08b79e2270f12e9040bffe343873fb3`, emitted the complete version-7 serial marker including `native-wvb=pass`, and returned the guest-controlled host exit code 1.

This qualifies the OS AOT consumer, not a runtime WVB loader. C#/.NET still constructs the image; the guest does not retain, decode, or verify WVB, and this milestone does not add capabilities, general memory, traps, clean shutdown, Hyper-V evidence, Windvale-written native execution, native self-hosting, or .NET retirement. Decision 0063 remains the Windows/Debian qualification of the shared backend itself.

## Versioned native execution context and console-service qualification

Decision 0065 advances the experimental shared target to `x86-64-wvb-baseline-v6` and ABI 6. One exact 32-byte versioned execution context replaces positional instruction/depth arguments and carries an optional 16-byte service table. The first closed service entry lowers only immutable static UTF-8 through authorized `console.write_line`; exact Windows/System V thunks translate identical generated `R8`/`R9D` service calls to their external ABIs while preserving the shared counters and context register. Missing authorization or implementation and callback failure remain recoverable Windvale traps. The independent fragment decoder rejects corrupt context/service instruction shapes, service metadata, relocations, and UTF-8 before WVO or W^X publication.

Exact candidate commit `2fcf5318acb51bbb76279dca56bdb187c4beeb1b`, tree `cf9bb57f9bb86a8f89cbdadf03a3b4c9b7d9e924`, was archived as `windvale-native-context-2fcf531.tar.gz`, 2,829,717 bytes with SHA-256 `3a76147ef24f3932389f83f9e6a36b04a6ab7dbc4cebf90c1fcd54b716447587`. The digest matched after transfer to the isolated E-Worker Debian QA host. Its focused Linux service case passed through the real System V `mmap`/`mprotect` and callback path in 177 milliseconds with a zero-warning build.

Windows Qualification completed in 460.1 seconds with a 227.471-second suite. Debian Qualification completed in 474.8 seconds with a 238.150-second suite. Both hosts used .NET SDK `10.0.302`, passed zero-warning Release builds, all 50 tests, exact Stage 1 to Stage 2 compiler reproduction, and the complete native CLI verifier. The 15,563-byte Windows report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f5`; its 10,898-byte timing report has SHA-256 `cf54c651dbb38ed332c5e0cfe9bd1dadc2341896b72a9a50d6cfe1f161c4a5ba`. The 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`; its 10,533-byte timing report has SHA-256 `2bc947870ecbefeaf13e2fd2c0ea30039ca86affd3acff20fa16803fb256effc`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The 2,304,054-byte Debian evidence bundle has SHA-256 `84a26fb6f2eb26da512bc1d193ee02c3a28a119bf97e013c68b5fe1e412208bd`. After retrieval and comparison, the resolved exact QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

The OS bridge moves to version 2 and constructs the same ABI-6 context on the kernel-owned stack with exact instruction/depth budgets 203/2 and no service table. All 15 OS tests pass. The unchanged 502-byte portable WVB produces a 2,360-byte WVO with SHA-256 `712350a395120c42f604966dffe04c397012af3696666f51c1a069cd9db0be61`; the 305-byte bridge has SHA-256 `2e22ee17e52ee8cc2c8fa6547424f0234bd770555b0a75c23d63003b6257331e`. A pinned QEMU `11.0.0`/Q35/TCG boot produces the exact 10,240-byte EFI image with SHA-256 `61cd90eac963ed96d1fdd86d447cb7f2cdeffa50a3de2f5306239de27c1be6b0`, emits the complete version-8 marker including `native-context=pass`, and returns guest-controlled host exit code 1.

This qualifies the first service-table entry and versioned execution context, not a complete native runtime. WVO 1.0 still lacks standalone service metadata; dynamic text/bytes, allocation, file/process services, hosted PE/ELF containers, Windvale-written native selection/publication, and .NET retirement remain open.

## Borrowed-byte and unsigned native-value qualification

Decision 0066 advances the experimental shared target to `x86-64-wvb-baseline-v7` and ABI 7. Every native local and temporary owns one zero-initialized 16-byte value cell. Scalars use the low dword; an immutable borrowed-byte value uses an exact pointer/length/reserved descriptor. Static byte data, length, checked slicing, all four fixed-width little-endian reads, `u8`/`u32` comparisons, widening, and checked `u32` arithmetic lower through the same native IR and selector into the W^X and WVO/AOT sinks. Borrowed bytes may pass through as many as four internal parameter positions by descriptor pointer and immediate callee copy. Byte range failure returns packed status 6 / `WVR3008` without a host signal.

The focused differential program returns `42` through the reference interpreter, actual W^X native execution, and linked WVO/AOT while exercising every admitted byte read, a slice, length, a borrowed-byte internal parameter, scalar result, unsigned comparison, widening, and checked arithmetic. The independent decoder rejects corrupt data targets, descriptor lengths, descriptor slot types, byte argument forms, and byte-bounds targets before publication. Separate programs prove exact `WVR3008` bounds and `WVR3007` unsigned-overflow agreement.

Exact candidate commit `8d375bf3fae099e904f4536626b670d640b5e375`, tree `b9d9738ce04c8a011087c5591e69606f7ed03332`, was published to both configured remotes. Its 2,841,701-byte archive has SHA-256 `4abf83ea5f6a19f97112defc81afa435a09ea0dbd0851413d49d5b940c3c74af`; Debian computed the same digest before extraction into the isolated E-Worker QA directory. The Linux-specific native-byte case passed the actual System V `mmap`/`mprotect` path in 201 milliseconds.

Windows Qualification completed in 428.9 seconds with a 208.902-second suite. Debian Qualification completed in 428.5 seconds with a 217.291-second suite. Both hosts used .NET SDK `10.0.302`, passed zero-warning Release builds, all 51 tests, exact Stage 1 to Stage 2 compiler reproduction, and the complete native CLI verifier. The 15,563-byte Windows report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f5`; its 11,065-byte timing report has SHA-256 `71ae04a67e8cf7ff8a2f0b79071fc704ac6b2f4d9fe680383dbe1f91d7ee17f5`. The 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`; its 10,698-byte timing report has SHA-256 `5418a3fce6854c594ba5c49d77213685993b002e5e4558cf139b3435b41330a5`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. The 2,298,060-byte Debian evidence bundle has SHA-256 `e6774f3f2495e3cacc17963adc398a8a3ded7bc80d6bcae35d5cc002db513789`. After retrieval and comparison, the exact QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

Portable kernel probe version 3 adds immutable byte header `[0, 5, 0, 0, 0, 255]`, a borrowed-byte internal function, slicing, and checked `u8`/`u32` reads while retaining result 29. Its 929-byte WVB has SHA-256 `0653613d868abbba99b5e31230fb2a1f92581c4989318577cb77a6d6e60f8339`; the 7,882-byte ABI-7 WVO has SHA-256 `24f5359fa5d335eb273e8680c671924dda43d4ee4c6e00c95f1bbebc742dfa99`; and the 305-byte exact-budget bridge has SHA-256 `ef8993c59eb816c7983c5b8033922231baf1d897f846a8d5e54d8232677ef75a`. All 15 deterministic OS tests pass on both hosts. Pinned QEMU `11.0.0`/Q35/TCG boots the deterministic 15,360-byte EFI image with SHA-256 `ac92cd4759961c7a046ede49af8dce7626016fbcf8bb46e7d90027f5974bffa4`, emits the complete version-9 marker, and returns guest-controlled host exit code 1.

This qualifies a borrowed immutable binary view, not a native Windvale tool or complete runtime. Native file/argument input, owned or returned bytes, dynamic text, records, allocation, hosted PE/ELF containers, Windvale-written native selection/publication, and .NET retirement remain open.

## Borrowed hosted-input and first native WVB inspector qualification

Decision 0067 advances the experimental shared target to `x86-64-wvb-baseline-v8` and ABI 8. Borrowed text and bytes share a pointer/length/reserved descriptor in zero-initialized 16-byte value cells. Service-table version 2 adds explicitly authorized bounded argument count, argument text, and file snapshot input beside console output. One execution owner validates, caches, and releases every host-returned buffer; the reference and native adapters share file-snapshot limits and exact `WVR302x` failures. Exact Windows/System V thunks preserve one platform-neutral generated-code convention, and the independent decoder validates all descriptor and service-load shapes before WVO or W^X publication.

The checked-in `Examples/Foundation/Wvb-Header-Inspector.wv` accepts one filename, prints the host-returned text, reads a real compiler-produced WVB twice through one adapter snapshot, validates `WVB1` and version `1.6`, prints `wvb-header=pass`, and returns zero under both the reference interpreter and native execution. The focused tests also prove deterministic native code, canonical service metadata, corruption rejection, and agreement for out-of-range arguments (`WVR3020`) and missing files (`WVR3022`).

Exact candidate commit `d970c2789058d37c7059264fcbd76e164a01a939`, tree `1723f1ee8342e36e3237f14b21927e334c9bddae`, was published to both configured remotes. Its 2,851,048-byte archive has SHA-256 `1188c5fdf12c71426d4a5735eb533326b739432f75618c2e8aba134ba1862374`; Debian computed the same digest before extraction into the isolated E-Worker QA directory. The focused Linux native hosted-input case passed the actual System V `mmap`/`mprotect` and callback path in 178 milliseconds.

Windows Qualification completed in 408.1 seconds with a 201.569-second suite. Debian Qualification completed in 421.9 seconds with a 213.068-second suite. Both hosts used .NET SDK `10.0.302` and runtime `10.0.10`, passed zero-warning Release builds, all 52 tests, exact Stage 1 to Stage 2 compiler reproduction, and the complete native CLI verifier. The 15,563-byte Windows report has SHA-256 `6780bd68cfcf7dacea10d34f5a4b9d7eeb6cdc2c2c4a70cf055c6830429330f5`; its 11,244-byte timing report has SHA-256 `350a72a7c699a9bde83e330c674fd108e1ae6742c4cc1538082da2951945a4bb`. The 15,473-byte Debian report has SHA-256 `1fec4c222425f2737a7f41c415b1841f88aab0a8e7458b40d4e7d170e1d9c35d`; its 10,869-byte timing report has SHA-256 `66358cacf8220d5b8231c2800961d8ad1546c0267ec0a3b4b8e1e642865ef029`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,612 bytes, matched Windows byte for byte. Their canonical name/size/SHA-256 manifest has SHA-256 `723354f893e2bb1c4bfaa6f7a04dc23995a51a2d5444d9a55759710d76b47f39`. The 2,293,338-byte Debian evidence bundle has SHA-256 `d02a98ce1ef1e48998ef4e06d33a58a368233382883068722d766ae9c2e8acca`. After retrieval and comparison, the resolved exact QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

Firmware probe version 10 rebuilds the service-free portable consumer through ABI 8 while retaining the zero service-table pointer. The portable WVB remains 929 bytes with SHA-256 `0653613d868abbba99b5e31230fb2a1f92581c4989318577cb77a6d6e60f8339`; its 7,882-byte WVO remains SHA-256 `24f5359fa5d335eb273e8680c671924dda43d4ee4c6e00c95f1bbebc742dfa99`. All 15 deterministic OS tests pass on Windows and Debian. Pinned QEMU `11.0.0`/Q35/TCG boots the 15,872-byte EFI image with SHA-256 `9228995f3b2522e15bd87ca63dc2637cc290f93b37f3e32b24cd8e3906671b75`, emits the complete version-10 marker, and returns guest-controlled host exit code 1.

This qualifies the first native Windvale-written file-backed inspector and hosted-input boundary, not full `wvdump`, native self-hosting, or a complete operating system. Dynamic text/bytes, native nominal aggregates, structured diagnostic output, allocation, in-guest WVB loading/verification, hosted PE/ELF containers, and .NET retirement remain open.

## Bounded native nominal values and structural wvdump qualification

Decision 0068 advances the implemented target to `x86-64-wvb-baseline-v9` and ABI 9. Enums use canonical signed dword values with nominal identity. Immutable records use 32-bit offsets into one checked 1 MiB execution arena and retain one complete 16-byte typed cell per field. Context version 2 carries arena pointer/length/used fields; exhaustion is packed status 7 / `WVR3017`. Service-table version 3 adds capability-free strict `Textˉutf8ˉisˉvalid` support through exact platform thunks.

The existing structural portion of `Examples/Foundation/Wv-Dump-Core.wv`, through `Inspectˉwvbˉpayloads`, now runs as the same verified native fragment under W^X JIT and linked WVO/AOT. It validates the full canonical seven-section fixture and agrees exactly with the reference interpreter. `Scanˉshape` now encodes its two boolean policies in one bounded `u32`, preserving behavior within the existing four-register call ABI. This is the real checked-in parser, not a parallel toy implementation.

The focused enum/record test covers construction, fields, parameters, returns, equality, inequality, deterministic WVO, corruption rejection, zero-default policy, and arena exhaustion. A separate valid/invalid UTF-8 case proves the pure runtime service with no capability authorization. The ABI-9 portable WVO is 7,946 bytes with SHA-256 `94d10d60f5d37cdab3d0f0c3678ee1e86b312564c06d633baf4736939595fed2`; the version-4 bridge object is 315 bytes with SHA-256 `949c5fdd641722c541e2ed6583a5c28bd681b77d91f28ea5cda2999026d75a23`; the firmware-probe-11 image remains 15,872 bytes with SHA-256 `100070e26666bfc97d0fff8da42d996249d072b5771014838a376abfc0a13d6a`.

Exact candidate commit `7edc243f3c6cae8d25234165e3975caaa03ec8ee`, tree `601d25d6107526d17eeaed546149fb10076cf0d3`, was published to both configured remotes. Its 2,861,137-byte archive has SHA-256 `940d41bbf46537da7183d4a387823690fac8c9820379afe041b4193c7ea9c1b2`; Debian verified the same digest before extraction in the isolated E-Worker QA environment.

Windows and Debian GNU/Linux 12 x64 passed zero-warning Release builds, all 54 tests, exact compiler reproduction, and complete CLI reconstruction. The Windows suite took 204.633 seconds; Debian took 213.327 seconds and 430.092 seconds for the complete command. The 15,563-byte Windows report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 11,569-byte timing report has SHA-256 `778bda75d3d224d297dd5585449aed99612aedd20813d87115b6d5d51f2c4717`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,186-byte timing report has SHA-256 `9b077e8fcb943fdd5bee7cd6b43c72e09a5579817e41078fc3720ed208144926`. Their normalized contracts match exactly.

All 61 portable artifacts, totaling 7,752,647 bytes, match byte for byte; their canonical manifest has SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The 2,299,583-byte Debian evidence bundle has SHA-256 `aad7c14cad307f53c2182ef8bae948dc67e61da677d36d22a930b79e010445b6`. Both hosts pass all 15 OS tests. Pinned QEMU 11.0/Q35/TCG boots the exact probe-11 image, emits the complete `windvale-os-boot 11` success transcript, and returns guest-controlled host exit code 1. After retrieval and comparison, the resolved exact QA directory, source archive, and remote evidence bundle were removed and confirmed absent. This supersedes Decision 0067 as the latest qualified native/OS evidence without claiming dynamic text, full native `wvdump` output, a native runtime, or .NET retirement.

At the ABI-9 qualification point, the remaining full-`wvdump` native gap was bounded dynamic text construction and formatting, descriptor returns, void-return calls, and diagnostic output. The C# Stage 0 selector, decoder, platform adapter, oracle, recovery implementation, and OS image builder remained required.

## Dynamic native text and complete wvdump qualification

Decision 0069 advances the implemented target to `x86-64-wvb-baseline-v10` and ABI 10 while retaining execution-context version 2. Service-table version 4 is 96 bytes and adds authorized diagnostics plus pure enum naming, invariant integer formatting, bounded concatenation, and deterministic quoting. One execution-owned 16 MiB text arena backs dynamic results; individual text retains the WVB 1 MiB limit (`WVR3012`) and aggregate exhaustion becomes `WVR3018`. Strict `Textˉfromˉutf8` uses the proven validation service and packed status 8 / `WVR3014` for malformed input.

Descriptor-returning callers pass one verified result-cell address in `RAX`; callees save it in a dedicated final frame cell, copy the complete descriptor on success, and preserve packed trap propagation. Void calls use the same status convention without a result. The independent fragment decoder admits only the exact prologue, call, return, service, provenance, and failure shapes and cross-checks caller/callee return kinds.

The complete 1,441-line checked-in `Examples/Foundation/Wv-Dump-Core.wv` now compiles without slicing, inspects a real compiler-produced WVB, and emits byte-identical full reports and diagnostics under the reference interpreter, Windows/Linux W^X JIT, and linked WVO/AOT. Focused negative cases cover invalid UTF-8, aggregate text exhaustion, and corrupt hidden-result metadata. The service-free OS consumer advances to kernel probe version 5 and firmware probe 12. Both hosts pass all 15 OS tests; the 8,010-byte portable WVO has SHA-256 `f3d0d2aec5b7fb81d02e4188fb6ba48b6a21dc91c89bdf7f00daaf7b0a981038`, and the 15,872-byte EFI image has SHA-256 `3010bc72b9c26386f062f78481c900cac841321b040b41447a0bbb65a9e392fe`.

Exact commit `79799332deb2d144e9b7557700829f14322a3062`, tree `d86080a14f654c26e839fe50574ae5c441670572`, was published to both configured remotes. Its 2,872,181-byte archive has SHA-256 `c9fe842f0f137b523003acd8360196b138cfaae36c73b2ad7d08f0d837c9a57e`; the digest and size matched after transfer to the isolated E-Worker Debian QA host.

Windows and Debian GNU/Linux 12 x64 with .NET SDK `10.0.302` pass zero-warning Release builds, all 56 tests, exact compiler reproduction, and the complete native CLI verifier. The Windows suite takes 212.675 seconds; Debian takes 233.729 seconds. The 15,563-byte Windows report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`, and its 11,916-byte timing report has SHA-256 `9fe062225e6a0aa61ce78b5aca09731ab890539a5032a86c571846d8c7005f77`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`, and its 11,522-byte timing report has SHA-256 `0e28cbaa4868d83a8230ee91afd9bdbe99c9303b416f32263e2d3790c473be5d`. Their normalized contracts match exactly.

All 61 portable artifacts, totaling 7,752,647 bytes, match byte for byte; their retained canonical name/size/SHA-256 manifest has SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The 2,303,863-byte Debian evidence bundle has SHA-256 `a6b0962dbaad4b0991f99e9beb523758e192f200334f830cead48dedea1cca38`. Both hosts pass all 15 OS tests. Pinned QEMU 11.0/Q35/TCG on Windows boots the exact probe-12 image, emits the complete `windvale-os-boot 12` success transcript, and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU, so no Debian emulator run is claimed. After retrieval and comparison, the resolved exact QA directory, source archive, and evidence bundle were removed and confirmed absent.

This supersedes Decision 0068 as the latest qualified native/OS evidence without claiming a Windvale-written native runtime, native compiler execution, a standalone hosted PE/ELF container, in-guest WVB loading, a functioning operating system, or .NET retirement. C#/.NET remains the Stage 0 selector, independent decoder, runtime-service provider, oracle, recovery implementation, and OS image builder.

## First runtime-native UTF-8 service qualification

Decision 0070 retains qualified ABI 10 and service-table version 4 while replacing the `Textˉutf8ˉisˉvalid` managed delegate and Windows/System V adapters with one identical runtime-owned x86-64 leaf. Its internal inputs remain the proven `R8` address, bounded `R9D` length, and verified `RCX` Boolean cell; it writes normalized zero/one, returns status zero, and leaves the resource/context registers untouched.

The leaf is exactly 800 bytes with SHA-256 `4c3d2e370d62c8d2f54a3c453f39b94cf46ddabd6db3c2f3d6b65f0713b68aaf`. Windows and Linux W^X execution agree with the managed strict-decoder oracle over 23 ranges covering empty/ASCII input, every canonical multibyte lead family and boundary, overlong forms, surrogate encodings, truncation, bad continuations, stray continuations, and the `U+10FFFF` ceiling. Rebuilding the leaf is byte-deterministic, and a one-byte corruption fails identity validation before W^X publication. The complete native dynamic-text case continues to pass through strict `Textˉfromˉutf8` conversion.

Exact commit `53cee69bdd24e96bb42521d53e9d55b0c59c5876`, tree `2e6aa434d5956be489fa24f3df8da0e9fc25ec9d`, was archived as 2,877,521 bytes with SHA-256 `de6810995b67e7f8eb678f0ca228ca1c774c1b1ce901c104f8f42646bda55133`. The exact archive was independently extracted on Windows and in `/tmp/windvale-native-utf8-53cee69-20260801` on the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`. Both hosts build Release with zero warnings, pass all 56 tests and exact compiler reproduction, and complete the CLI verifier. The Windows suite takes 214.004 seconds; Debian takes 229.082 seconds. GitHub [Verify run 30692761371](https://github.com/eworker-inc/Windvale/actions/runs/30692761371) also passes for the exact candidate.

The 15,563-byte Windows report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 11,915-byte timing report has SHA-256 `81a90a749bf1e651ed0415c449e9c23f1e35c1a575e8090046140eeb030f5058`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,522-byte timing report has SHA-256 `8232fbccbb1c9765547d156967257126e05df9e161095d8ef897ac13f0d7edb0`. Their normalized contracts match exactly.

All 61 portable artifacts, totaling 7,752,647 bytes, match byte for byte; their retained canonical name/size/SHA-256 manifest has SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The retrieved 2,297,339-byte Debian evidence bundle has SHA-256 `3f24b92447ea6f124a2fa1986beabaa52a623de7127a0595b647aa2629c2738c`. Both hosts pass all 15 OS tests. Pinned QEMU 11.0/Q35/TCG on Windows boots the unchanged exact 15,872-byte probe-12 image with SHA-256 `3010bc72b9c26386f062f78481c900cac841321b040b41447a0bbb65a9e392fe`, emits the complete success transcript, and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU, so no Debian emulator run is claimed. After evidence retrieval, the resolved exact QA directory, transferred archive, evidence bundle, and transient log were removed and confirmed absent.

The C# executor still builds and publishes the service bytes, owns buffers and executable memory, and supplies every other runtime service; no Windvale-written native runtime or .NET-retirement claim is made. This supersedes Decision 0069 only as the latest qualified native-runtime evidence; ABI 10, WVB/WVO, and the OS image remain unchanged.

## Native text-arena and core-text-service qualification

Decision 0071 advances the qualified target to `x86-64-wvb-baseline-v11`, ABI 11, and execution-context version 3. The context grows from 48 to 72 bytes and gives native and managed services one explicit 16 MiB text-arena base, capacity, allocation cursor, and exact failure-detail channel. Service-table version 4, WVB 1.6, WVO 1.0, and all service numbers remain unchanged.

`Textˉconcat`, `I32ˉformat`, and `U32ˉformat` no longer use managed delegates or Windows/System V adapter thunks. Their exact platform-neutral x86-64 leaves are respectively 249 bytes / `75c5588117e1f5f58a593a23aae6156a3a68a6302df5f50153b977bccbaaa3a0`, 225 bytes / `c33758106e8d7cd31bbed8ef1e789a8e355c52736c119c75493154a4184fa41e`, and 191 bytes / `b98f2d55e30bb7369e233f94e4ade5f3e8917a7730114446f1ebc81f353e1e43`. The dynamic-text differential case covers signed minimum, unsigned maximum, zero, native/managed shared allocation, the 1 MiB value limit, aggregate exhaustion, deterministic reconstruction, and corrupt service identity. Enum naming and quoting remain managed services; hosted adapters are unchanged.

Exact commit `88889513e6c6d9ef673f7dcdca761628a430e31f`, tree `875c8626e3e605c5080ce86c1adbb2ec00e960cc`, was archived as 2,885,974 bytes with SHA-256 `98b775a5cae3b0f48a83b9465931707226a96b8dc67bb5302008e58a41318ef3`. The exact archive matched after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`. Windows and Debian build Release with zero warnings, pass all 56 tests and exact compiler reproduction, and complete the CLI verifier. The Windows suite takes 233.375 seconds; Debian takes 248.582 seconds. GitHub [Verify run 30694649557](https://github.com/eworker-inc/Windvale/actions/runs/30694649557) also passes for the exact candidate.

The 15,563-byte Windows report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 11,918-byte timing report has SHA-256 `c08679ef53e9f5b722989feb276ec28e4882f97dadffb9c8614b782477a39ec8`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,524-byte timing report has SHA-256 `b613d16c834cc6a35de69b8b7eb620dd057ff345d822514f53aef13b6e28fd19`. Their normalized contracts match exactly.

All 61 portable artifacts, totaling 7,752,647 bytes, match byte for byte; their retained canonical manifest has SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The retrieved 2,297,411-byte Debian evidence bundle has SHA-256 `7e723eeb634e145e7b7a8dcd609ec7f0e7a78e04fe402bf8ea0a54e012997b6a`. Both hosts pass all 15 OS tests. The portable kernel WVB remains 929 bytes with SHA-256 `0653613d868abbba99b5e31230fb2a1f92581c4989318577cb77a6d6e60f8339`, and its service-free WVO remains 8,010 bytes with SHA-256 `f3d0d2aec5b7fb81d02e4188fb6ba48b6a21dc91c89bdf7f00daaf7b0a981038`. The qualified bridge object is 330 bytes with SHA-256 `8b28ed85af29baa65810e0ed0ce8e2893e9696cebd666ccf72a1a53f68cde2b9`; qualified probe 13 is 15,872 bytes with SHA-256 `ceffc3e33bf007e47b109f3b6a71db2fdceac3c0e908d1471f056909ee42532d`. Pinned QEMU 11.0/Q35/TCG emits the complete success marker and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU. After retrieval and comparison, the resolved exact QA directory, transferred source archive, remote evidence bundle, and temporary QA inputs were removed and confirmed absent.

This supersedes Decision 0070 as the latest qualified native-runtime evidence without claiming native enum metadata, native deterministic quoting, native hosted adapters, a Windvale-written native runtime, or .NET retirement.

## Final pure runtime native services qualification

Decision 0072 retains ABI 11, context version 3, service-table version 4, WVB 1.6, WVO 1.0, kernel bridge 6, and firmware probe 13 while moving the final two deterministic pure services off managed callback and platform-thunk paths. `Enumˉname` uses one exact 323-byte platform-neutral x86-64 leaf with SHA-256 `fb05590c5b6e1791380ba288c4112387e791a18722428c90276796bd409d130a`. It reads an adjacent bounded runtime-private `WVEN` version-1 block reconstructed from the fragment's independently verified canonical types. The complete leaf-plus-metadata bundle is independently parsed, reconstructed, and byte-compared before W^X publication.

`Textˉquote` uses one exact 1,165-byte platform-neutral x86-64 leaf with SHA-256 `4f334af9b6349437d36fd703edb6b5882416f033fae47906a40a4bafdc083bb7`. Its first pass strictly validates UTF-8, measures the existing UTF-16-code-unit escape representation, and proves the 1 MiB value and 16 MiB arena limits before allocation; its second pass writes the exact ASCII result. Differential coverage includes every short escape, remaining controls, printable ASCII boundaries, BMP Unicode, both supplementary boundaries, multiple enum types, deterministic bundle reconstruction, corrupt metadata, value-limit failure, and separate enum/quote arena exhaustion.

Exact commit `f97d22154d1c7562f72552a617dd912d18943880`, tree `f79cf95eb5d53b005bbd5f1b67733ba7648ce98d`, was published to both configured remotes and archived as 2,893,521 bytes with SHA-256 `992235ac005c9f953a3d43a2a101f44693729c9ccd606ac5540c5c515bff66d8`. The digest and size matched after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 56 tests, exact compiler reproduction, and the complete native CLI verifier. Their suite times are 220.741 and 233.171 seconds. The native dynamic-text case takes 0.184 seconds on Windows and 0.208 seconds on Debian. The 15,563-byte Windows conformance report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 11,917-byte timing report has SHA-256 `f3bd9d9f47b8ae75cd1f1eaa7e7ed64d3040561f616254653d00c44ae4d3445b`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,522-byte timing report has SHA-256 `64fc010062ca258a394ac584e94e3b68e675233a1cc3431823521ba5d3fa7c5a`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,647 bytes, match byte for byte and retain canonical manifest SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The 2,299,000-byte Debian evidence bundle has SHA-256 `489702b53e1ffb8f2630d8c33c9d1fe43897f7ed3e4467be29f3ddf725eafc1e`. Both hosts pass all 15 OS tests. Pinned QEMU 11.0/Q35/TCG boots the unchanged exact 15,872-byte probe-13 image with SHA-256 `ceffc3e33bf007e47b109f3b6a71db2fdceac3c0e908d1471f056909ee42532d`, emits the complete success marker, and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU.

GitHub [Verify run 30696841988](https://github.com/eworker-inc/Windvale/actions/runs/30696841988) passes for the exact candidate. After evidence retrieval and comparison, the resolved QA directory, transferred source archive, remote evidence bundle, and temporary QA inputs were removed and confirmed absent.

All six deterministic pure runtime services now execute without managed callbacks or platform adapters. C# still constructs and verifies the service bundles, publishes executable memory, owns the arenas and executor, supplies the five hosted/capability adapters, and remains the reference/recovery implementation. This supersedes Decision 0071 as the latest qualified native-runtime evidence without claiming a Windvale-written native runtime, standalone hosted executable/container, native compiler execution, in-guest WVB loading, or .NET retirement.

## Native argument table and process-input services qualification

Decision 0073 advances the qualified target to ABI 12 and execution-context version 4 while retaining service-table version 4, WVB 1.6, WVO 1.0, and generated service-call shapes. The context appends one execution-owned immutable argument-table pointer and count. Stage 0 packs at most 67 prevalidated strict-UTF-8 arguments, independently rereads every descriptor, range, reserved field, and byte before publication, and releases the snapshot after execution.

`Processˉargumentˉcount` now uses one exact 5-byte platform-neutral x86-64 leaf with SHA-256 `2358e7e2c72d6476cfe05134db4f0eb5e6987fcca1b10894a8588a28d3929829`. `Processˉargument` uses one exact 70-byte checked descriptor-copy leaf with SHA-256 `2253e1435f141df5b68f9f7e9e9aa0de448410c42dcf33ad76dcf131afea65d1`. Differential coverage consumes the complete 67-entry snapshot including empty, ASCII, euro, and supplementary-Unicode text, proves zero arguments, deterministic reconstruction, corruption rejection, and exact out-of-range failure.

Exact commit `328e4552829b236e5baeb1f01447010855842730`, tree `cc5dc70848655e3bc1a403d28c700bf4e3c3f71d`, was published to both configured remotes and archived as 2,900,102 bytes with SHA-256 `692774c8780eeabc30f758aec49373b23685d542252ea80475c04289ec84f3d7`. Its digest and size matched after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 56 tests, exact compiler reproduction, and the complete native CLI verifier. Their suite times are 238.075 and 252.295 seconds; their native hosted-input cases take 0.020 and 0.022 seconds. The 15,563-byte Windows conformance report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 11,916-byte timing report has SHA-256 `aff0fece57fb555ecfad153dbbb9c2745ae305f67826aa2248da10ee6eb98c9f`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,523-byte timing report has SHA-256 `a0c8e76975837d2bb9f5a1179b58976d9be71990926812258becc0d3421cce7c`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,647 bytes, match byte for byte and retain canonical manifest SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The 2,299,022-byte Debian evidence bundle has SHA-256 `c9043ab9fdc72241affda5dabd8250adf5d39ecc73b9bf48901e27dc4e056c94`. Both hosts pass all 15 OS tests. The service-free kernel WVB remains 929 bytes with SHA-256 `0653613d868abbba99b5e31230fb2a1f92581c4989318577cb77a6d6e60f8339`; its WVO remains 8,010 bytes with SHA-256 `f3d0d2aec5b7fb81d02e4188fb6ba48b6a21dc91c89bdf7f00daaf7b0a981038`. The bridge object is 340 bytes with SHA-256 `0f6d4f00e6a66c23dedc7c6224cdae3f556c5d1c0ff927e596c927a73fd9829f`; probe 14 is 15,872 bytes with SHA-256 `aadfbc5cb56f6afea94605ad31ee6af6a60b1e821403dfb8e1c2550631b6d548`. Pinned QEMU 11.0/Q35/TCG emits the complete success marker and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU.

GitHub [Verify run 30698761104](https://github.com/eworker-inc/Windvale/actions/runs/30698761104) passes its independent Windows and Linux jobs for the exact candidate. After evidence retrieval and comparison, the resolved QA directory, transferred source archive, remote evidence bundle, and temporary QA inputs were removed and confirmed absent.

Eight of eleven closed runtime-service slots now execute without managed callbacks or platform adapters. C# still constructs and verifies the service leaves and argument table, publishes executable memory, owns arenas and execution, supplies console, diagnostic, and file-input adapters, and remains the reference/recovery implementation. This supersedes Decision 0072 as the latest qualified native-runtime evidence without claiming a Windvale-written native runtime, standalone hosted executable/container, native compiler execution, in-guest WVB loading, or .NET retirement.

## Native Windows and Linux output-service qualification

Decision 0074 advances the qualified target to ABI 13 and execution-context version 5 while retaining service-table version 4, WVB 1.6, WVO 1.0, and generated service-call shapes. One checked runtime-private `WVIO` table carries separate console and diagnostic targets. Exact platform-specific output leaves write strict UTF-8 plus LF directly: Windows uses 258-byte console and diagnostic leaves with SHA-256 `10f3a500aca7f0236cdf9f6c20658591df88bc612e677264cdaa0bcef59a0a48` and `1b4068c01b2050c3055c78eb82303c71b8488e8766f7b628fab10ffb23e5ffe2`; Linux uses 213-byte leaves with SHA-256 `c5ea073a24c46dd634b1a67a7e7041d476dbce856d058aa8adc2c4e680d3d226` and `1c81018143fa9b708373eaceda62722ca40fb1e11b20808f765fe5ece33406fe`. The leaves complete partial writes, retry interrupted Linux writes, preserve native counters/context, and map OS rejection to stable `WVR3029`.

Exact commit `66b273f7e0839c7ebcd504cfdfdb9f0a4aad787e`, tree `278582a0f2f2465738b9ef98dd91df96ce52ec27`, was published to both configured remotes and archived as 2,909,424 bytes with SHA-256 `d36f20e21e78f31e02ac08d13452a7e759fd620a2492565b130080442f99c8ad`. The same size and digest were verified after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 56 tests, exact compiler reproduction, and the complete native CLI verifier. Their suite times are 232.193 and 247.986 seconds, and complete Qualification takes 481.569 and 503.169 wall-clock seconds. The native-output cases take 0.042 and 0.037 seconds. Coverage reconstructs and corrupts all four leaf identities, separates console and diagnostic OS-backed files, exercises direct JIT and linked WVO/AOT output, covers empty, ASCII, euro, and supplementary-Unicode text, and forces rejected writes under the reference and native runtimes.

The 15,563-byte Windows conformance report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 11,918-byte timing report has SHA-256 `70fc2082473a3b3c56f74c132f0ad626ae676bf23f3e39c9cf4f8a5c1927341f`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,526-byte timing report has SHA-256 `10d2dc708d7c43505a8fd52754cea677da0be1197dc6787057312e6a2720015e`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,647 bytes, match byte for byte and retain canonical manifest SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The 2,299,260-byte Debian evidence bundle has SHA-256 `20f600498fa1257d6586d0e3f33f9e5e1e057850f6c4cfe10547d01b1d9599ef`. Both hosts pass all 15 OS tests. The service-free kernel WVB remains 929 bytes with SHA-256 `0653613d868abbba99b5e31230fb2a1f92581c4989318577cb77a6d6e60f8339`; its WVO remains 8,010 bytes with SHA-256 `f3d0d2aec5b7fb81d02e4188fb6ba48b6a21dc91c89bdf7f00daaf7b0a981038`. The 345-byte bridge object has SHA-256 `0a0393457200dbf5ecfbb667c6c283510a6eb13a3e7e77537a0b6d8e0f503d68`; probe 15 is 15,872 bytes with SHA-256 `d716b77a91646da6b423bacb1faa6d70f5a097241c610fe49291b068f33d5f29`. Pinned QEMU 11.0/Q35/TCG emits the complete version-15 marker and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU.

GitHub [Verify run 30701244938](https://github.com/eworker-inc/Windvale/actions/runs/30701244938) passes its independent Windows and Linux jobs for the exact candidate. After evidence retrieval and comparison, the resolved QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

Ten of eleven closed runtime-service slots now execute without managed callbacks or platform adapters. `file.read_bytes` is the sole remaining callback. C# still constructs and verifies service leaves, argument/output tables, publishes executable memory, owns arenas and execution, and remains the reference/recovery implementation. This supersedes Decision 0073 as the latest qualified native-runtime and OS evidence without claiming a Windvale-written native runtime, standalone hosted executable/container, native compiler execution, in-guest WVB loading, or .NET retirement.

## Native Windows and Linux file-input qualification

Decision 0076 advances the qualified target to ABI 14 and execution-context version 6 while retaining service-table version 4, WVB 1.6, WVO 1.0, and generated service-call shapes. One checked runtime-private 136-byte `WVFI` table carries a 64-record snapshot directory, bounded name/data arenas, path scratch, platform identity, and the narrow Windows function-pointer boundary. Exact 1,218-byte Windows and 996-byte Linux leaves read host files directly, preserve ordinal first-success immutable snapshots, retry interrupted reads, and map native failures to stable `WVR3021` through `WVR3025` and `WVR3028`. Their SHA-256 values are `3d2fffc028083cdc4cfd39e553dea603e9a1ae661bb5df3f14ca438c4d3e3cf8` and `55ae4524c463f064aee0964d7f9b64438701fb4375a97c53d11f2f17902c12cb`.

The initial exact Debian run rejected the candidate after exposing two Linux-leaf implementation defects: a cleared table register caused the complete native `wvdump` path to fault, and a read-request stack slot overlapped the saved scratch pointer and misclassified the 4 MiB + 1 boundary. Both were corrected before qualification. The final Debian hosted-input case passes the complete `wvdump`, real JIT and WVO/AOT file execution, zero calls to the supplied Stage 0 reader, first-success cache reuse, invalid/missing/oversized failures, and the 65th-name limit.

Exact commit `ef0861980f7309ca7cac709f6930b5e11a4c8208`, tree `bcafa888e4f9bf470a4c42e3aa0173acd47bd13f`, was published to both configured remotes and archived as 2,931,444 bytes with SHA-256 `d9fdfead5c1d42586d4c9a96102389491553f494059df120df2318d7867fafb7`. The same size and digest were verified after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 57 tests, exact compiler reproduction, and the complete native CLI verifier. Their suite times are 238.115 and 257.556 seconds, and complete Qualification takes 499.037 and approximately 529 seconds wall-clock. The native hosted-input cases take 0.130 and 0.101 seconds. The 15,563-byte Windows conformance report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 12,074-byte timing report has SHA-256 `deb25a16aaaadeabaf15395167e58fc435f260c4f792416f80089072deb2a232`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,675-byte timing report has SHA-256 `0d0de32e961bc112f2f1e03f64b4f87de2cb50a3ab97953e7963d7646c7b6aac`. Their normalized contracts match exactly.

All 61 directly retrieved portable artifacts, totaling 7,752,647 bytes, match byte for byte and retain canonical manifest SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The 2,299,009-byte Debian evidence bundle has SHA-256 `2ac389f97d5f94b4ae60a5dd0ee8fee3cf9a62a0851b5fbd20e35fcf7e829a89`. Both hosts pass all 15 OS tests. The service-free kernel WVB remains 929 bytes with SHA-256 `0653613d868abbba99b5e31230fb2a1f92581c4989318577cb77a6d6e60f8339`; its WVO remains 8,010 bytes with SHA-256 `f3d0d2aec5b7fb81d02e4188fb6ba48b6a21dc91c89bdf7f00daaf7b0a981038`. The 350-byte bridge object has SHA-256 `3cbf50a4828a1a69ca7441a667cb95e569055468c345ed26b8a580fda3facfc5`; probe 16 is 15,872 bytes with SHA-256 `206a036f8cbe3198544b6878bf52c80ef8d489c14d5437c6c7004ff1d6599504`. Pinned QEMU 11.0/Q35/TCG emits the complete version-16 success marker and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU.

GitHub [Verify run 30704485295](https://github.com/eworker-inc/Windvale/actions/runs/30704485295) passes its independent Windows and Linux jobs for the exact candidate. After evidence retrieval and comparison, every resolved ABI-14 QA directory, transferred source archive, transient trace, and remote evidence bundle was removed and confirmed absent; the temporary diagnostic package was also removed.

All eleven closed runtime-service slots now execute without managed callbacks or platform adapters. C# still constructs and verifies service leaves and runtime-private tables, publishes executable memory, owns arenas and execution, maps traps, packages the OS image, and remains the independent reference/recovery implementation. This supersedes Decision 0074 as the latest qualified native-runtime and OS evidence without claiming a Windvale-written native runtime, standalone hosted executable/container, native compiler execution, in-guest WVB loading, or .NET retirement.

## First Windvale-owned native-stencil qualification

Decision 0077 qualifies the first active runtime construction artifact owned by Windvale assembly without changing ABI 14, execution-context version 6, service-table version 4, WVB 1.6, WVO 1.0, kernel bridge 9, firmware probe 16, or the final service bytes. `Compiler/Native/Stencils/Process-Argument-Count.wva` describes one exact five-byte shell plus a versioned typed one-byte execution-context-offset patch. The Windvale-written assembler produces the retained canonical 166-byte WVO with SHA-256 `e2057943b9c79e10a432ea20a77da5ed0a261e3effdd36511cbb34e77e55c10b`. The live native executor loads its strict bounded representation and instantiates the unchanged five-byte leaf with SHA-256 `2358e7e2c72d6476cfe05134db4f0eb5e6987fcca1b10894a8588a28d3929829`.

Exact integrated commit `da593126980d19aecacc354591eb888edc5da2c5`, tree `1d1d7d3866bb0e41ecf9426a2a875a98766dc793`, includes the concurrently published Windvale project-manifest parser and was archived as 2,944,228 bytes with SHA-256 `b547c5adb8377052c0df1db8262fd937862f7678c3e2eb198c8aa987e7010812`. The exact archive retained its size and digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 59 integrated Seed tests, exact compiler reproduction, and the complete native CLI verifier. Their suite times are 253.312 and 266.554 seconds; complete Qualification takes 499.750 and 512.050 seconds wall-clock. The native-stencil cases take 0.912 and 0.881 seconds, and the live hosted-input cases take 0.174 and 0.104 seconds. The 15,563-byte Windows report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`; its 12,398-byte timing report has SHA-256 `f7fc8fa08fe090d5bea3ce4bc03bf084ad975af1d403f5c78fa2e8fd9cddc56b`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 11,990-byte timing report has SHA-256 `04c612c9fe6fbc83994428da34d33afab2024cb5f995cf7f1e8d1eccca7fd1d3`. Their normalized contracts match exactly.

All 61 established portable artifacts, totaling 7,752,647 bytes, match byte for byte and retain canonical manifest SHA-256 `11ac1d4a57fce3648004d7a6002e6124d6e2fbeefc108b31bfe305523b2de0de`. The retrieved 2,585,001-byte Debian evidence bundle has SHA-256 `4c41d6b711e16b7941c2fe77e9fc278b1ca782e319fa3273442d45895c9d863d`. Both hosts pass all 15 OS tests. Pinned QEMU 11.0/Q35/TCG boots the unchanged exact 15,872-byte probe-16 image with SHA-256 `206a036f8cbe3198544b6878bf52c80ef8d489c14d5437c6c7004ff1d6599504`, emits the complete success transcript, and returns guest-controlled host exit code 1. The Debian QA host does not provide QEMU.

One earlier detached Windows launch was discarded after the launcher's default console decoding replaced a macron in otherwise successful CLI output. The exact UTF-8-configured rerun passed the complete unmodified verifier with zero stderr. The issue was confined to the detached test launcher and did not change Windvale source, artifacts, or semantics.

GitHub [Verify run 30706925533](https://github.com/eworker-inc/Windvale/actions/runs/30706925533) passes its independent classification, Windows, and Linux jobs for the exact implementation commit. After evidence retrieval, both resolved exact QA directories, both transferred source archives, and both remote evidence bundles were removed and confirmed absent.

This supersedes Decision 0076 as the latest qualified native-runtime and OS evidence. It qualifies Windvale ownership of one active runtime stencil's source and patch description, not a general baseline JIT, Windvale-owned WVO loading or patch application, native publication, arenas, standalone host tools, in-guest WVB loading, or .NET retirement.

## Multi-patch native-stencil and native project-shell qualification

Decision 0078 qualifies the measured second Windvale-owned native construction artifact without changing ABI 14, execution-context version 6, service-table version 4, WVB 1.6, WVO 1.0, kernel bridge 9, firmware probe 16, or either final argument-service leaf. `Compiler/Native/Stencils/Process-Argument.wva` describes the complete zero-hole 70-byte `process.argument` shell plus eight ordered one-byte patches with six named ABI meanings. The Windvale-written assembler produces the canonical 321-byte WVO with SHA-256 `307e61dcb2a156eb0d4b77f7d93676d7b1ac24f9bb6fe1f31217837213352bad`; the live runtime instantiates the unchanged 70-byte leaf with SHA-256 `2253e1435f141df5b68f9f7e9e9aa0de448410c42dcf33ad76dcf131afea65d1`.

Exact commit `50294d9d5cc24edc26a3e56994cb3aa28e16352c`, tree `4dca2de1f65b929f18d3e12cffe3f4b47f6536ff`, includes the concurrently integrated native Windvale Project 1 shell. Its 2,956,277-byte archive has SHA-256 `d3015804c219993e3c75d53f6ea19842593612a1c37a7093f331fe0a08a14398` and retained that identity on the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 60 Seed tests, exact compiler reproduction, and the complete native CLI/reproduction gate. Their suite times are 224.398 and 242.491 seconds; complete Qualification takes 459 and 477 seconds wall-clock. The combined stencil cases take 1.475 and 1.550 seconds, live hosted input takes 0.123 and 0.101 seconds, and the native project-manifest cases take 0.388 and 0.312 seconds. The 15,563-byte Windows report has SHA-256 `c34a2199e548631323b2186dda0dcf8ffcb0a3a3c6eb7d53d9a405c314837a4b`. The 15,473-byte Debian report has SHA-256 `0a8116b03185d7344dd47fb0996c1cc9402c3b9583522574a2a77b0e2fa1f5cf`; its 12,160-byte timing report has SHA-256 `637e0f465a28a3d9e9f3160eadc276236de488aea88dba1fe4997d50498e7485`. Their normalized contracts match exactly with SHA-256 `d7af450f930865f91672e6291d2da80f7226330c53d5d4b66c0d597c088c9711`.

All 62 current portable artifacts, totaling 7,753,361 bytes, match byte for byte. The new 714-byte `Module-Composition-Demo-Project.wvb` accounts for the increase from 61 and has the established composition SHA-256 `0980b7178943be516cd9b6924f179d5977ca147e11bf105c5063ea078c645b60`. The canonical name/size/SHA-256 manifest has SHA-256 `5c21498b51ad93d5e41e895249e288aaa203abb07bcb00de305b9b686764bf17`. The retrieved 2,299,392-byte Debian evidence bundle has SHA-256 `38c1884323badcab44d4a5b5e53d8bbdc51e2311560e97ea0837a19ca8737e15`.

Both hosts pass all 15 OS tests. Pinned QEMU 11.0/Q35/TCG boots the unchanged exact 15,872-byte probe-16 image with SHA-256 `206a036f8cbe3198544b6878bf52c80ef8d489c14d5437c6c7004ff1d6599504`, emits the complete success transcript, and returns guest-controlled host exit code 1. GitHub [Verify run 30708475858](https://github.com/eworker-inc/Windvale/actions/runs/30708475858) independently passes classification, Windows, and Linux verification for the exact implementation commit.

After evidence retrieval, the exact Debian QA tree, transferred source archive, and remote evidence bundle were removed and confirmed absent.

This supersedes Decision 0077 as the latest qualified native-runtime and OS evidence. It qualifies both process-input stencil sources, ordered typed patching through the C# consumer, the portable project parser/native shell, and their live use. It does not claim a general baseline JIT, Windvale-owned WVO loading or patch application, native publication, arenas, a standalone native build driver, in-guest WVB loading, or .NET retirement.

## First Windvale native-stencil consumer qualification

Decision 0079 implements `Compiler/Windvale/Native-Stencil-Core.wv`, the first capability-free Windvale consumer of the two retained WVO/WVSP process-input artifacts. It validates every byte of each complete object shape, derives the six current ABI values from closed semantic patch kinds, constructs immutable output, and reports a typed status plus failure offset. The production-tied demo covers both valid outputs, deterministic repetition, representative malformed families, and one changed value at every byte position of both objects.

The qualified `Native-Stencil-Core.wvb` is 21,295 bytes with SHA-256 `d40fc83c3288043c7af80a261e351066bf3507913b34371a9839014b51ed4b2f`. The composed `Native-Stencil-Demo.wvb` is 26,330 bytes with SHA-256 `651d9435c2b11b4f102a086615bdd159eb981096e2a2324027d5f86a29e36a15` and returns `0` under the reference interpreter, native JIT, and linked WVO/AOT. The final pre-commit focused Windows test passes in 0.879 seconds.

Exact commit `f3a4ba4ebf4d2e9de28dfa73e34e14362691919b`, tree `e0eac295bc42c19bf6a1461b5e32e2c7c5fae33b`, was published to both configured remotes. Its 6,727,680-byte archive has SHA-256 `073e3465d0f19516f9c26c6bad55d6bbac0cbfdacb1336eb30589b981e0c6b05`; the archive retained the same size and digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 61 Seed tests, exact compiler reproduction, and the complete native CLI/reproduction gate. Complete Qualification takes approximately 492.4 and 490.8 seconds, with suite times of 246.227 and 248.495 seconds. The new consumer case takes 0.390 and 0.358 seconds, the existing assembled-stencil case 1.596 and 1.210 seconds, hosted input 0.140 and 0.102 seconds, and the golden contract 184.619 and 187.213 seconds.

The 15,798-byte Windows report has SHA-256 `9196b6f7049cd810c8bfd09037886e6c8628c1631c053543250290bc2acd50d7`; its 12,741-byte timing report has SHA-256 `b7b8f6a2b9f71d567cab97188dbaf7c242dcec7ca8afd685d4ab30456439773a`. The 15,705-byte Debian report has SHA-256 `c05f408eeb3d356e719382321705918bb6f9e6da803cd8e4efd5ddcfb1ebcc1f`; its 12,322-byte timing report has SHA-256 `280b039eb7fbd0e08f38847664221b950942d7effc43acccf025dc1f4333348d`. Their normalized contracts match exactly with SHA-256 `77cd1d742509fc235f23b2dd743518a5694bd94c80d4bb6d81340dfe37e506e4`.

All 64 portable artifacts, totaling 7,800,986 bytes, match byte for byte; their canonical name/size/SHA-256 manifest has SHA-256 `8ed951d59d31c0f9d2975f5ada128c2ee7faf84f69e76bf930b58482749b3374`. The retrieved 2,320,237-byte Debian evidence bundle has SHA-256 `0c9d142897eee8279a7a4f6ab25f5d051b2c4b604e77b916304f17f46ef6cf15`.

Both hosts pass all 15 OS tests. Pinned QEMU boots the unchanged 15,872-byte probe-16 image with SHA-256 `206a036f8cbe3198544b6878bf52c80ef8d489c14d5437c6c7004ff1d6599504`, emits the complete success transcript, and returns guest-controlled host exit code 1. GitHub [Verify run 30711006342](https://github.com/eworker-inc/Windvale/actions/runs/30711006342) independently passes classification plus Windows and Linux verification for the exact candidate. After evidence retrieval and comparison, the resolved Debian QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

C# remains the live loader and oracle because the current native invocation returns only `i32`. This milestone transfers portable acceptance and patch semantics, not yet the live byte-return or W^X publication boundary.

The first detached Windows qualification attempt passed all 61 in-process tests but inherited a redirected-console code page while reading later UTF-8 CLI output, replacing the macron in `Sumˉdata` and correctly failing the exact text assertion. Because this had also appeared in an earlier milestone, the verifier now fixes its native-process input/output encoding to UTF-8 internally. The incomplete Windows run and concurrently running pre-fix Debian attempt were discarded. The exact detached/redirected Windows rerun from `f3a4ba4` passed the formerly failing macron assertion and the complete gate.

This supersedes Decision 0078 as the latest qualified native-runtime and OS evidence. It qualifies Windvale ownership of the two exact stencil acceptance and patch contracts, not a general WVO loader, native byte-result bridge, W^X publication, arena/execution ownership, standalone native build driver, in-guest WVB loading, or .NET retirement.

## Native byte-result and live stencil-consumption qualification

Decision 0080 admits capability-free portable `Main() -> bytes` through one independently decoded native descriptor entry. The executor supplies one verified 16-byte result cell in physical `RCX` on both host ABIs, accepts successful data only from an exact immutable fragment-data symbol or the committed execution-arena prefix, and copies before teardown. Scalar entries remain byte-for-byte unchanged and hosted byte entries remain rejected.

`Compiler/Windvale/Native-Stencil-Bridge.wv` embeds the two retained production objects, calls the Decision 0079 core, and returns their exact instantiated leaves as one immutable 75-byte bundle. Its retained 21,447-byte `Native-Stencil-Bridge.wvb` has SHA-256 `5e1c6c360d93ac54c9281adb0f27b53c77937cf78027e80a9d3fc177877ae7e9`. The live runtime verifies, lowers, independently decodes, publishes through W^X, executes, and caches that Windvale-produced bundle, then splits it into the unchanged five-byte count leaf and 70-byte argument leaf. The C# stencil implementation remains an independent oracle and malformed-object test surface, not the live constructor.

Exact commit `f547af8dcf8e257ab8ad8a76a49bbdd1b9136677`, tree `76de4b2d0956230ed4526fa0a7ef4f502168ae2c`, was published to both configured remotes. Its 6,789,120-byte archive has SHA-256 `4b646bcbaa24e6737e21c4b252d94b4097081552bd2c7dd788a4d2e1633bc45e`; the archive retained the same size and digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 62 Seed tests, exact bridge reproduction, and the complete native CLI/reproduction gate. Complete Qualification takes approximately 491.5 and 499.6 seconds, with suite times of 234.260 and 250.804 seconds. The new descriptor-bridge case takes 0.214 and 0.233 seconds; the Windvale stencil-consumer case takes 0.605 and 0.531 seconds; the assembled-stencil case takes 1.852 and 2.099 seconds; and the golden contract takes 170.865 and 184.643 seconds.

The 15,798-byte Windows report has SHA-256 `9196b6f7049cd810c8bfd09037886e6c8628c1631c053543250290bc2acd50d7`; its 12,922-byte timing report has SHA-256 `e84dc5059468b178e5fc04ade2536e9af7bbddf1e441711609a22d17d5e10942`. The 15,705-byte Debian report has SHA-256 `c05f408eeb3d356e719382321705918bb6f9e6da803cd8e4efd5ddcfb1ebcc1f`; its 12,497-byte timing report has SHA-256 `ebd00af20567ea0f9f5d053c0f74aaf9e84cb1a118d437501d9e13cc222c20aa`. Their normalized contracts match exactly with SHA-256 `77cd1d742509fc235f23b2dd743518a5694bd94c80d4bb6d81340dfe37e506e4`.

All 65 portable artifacts, totaling 7,822,433 bytes, match byte for byte. The new bridge WVB accounts for the increase from 64. Their canonical name/size/SHA-256 manifest has SHA-256 `125a13fd20da4a8b94440854bb7984f3724c2d3859068122b30994ac3c80764a`. The retrieved 2,327,039-byte Debian evidence bundle has SHA-256 `430a8ab6cf6a18f0902a7b2e1352d4363209c94bfcba48cdf1c3acdf5a62d9bb`.

Both hosts pass all 15 OS tests. Pinned QEMU boots the unchanged 15,872-byte probe-16 image with SHA-256 `206a036f8cbe3198544b6878bf52c80ef8d489c14d5437c6c7004ff1d6599504`, emits the complete success transcript, and returns guest-controlled host exit code 1. GitHub [Verify run 30712510080](https://github.com/eworker-inc/Windvale/actions/runs/30712510080) independently passes classification plus Windows and Linux verification for the exact implementation commit.

The Windows verifier now fixes native-process input, output, and pipeline encodings to UTF-8 internally. The exact detached and redirected qualification preserved every macron-bearing CLI identifier, closing the repeated launcher-only decoding problem at the verifier boundary. After evidence retrieval and comparison, the resolved exact Debian QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

This supersedes Decision 0079 as the latest qualified native-runtime and OS evidence. It qualifies the exported byte-result contract and live Windvale production of both process-input leaves, not Windvale-owned WVB loading, native lowering or independent verification, W^X publication, arena/lifetime ownership, standalone native host tools, in-guest WVB loading, or .NET retirement.

## Terminal CPU exception and native publication-layout qualification

Decisions 0081 and 0082 qualify two separate ownership advances from the same exact candidate. Probe 17 installs the first kernel-owned terminal x86-64 exception destination for invalid opcode. The native executor now asks portable Windvale code to validate the closed service inventory and choose the complete executable-image extent plus canonical 16-byte service placements before writable allocation; the independently verified fragment bytes are copied without a second C# patch rewrite. Windows/Linux memory allocation and protection, instruction-cache publication, copying, invocation, arenas, and teardown remain in the narrow C# platform adapter.

`Native-Publication-Core.wvb` is 7,189 bytes with SHA-256 `9d75d59e4ba0fc689ae9bc4ac3ac019e520db06d21f54d4ee1480a0bb356e967`. The retained 7,105-byte `Native-Publication-Bridge.wvb` has SHA-256 `5102fd0119e37bb7e5f83bb3c4d1bff6303f37818bfe48825b320bf28f27eada`, exactly one `file.read_bytes` capability, and exactly one `Main() -> bytes` export. The focused case covers no services, all eleven ordered services, exact fragment and 34 MiB image ceilings, every request status family, deterministic repetition, malformed response envelopes and placements, source/retained reproduction, and live unchanged relative-data patch execution.

Exact commit `ba2cf69cd4a97876f5e953b3938d032fc75a8ff7`, tree `7ca8f6fdbf5a7caff8300ad4b1dbd490295930d6`, was published to both configured remotes. Its 3,014,724-byte archive has SHA-256 `ef6892d6c37cdc5e1461e66c8f2d5b72d7514f847078950220124b92690daa3c` and retained the same size and digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 63 Seed tests, exact compiler and retained-WVB reproduction, and the complete native CLI gate. Complete Qualification takes 485.476 and 495.249 seconds wall-clock, with suite times of 241.821 and 247.813 seconds. The publication-layout case takes 17 and 22 milliseconds; the golden contract remains the dominant case at 181.626 and 186.686 seconds.

The 15,798-byte Windows report has SHA-256 `9196b6f7049cd810c8bfd09037886e6c8628c1631c053543250290bc2acd50d7`; its 13,084-byte timing report has SHA-256 `4b1bc7a87126bdb5de8961b707f8f03931876760aba1d45faf0144086efef77d`. The 15,705-byte Debian report has SHA-256 `c05f408eeb3d356e719382321705918bb6f9e6da803cd8e4efd5ddcfb1ebcc1f`; its 12,654-byte timing report has SHA-256 `b1b7b7210eebe5dce7465ae2afbed630c018c29bd23fa384148678675f932f72`. Their normalized contracts match exactly with SHA-256 `77cd1d742509fc235f23b2dd743518a5694bd94c80d4bb6d81340dfe37e506e4`.

All 67 portable artifacts, totaling 7,836,727 bytes, match byte for byte. Their canonical name/size/SHA-256 manifest has SHA-256 `527ac023a43dc6ca455c3b79654617138cc0bd5dd8b36d0801ba6f788ae3634c`. The retrieved 2,334,586-byte Debian evidence bundle has SHA-256 `a6a7441b7f790e2d1052e63ecd04f92bec17f8325acc6b7aaeeab74a91fb8e33`.

Both hosts pass all 17 OS tests. Pinned QEMU 11.0/Q35/TCG boots both exact 17,920-byte probe-17 images. The ordinary image has SHA-256 `d2c0a7e4e5e1605fc8639c05ab27ad07ee2b015ad2dc151d8637830b8acb3f18`, emits the complete success transcript including `cpu-exceptions=armed`, and returns guest-controlled host exit code 1. The explicit invalid-opcode image has SHA-256 `26ccfaf862024e022339ca9fa8114c71b4fe601fe59a806d366e1d330b6d106d`, emits the exact terminal panic suffix with no later success marker, and returns expected host code 3 without timeout. GitHub [Verify run 30715672194](https://github.com/eworker-inc/Windvale/actions/runs/30715672194) independently passes classification plus Windows and Linux verification for the exact implementation commit.

The verifier's permanent UTF-8 native-process and pipeline settings remain effective in this exact candidate: detached and redirected Windows qualification preserves the macron-bearing CLI assertion that previously exposed the recurring launcher-only decode failure. No normalization or weakened text comparison is used. After evidence retrieval and comparison, the resolved exact Debian QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent.

This supersedes Decision 0080 as the latest qualified native-runtime and OS evidence. It qualifies Windvale ownership of executable-image layout and one terminal CPU exception destination. It does not qualify Windvale-owned native lowering or fragment verification, OS memory calls, complete W^X publication/lifetime, arena ownership, a general exception/interrupt system, standalone native host tools, in-guest WVB loading, clean shutdown, Hyper-V, or .NET retirement.

## Native publication-lifetime qualification

Decision 0083 qualifies the next bounded native ownership transfer without changing ABI 14, execution-context version 6, service-table version 4, WVB 1.6, WVO 1.0, kernel bridge 9, or either probe-17 image. Portable Windvale code now emits the exact nine-transition publication graph for one already-planned executable image. C# independently reconstructs every transition, while one small internal executable-image owner contains the raw address, actual state, and Windows/Linux allocation, copy, W^X seal, instruction-cache publication, invocation, and release operations. Release is admitted from writable, copied, executable, and invoked states; malformed plans and invalid action order fail before the platform operation.

`Native-Publication-Lifetime-Core.wvb` is 4,954 bytes with SHA-256 `52b1cb6dd0d7fa9d17c1cba50b527912876e4acf1cd9663846ce915b4c56aed5`. The retained 4,857-byte `Native-Publication-Lifetime-Bridge.wvb` has SHA-256 `74dfaf40bb6ea83f0fd72757c9c4cb85f5c8dd28a41f3993325871d348e88d32`, exactly one `file.read_bytes` capability, and exactly one `Main() -> bytes` export.

Exact implementation commit `a898fe84a34e96c3768b77ab50ffc5bed6401fb0`, tree `70ae2e901d1eebd192d47b0f9a6b3a45e588adf1`, was published to both configured remotes. Its 2,806,844-byte archive has SHA-256 `69124612af949ada890b844c76814975b23717e04269077321a76d070635e042` and retained the same size and digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 64 Seed tests, exact compiler and retained-WVB reproduction, and the complete native CLI gate. Complete Qualification takes 484.1 and 513.7 seconds wall-clock, with suite times of 240.113 and 256.330 seconds. The publication-lifetime case takes 12 and 14 milliseconds; the golden contract remains the dominant case at 178.379 and 191.437 seconds.

The 15,798-byte Windows report has SHA-256 `9196b6f7049cd810c8bfd09037886e6c8628c1631c053543250290bc2acd50d7`; its 13,237-byte timing report has SHA-256 `48da2991322f68ec4b711d06b0da9edecdcd433d27e315151a537c7cc8b3d99a`. The 15,705-byte Debian report has SHA-256 `c05f408eeb3d356e719382321705918bb6f9e6da803cd8e4efd5ddcfb1ebcc1f`; its 12,803-byte timing report has SHA-256 `c38e69979a60259924bb49811b0ceb69b9e36bb58bbe5286719bd73851c5ff7b`. Their normalized contracts match exactly with SHA-256 `77cd1d742509fc235f23b2dd743518a5694bd94c80d4bb6d81340dfe37e506e4`.

All 69 portable artifacts, totaling 7,846,538 bytes, match byte for byte. The two new lifetime WVBs account for the increase from 67. Their canonical name/size/SHA-256 manifest has SHA-256 `ae7377b88e16be49566ac634878564c64e9a3d8e6500fa53436526b675f15711`. The retrieved 2,313,699-byte Debian evidence bundle has SHA-256 `d2de5b3acdfa8189be14f10a3c27f82fd7f5797324f51ee6068bf5c5ed5df9b0`.

Both hosts pass all 17 OS tests. Pinned QEMU 11.0/Q35/TCG retains both exact 17,920-byte probe-17 images. The ordinary image has SHA-256 `d2c0a7e4e5e1605fc8639c05ab27ad07ee2b015ad2dc151d8637830b8acb3f18`, emits the complete success transcript, and returns guest-controlled host exit code 1. The explicit invalid-opcode image has SHA-256 `26ccfaf862024e022339ca9fa8114c71b4fe601fe59a806d366e1d330b6d106d`, emits the terminal vector-6 panic with no later success marker, and returns expected host code 3. GitHub [Verify run 30717660874](https://github.com/eworker-inc/Windvale/actions/runs/30717660874), attempt 2, independently passes classification plus Windows and Linux verification for the exact implementation commit.

The verifier's permanent UTF-8 input/output/pipeline configuration remains green on the exact archive, including the macron-bearing native CLI assertion under redirected execution. This is a retained regression contract, not output normalization. After evidence retrieval and comparison, the resolved exact Debian QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent; the exact local archive and evidence remain retained.

This supersedes Decision 0082 as the latest qualified native-runtime and OS evidence. It qualifies Windvale ownership of the executable-image lifetime policy and isolates all raw executable-memory authority in one internal C# owner. It does not qualify Windvale-native platform calls, context/service-table/arena/result-cell lifetime, a native loader or code cache, a general exception/interrupt system, standalone native host tools, in-guest WVB loading, clean shutdown, Hyper-V, or .NET retirement.

## Native file-output and composed probe-20 qualification

Decisions 0085 through 0087 qualify three integrated advances at one exact source state. WVA owns the fixed Q35 clean-poweroff adapter plus normalized entry stubs for invalid opcode and general protection. ABI 15/context 7/service-table 5 appends native service 12, `file.write_bytes`, behind an explicit runtime-private `WVFO 1` table. The exact platform leaves create or replace one bounded file, complete partial writes, flush, close, and map expected host failures without calling the managed writer. Probe 20 composes those contracts through bridge 10 while providing a zero file-output pointer to its service-free guest module.

The Windows leaf is 787 bytes with SHA-256 `a331248b12fc5830587f6fd8ddf06a546859b8f57366e205032aa2c37db48bb1`; the Linux leaf is 823 bytes with SHA-256 `fc688f2a84936dc1082fcb5654667a8a60b0581bff29b1868d48ef2d4af77422`. The 7,189-byte publication core has SHA-256 `b25fa550518caa4ef43c7ae886cce328148777782f70e3faa25ac19821b6d439`, and its 7,105-byte retained bridge has SHA-256 `750b6134395c46c9e1c703ae2a56449bd1710f517e516397e10a1ccc951c503e`. Exact preflight of the qualified 599,868-byte compiler WVB with SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066` advances from unsupported `file.write_bytes` to bounded diagnostic `WVN2002` in `Compilerˉbodyˉblockˉstepˉvalid`. Subsequent exact inventory under Decision 0089 proves the record return was already supported and the actual rejected shape was eight parameters against ABI 15's four-parameter limit.

Exact implementation commit `12e9e2ebcd4960f856b90064f6343ea5856b5b43`, tree `3b42f22fcc2f181029847ac6e1549650b0b49031`, was published to both configured remotes. Its 2,868,384-byte archive has SHA-256 `a52e68505f7ac9aeef5a3bfacf16c5c722fa60efaac75b3c9d7ddb85c0b600a7` and retained the same digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 66 Seed tests, exact compiler and retained-WVB reproduction, and the complete native CLI gate. Complete Qualification takes 523.3 and 524.3 seconds wall-clock, with suite times of 257.247 and 267.047 seconds. The native-file-output case takes 1.015 and 1.162 seconds; the browser-playground contract added by the same integrated source state takes 0.719 and 0.229 seconds; and the golden contract remains dominant at 189.984 and 197.493 seconds.

The 15,798-byte Windows report has SHA-256 `2b394b61a01e03a5e81623e203028303b4e29595dfec36b15bdd8d5ca6105182`; its 13,566-byte timing report has SHA-256 `f26b28352ef1170113842c51491f45c338243d4e609ae0c12c69f2ca405bc508`. The 15,705-byte Debian report has SHA-256 `59fb87fd8695ba2199bf6d404424366112b322e197c2c7434624d431f80a6ccb`; its 13,125-byte timing report has SHA-256 `30a1ee9fa772d06bf638706eb6393f0b3d9f71b7cc5477a0120200a2bddfc95f`. The built-in report comparator confirms exact normalized-contract equality; the canonical compact contract JSON has SHA-256 `e41b9c1ccae150ebac3465cdfee74f89796451d528f4f246f70290926e02e621`.

All 69 portable artifacts, totaling 7,848,859 bytes, match byte for byte. Their canonical name/size/SHA-256 manifest has SHA-256 `2545622421f77c8f40180732d779e7c82a9575b15e4deef45f8072ccf4aac676`. The directly retrieved 2,334,180-byte Debian portable-evidence bundle has SHA-256 `97795302a4bb03ccae660121a3b592f990c91504004bd7b6adeca13dcc222ae4`.

Both hosts pass all 18 OS tests. The version-5 WVA shim remains 620 bytes with SHA-256 `4bb07c28877905de6e57d79454e33402de4ac54048d5ce09a26b49ad0d8347a5`; bridge-10's 143 code bytes produce a 355-byte object with SHA-256 `bfa2b522b2bf22b3681b523c66c2986b1af366ba042adeddd8a106b5a96a5225`. Exact-archive pinned QEMU 11.0/Q35/TCG qualifies all three 20,992-byte probe-20 images. Normal SHA-256 `d4a9e3625779dd3ef2a03fd71ecfe1502c1ad39378da7adbcf7e4b55636eed8c` emits the complete clean-shutdown transcript and exits 0. Invalid-opcode SHA-256 `705670b1054589b80e3c918c03e9f751304e3f4b5bda77485f606433db68a757` emits `(6, 0)` and exits 3. General-protection SHA-256 `df45d8e0f69581e5ed3b46608598e6170413f80c5c1bbba9233e9842cdd7a04d` emits `(13, 0)` and exits 3. The Debian QA host does not provide QEMU, so its OS evidence is the complete 18-test suite rather than a duplicate emulator run.

GitHub [Verify run 30721964387](https://github.com/eworker-inc/Windvale/actions/runs/30721964387) independently passes classification plus Windows and Linux verification for the exact implementation commit. The permanent UTF-8 input/output/pipeline configuration remains green, including the macron-bearing native CLI assertion under redirected execution.

The first exact implementation archive, commit `54ff401`, passed all 66 in-process tests on both hosts but qualification correctly rejected stale hard-coded publication-core and bridge identities in the verifier. Commit `12e9e2e` updates those qualification pins to the already-produced exact artifacts and reruns the complete gate; no artifact or runtime semantic was normalized away.

After evidence retrieval and comparison, the exact Debian QA trees, transferred source archives, remote evidence bundles, and earlier working-copy QA inputs were removed and confirmed absent. The exact local archive, both host reports/artifacts, and retrieved portable-evidence bundle remain retained.

This supersedes Decision 0083 as the latest qualified native-runtime and OS evidence. It qualifies all twelve current native service leaves, bounded whole-file publication, WVA-owned Q35 shutdown, normalized vector-6/vector-13 terminal entries, and composed probe 20. It does not qualify ABI-16 stack arguments, native compiler execution, Windvale-owned platform memory calls or service-table construction, a general trap/interrupt system, in-guest WVB loading, page-table ownership, standalone native host tools, Hyper-V, or .NET retirement.

## Bounded native stack-argument and probe-21 qualification

Decision 0089 replaces the current fragment target with ABI 16 while retaining execution-context version 7 and service-table version 5. Positions zero through three keep the prior register convention. Each later position receives one exact 16-byte outgoing cell; the source-language maximum of 64 parameters therefore bounds a call-site reservation to 960 bytes. The caller releases that reservation before packed-status propagation, and the callee copies every later scalar or descriptor into its own initialized frame.

Focused Windows differential evidence passes the maximum 64-parameter scalar call, a fifth-position borrowed-byte descriptor with descriptor return, and a five-parameter void call through the reference interpreter, actual W^X execution, and linked WVO/AOT. A second compilation is byte-identical. The independent fragment verifier rejects a changed maximum reservation, first outgoing-cell displacement, descriptor high-word destination, and release size as `WVN3030`.

The fresh zero-warning Windows Standard gate on the integrated Decisions 0088 through 0090 tree passes all 67 Seed tests in 237.692 seconds. The new case takes 39 milliseconds and the golden closure remains dominant at 177.231 seconds. All 43 compiler-area tests passed in the earlier focused sweep. All 21 current OS tests also pass, including the later fixed WVB-admission slice. The service-free probe's at-most-four-parameter code and WVO remain unchanged by the ABI extension; probe-21 firmware identities change only because Decision 0090 adds admission artifacts and evidence. The 15,798-byte candidate report has SHA-256 `310b2b003caf04e257bd14d9ae614f2bf02668fc5f6df0c26d9267ba4d1c25b1`; its 13,729-byte timing report has SHA-256 `dd663af1fa49004896174d195d822b9d088226ca3d1c8c72d61dc917d8220053`.

The exact 599,868-byte compiler WVB contains 328 functions: 103 require five through 23 parameters. ABI 16 passes the former eight-parameter blocker and all wider signatures before preflight stops at `WVN2002` in `Compilerˉsourceˉwirˉcompileˉblock`. That function has 11 supported parameters, 1,049 locals, and declared stack depth 12 against the native frame's current 1,024-slot cap. It is the compiler's only function at or above 1,024 locals; observed maxima are 1,049 locals, stack depth 34, and locals-plus-declared-stack depth 1,061. The next slice must measure lowered numbered-value pressure before choosing a larger fixed frame or deterministic slot reuse.

Exact integrated commit `860c69c00995de6ed048cb65f8bfb158287f19a2`, tree `5c885feac990dc65ab5a7577fd44c6d39dc55c10`, was published to both configured remotes. Its 7,057,219-byte source archive has SHA-256 `fdcddb7ebdbab7b791ef5e1c0e98e87fc0b4415ed5b078fe427c8920ff4c08a6` and retained the same digest after transfer to the isolated Debian GNU/Linux 12 x64 QA host with .NET SDK `10.0.302`.

Windows and Debian pass zero-warning Release builds, all 67 Seed tests, exact compiler and retained-WVB reproduction, and the complete native CLI gate. Complete Qualification takes 501.3 and 517.2 seconds wall-clock, with suite times of 243.529 and 257.101 seconds. The bounded-wide-call case takes 38 and 39 milliseconds; the golden contract remains dominant at 180.409 and 191.031 seconds. The permanent redirected native-process UTF-8/macron regression passes on both hosts.

The 15,798-byte Windows report has SHA-256 `310b2b003caf04e257bd14d9ae614f2bf02668fc5f6df0c26d9267ba4d1c25b1`; its 13,728-byte timing report has SHA-256 `79a7e3e22accc8fafec9a8b710d7e194cd670acf009bcbbf02f5f2b527f47415`. The 15,705-byte Debian report has SHA-256 `f54dbe55cd43e2199423034d68917baef60d8853b23995860ad10576b6639aaa`; its 13,280-byte timing report has SHA-256 `1734843c35e47362099726443a25242ed216cda562de10c0f099f4c626308ffe`. The built-in comparator confirms exact normalized-contract equality; the canonical compact contract has SHA-256 `240595dd55f602f724951e2e1d644ba577a1783606380ae584e210c82df9369b`.

All 69 portable artifacts, totaling 7,851,187 bytes, match byte for byte. The directly retrieved 2,335,941-byte Debian evidence bundle has SHA-256 `d39b1d8c8494a76337e36b492c07ca9d3342025b8062cf16c4fa55d96e0621f5`. Both exact hosts pass all 21 OS tests. Exact-archive pinned QEMU reproduces all three 47,104-byte probe-21 images: normal SHA-256 `c3a07e1a6c8f162720a3dcd690fdb945bd862b360b26665c53e5be0642a87c38` exits 0 after the complete paging/admission/shutdown transcript; invalid-opcode SHA-256 `0bbc0b6eedbd21aef853a2233d3fa3dbaa9564eca36f3344067b1c9b240237fc` emits `(6, 0)` and exits 3; general-protection SHA-256 `724907ffd0963f015003c91431b79b728109ed861de73f3c1b0bf5e7b58568b6` emits `(13, 0)` and exits 3.

GitHub [Verify run 30724785769](https://github.com/eworker-inc/Windvale/actions/runs/30724785769) independently passes classification plus Windows and Linux Qualification for the exact implementation commit. This supersedes Decision 0087 as the latest qualified native-runtime and OS evidence. It qualifies ABI 16 stack arguments, kernel-owned page tables, and one fixed Windvale-owned verify-before-execute WVB admission path. It does not qualify native compiler execution, frame-bound expansion, register allocation, a general guest verifier/loader, process isolation, standalone host tools, Hyper-V, or .NET retirement.

After evidence retrieval and comparison, the resolved exact Debian QA directory, transferred source archive, and remote evidence bundle were removed and confirmed absent. The exact local archive, Windows artifacts, and retrieved Debian evidence bundle remain retained.

## Probe-23 cross-host build and Seed evidence

Exact commit `22e350b8965bbe70452261dabfc411d28cf7a1d5` adds the first Windvale init/resource service, two protected CPL3 roots, reduced send/receive endpoints, deterministic block/wake coordination, and contained client fault evidence. GitHub [Verify run 30730151722](https://github.com/eworker-inc/Windvale/actions/runs/30730151722) passes classification plus the Windows and Linux verification jobs.

Both jobs restore and compile the complete solution, including `Windvale.Bootstrap` and `Windvale.Os.Tests`, then pass all 67 Seed qualification tests. The Windows suite reports 217.039 seconds; Linux reports 208.599 seconds. This is valid cross-host build and Seed evidence for the exact source state.

The workflow does not execute the `Windvale.Os.Tests` binary. Probe 23's 25 focused OS tests and four pinned-QEMU scenarios pass on Windows, but a Linux run of the OS-test binary from the same exact source state has not been recorded. Probe 23 therefore remains a candidate rather than replacing exact commit `860c69c`/probe 21 as the latest fully cross-host-qualified OS baseline.

## Probe-24 cross-host Seed and OS qualification

Exact commit `190174a01299369fb855e27ea676d34062e09c5b` adds the first user-space Windvale bytecode interpreter and integrates the focused OS suite into every regular non-Fast repository verification. The client process executes the admitted 174-byte WVB through its AOT-built Windvale interpreter at CPL3; the admitted program's host-built AOT derivative is absent from that computation path. The separate init service, two roots, reduced endpoints, deterministic wake, contained client fault, and terminal kernel faults remain composed.

GitHub [Verify run 30732061301](https://github.com/eworker-inc/Windvale/actions/runs/30732061301) passes classification plus the complete Windows and digest-pinned Debian 12 verification jobs for that exact source state. Each host passes all 67 Seed tests and all 25 OS tests. Windows Seed elapsed time is 247.422 seconds and Debian Seed elapsed time is 145.020 seconds. This promotes probe 24 over probe 21 as the latest fully cross-host-qualified OS checkpoint and qualifies the retained probe-22/probe-23 process and init-service composition.

Pinned Windows QEMU 11.0/Q35/TCG supplies the machine evidence: the 114,176-byte normal image SHA-256 `4248f3402a0abfe9eda531109460fb37a7c1f3f907ded162a49a785bab38fbb7` exits 0; the same-sized invalid-opcode image SHA-256 `465ac8d095160e169187860413b0b347148df4acc2f24802f7a5e166981f6c59` and general-protection image SHA-256 `0a65ba2982f551434553054fc92d82a1a6e727a73527c85a79526f328105b58c` exit 3; and the 114,688-byte contained-interpreter-fault image SHA-256 `8cd17f693ae088eefaccb2b8449fd47f34e345352bb46beebd4a2a32fe8fad5d` exits 0 after the independent init service completes. The cross-host workflow does not run QEMU on Debian, so it qualifies deterministic construction and all OS contract tests on both hosts without claiming duplicate Linux emulator evidence.

This evidence qualifies the fixed-offset interpreter profile, not arbitrary WVB input, general section discovery, a complete verifier/interpreter, JIT publication, scheduling, process lifecycle, Hyper-V, physical hardware, or .NET retirement. Decision 0094 begins the next candidate section-derived profile from this exact checkpoint.

## Probe-25 section-derived interpreter qualification

Exact commit `33555fdc4305f457638431ddbc40cb79fafa51c3` advances the CPL3 Windvale interpreter from fixed serialized offsets to checked WVB 1.6 envelope traversal, seven ordered section payloads, and one bounded function/export profile. A second compiler-produced WVB with a longer module name moves its code payload and still returns result 29. The process contract records interpreter profile 2, a 32-page RX image, four-page RW/NX stack, one RW/NX context page, exact instruction/call-depth budgets, and policy token 94. Kernel memory version 3 selects a complete 58-page arena at a 2 MiB-aligned address; paging version 3 admits the measured 256 KiB executable window.

GitHub [Verify run 30733685025](https://github.com/eworker-inc/Windvale/actions/runs/30733685025) passes classification and the complete non-Fast Windows and digest-pinned Debian 12 verification jobs. Each host passes all 67 Seed tests and all 25 OS tests. Windows Seed elapsed time is 228.831 seconds and Debian is 207.238 seconds.

Pinned Windows QEMU 11.0/Q35/TCG passes all four exact probe-25 scenarios. The 214,528-byte normal image SHA-256 `99686e2b2484de156a01bbec0ece68e6a15af7229a477d9245ee4f3068ed4f5b` exits 0; the same-sized invalid-opcode image SHA-256 `da14fd39c4ebf19471bb7205aa1cfce5b40a52c1b64979d5ff62eed143d64e90` and general-protection image SHA-256 `02e6a297c6495a0c35b4b799403a0cedc8a1538c3a62c52836b2fe6fa6a225c9` exit 3; and the 215,040-byte contained-interpreter-fault image SHA-256 `ede1d1d0ae8638086c81564e3150829137f4f3aceead9e7bfddeedc9445ae2f6` exits 0. The cross-host workflow does not claim a Debian QEMU run.

This evidence qualifies section-derived execution of one embedded admitted module. It does not qualify runtime-supplied WVB transport, arbitrary modules, general semantic verification or interpretation, executable publication, JIT, scheduling, process lifecycle, Hyper-V, physical hardware, or .NET retirement.

## Probe-26 runtime-supplied WVB qualification

Exact commit `6bb34bb4c6dc23e89fbdcd8592b31f0585f91ec5` removes the admitted program from the interpreter WVB and linked RX image. The hosted Windvale interpreter declares only `file.read_bytes`, requests exact `boot:main.wvb`, and receives a borrowed descriptor through an exact 199-byte WVA-owned ABI-16 service leaf. Protected-process version 5 maps the admitted 174-byte WVB in its own RO/NX page, records runtime profile 3, and independently binds its digest. Kernel memory version 4 expands the aligned boot arena to 59 pages while paging version 3 retains the W^X kernel contract.

GitHub [Verify run 30736314724](https://github.com/eworker-inc/Windvale/actions/runs/30736314724) passes classification and the complete non-Fast Windows and digest-pinned Debian 12 jobs. Each host builds the complete solution with zero warnings/errors and passes all 67 Seed tests plus all 25 OS tests. Windows Seed elapsed time is 154.646 seconds and Debian is 208.700 seconds.

Pinned Windows QEMU 11.0/Q35/TCG passes all four exact probe-26 scenarios. The 215,552-byte normal image SHA-256 `ab6b818beee3ac7419d48ad7ac5d04f06bab0ec67ab3909de64ed1b88c2e1170` exits 0; the same-sized invalid-opcode image SHA-256 `e439c3afa5168743076981aa4c0a278384508b65481d8f7ed0dc68959d4f49e8` and general-protection image SHA-256 `710d34c567f3411cf7728fd211b67e39fcdd175e9a4441960e0f4846d3ef4f52` exit 3; and the 216,064-byte contained-user-fault image SHA-256 `55b2bb810c34e2c4ea6f4f68558c09913ff802c4357282d6b24a6b6b3d6e1dcc` exits 0 after the interpreter resource call and independent init completion. The workflow does not claim a Debian QEMU run.

This evidence qualifies one fixed runtime-supplied immutable WVB and its Windvale OS adapter, not arbitrary loading, a resource namespace, init/package ownership or transfer, general semantic verification or interpretation, executable publication, JIT, scheduling, process lifecycle, Hyper-V, physical hardware, or .NET retirement.

## Probe-27 init-owned resource-grant qualification

Qualified Decision 0096 moves the first real boot-resource selection into Windvale init without changing the admitted WVB, interpreter, ABI-16 service leaf, or result channel. Protected-process version 6 places the exact 174-byte WVB in an init-owned RO/NX page. Process `2` begins with its target PTE absent and context service/resource pointers zero. Init's 273-byte Windvale WVB selects resource identifier `1`; its WVA shim invokes fixed grant syscall `4`, then waits for the existing result.

The grant validates one kernel-owned 128-byte `WVRES001` record and the exact owner/client process state. It installs one RO/NX alias, publishes service-table version 5 and `WVBR` version 1 in the client data page, changes owned state to borrowed, records borrower `2`, and sets grant/mapping counts to one. Init remains lifetime owner. Replay, changed owner bytes, a pre-mapped target, an out-of-range service leaf, changed WVB bytes, or a changed digest fails closed.

Exact implementation commit `4701200e707786f04d4462bb75a1664cd5ed13cc` passes GitHub [Verify run 30739172682](https://github.com/eworker-inc/Windvale/actions/runs/30739172682). Windows and digest-pinned Debian 12 build with zero warnings/errors, pass all 67 Seed tests and all 25 OS tests, and complete the non-Fast verifier. The Seed suites take 236.791 seconds on Windows and 190.185 seconds on Linux. The focused Windows suite also passes all 25 OS tests. Deterministic qualified identities include process-policy WVB `fad470d9988c997daf4e44f90bbfe665391f5f02dd84ba8e8025580efc11c49f` (4,610 bytes), init WVB `0fe423c499ce4f573095ddb9ff03355ee8b6ad927941f764ddaf2eaf9537f78b` (273 bytes), linked init image `ba2a2abe03d420506c79af61cc917f4b0124a2ad7687fa80117e353dde475727` (1,385 bytes), and normal process-machine WVO `d863e61be67659b30b370da8ba9174b712f0d0bd8f02f31b9cdbb9fd523334c3` (137,807 bytes).

Pinned Windows QEMU 11.0/Q35/TCG passes all four exact probe-27 scenarios. The 224,768-byte normal image SHA-256 `709ebd7f643f2f9d9c7cf4eb4042977c675a3ff19d7a34da4d7e26e0526a29b7` exits 0; the same-sized invalid-opcode image SHA-256 `a89e66da871fcf46637ce4d91463268b2c8cce4309d12953eb7c7b464f57178f` and general-protection image SHA-256 `ab10c10cc0af01ebe5603033d2f35bce86660b23953c33ff6012ad7cee83a1c5` exit 3; and the 225,280-byte contained-user-fault image SHA-256 `b5e726b51f26f48cc9948095bfce4eabaf0b3bc90b89a6c3b3650325adfb05bb` exits 0. Normal and contained-fault serial evidence includes `resource-grant=pass` only after the real CPL3 init grant and completes interpretation, IPC, init exit, and shutdown.

This evidence qualifies probe 27's fixed grant on Windows and Linux; QEMU boot execution remains Windows-only evidence. It does not qualify resource names, multiple resources/recipients, general capability transfer, ownership migration, revocation, teardown, reclamation, packages, arbitrary loading, broader semantics, JIT, scheduling, Hyper-V, physical hardware, or .NET retirement.

## Qualified probe-28 terminal resource-borrow cleanup

Qualified Decision 0097 advances protected processes to `WVPROC07`, the 128-byte resource record to `WVRES002`, Windvale policy token `96`, and firmware probe 28 while retaining memory version 5, paging version 3, ABI 16/context 7/service table 5, interpreter profile 4, both user images, the admitted WVB, and the WVA resource leaf byte-identically.

After process `2` exits or reaches the contained vector-13 fault, the process machine revalidates the live immutable borrow. It accepts the exact granted leaf with or without x86-64 accessed bit 5, which hardware sets during the interpreter's WVB read, and rejects every other mutation. It then clears the client PTE, both native-context resource pointers, the complete 104-byte service table, and the complete 32-byte `WVBR` table. The resource returns to owned/no-borrower state with grant count one and mapping count zero; init's physical page, WVB bytes, and digest remain owned and unchanged. Reloading init's CR3 flushes non-global client translations on the current single-CPU path; the client root is retired rather than reused.

The focused Windows suite passes all 25 OS tests. Deterministic coverage proves byte-identical normal, repeated, accessed-leaf, and contained-fault cleanup; rejects nonterminal/mixed state as `WVOS6201`, malformed live records as `WVOS6202`, inconsistent aliases/publication as `WVOS6203`, and a released-record replay; and retains every earlier deterministic artifact and malformed-input check. Qualified identities include policy WVB `c4aacb9036f825ecd3d038954c1d07c573b43eb4f6ee831d0b81d188f0682679` (5,152 bytes), policy WVO `d6290156c6d7cf709ba44b3035fac4eea75995ecf3ddfbcb8a0a9b73c0612509` (45,886 bytes), normal process-machine WVO `19da37cdc044505a92410449a14e72c35ed8573b409c095b0b9a8a8f9d21f065` (138,751 bytes), and fault process-machine WVO `bedd2a06969d3df295efe8eefa626dd1c97737731ef1056fbcb6df152f259138` (138,783 bytes).

Pinned Windows QEMU 11.0/Q35/TCG passes all four exact probe-28 scenarios with `resource-revoked=pass`. The 230,912-byte normal image SHA-256 `bc5f04c0e75fb217c9339bcc2a391bbe68f9f79ad97c18a93e35e310dab62d46` exits 0; the same-sized invalid-opcode image `2c9c6c60543d3729f7401720c0b03e98dc2c5e3654e45668bfcd4559650bc543` and general-protection image `bdccbd123dd457d88c4902f33e727320b08c93e6acd62ac0f706912d5f1163ca` exit 3; and the 231,424-byte contained-user-fault image `221c710d741565c7113a7b8c2ea94c66358018e144ae59f583a3c1ce10225494` exits 0.

Exact implementation commit `b2197fa4fb78b26d75e4fd5269cde590cbd98dcf` passes GitHub [Verify run 30741650532](https://github.com/eworker-inc/Windvale/actions/runs/30741650532). Windows and digest-pinned Debian 12 each pass all 67 Seed tests, all 25 OS tests, and the complete non-Fast verifier. Seed elapsed time is 237.598 seconds on Windows and 210.262 seconds on Linux. This qualified probe 28 as the then-latest cross-host OS baseline. It proves one automatic terminal borrow cleanup, not explicit revocation, page or root reclamation/reuse, SMP shootdown, multiple resources or recipients, ownership transfer, arbitrary loading, JIT publication, scheduling, Hyper-V, physical hardware, or .NET retirement.

## Qualified probe-29 typed two-resource lookup

Qualified Decision 0098 advances protected processes to `WVPROC08`, resource records to two `WVRES003` entries, the private directory to `WVBR002`, memory to `WVKMEM06`, Windvale policy token to `97`, and firmware to probe 29. ABI 16/context 7/service table 5, WVA seam 8, admission bridge 2, retained bridge 10, paging 3, and interpreter profile number 4 remain active.

Init owns two distinct pages: the exact admitted WVB under `boot:main.wvb`, and four little-endian bytes encoding budget `4` under `boot:main.budget`. Its Windvale `Main` selects ordered set token `131073`. The kernel accepts no unknown, duplicate, reversed, or partial set; validates both source pages, digests, records, and absent target PTEs; then installs both aliases and publishes the 80-byte typed directory atomically. The WVA-owned 347-byte leaf validates one of the two exact entries. The Windvale interpreter charges one unit for each of the canonical four opcodes; budget `3` exhausts before return. Exit and contained fault revalidate and clear both aliases and the complete publication while preserving init ownership and one historical grant per record.

Pinned QEMU exposed a machine-only stack bound that the reference models could not show. The enlarged Windvale process policy overwrote state on the old two-page kernel stack; a measured three-page configuration still did not complete process construction; four pages pass the complete composition. Memory version 6 therefore uses a 63-page arena: state page 0, kernel stack pages 1–4, IDT page 5, paging pages 6–11, init pages 12–20, and client pages 21–62. The process coordinator reconstructs the 2 MiB-aligned arena base from the owned stack and revalidates `WVKMEM06` after the policy call.

The local Windows focused suite passes all 25 OS tests. Coverage includes exact/repeated grant and cleanup, ordinary exit, contained fault, hardware accessed bits, distinct source pages/PTEs, exact `WVBR002` entries, budget success/exhaustion/bounds/malformed/missing cases, and rejection of token, budget kind/length/digest/page, directory, PTE, record, and service mutations. Deterministic qualified identities include process-policy WVB `5134108d706aefd18ac90c18cefe793d9ec166f19066484219e2618300e4cedb` (6,955 bytes), interpreter WVB `7fbb25fe08136c86c063c08395451f8db1219bd17e0adc0748b5fa2d9a3f8fee` (12,851 bytes), normal process-machine WVO `b18ccf8f4c2eb065d017e2fafb2254fbf0299af1cf0eb130c3ca3405a34392e2` (149,483 bytes), and fault process-machine WVO `a3bc89dbdd1539934417ae03f4375bd36af1ef6617d1b4f7651b5955f923ebd0` (149,531 bytes).

Pinned Windows QEMU 11.0/Q35/TCG passes all four clean Probe 29 scenarios with `typed-resources=pass`. The 258,048-byte normal image `a8a14581eab4c1a6d67aba7af0cec1baa956574a410a4cd0de1121e1f843ee67` exits 0; the same-sized invalid-opcode image `35ee08e97aff4f6a2c0018c962960d5c7ee8af58fe6d5b36565613a99292ad0f` and general-protection image `92ae33986ab53245f57dc9263179e6dcd2c66cf79b634dcedaee51e93f915ca7` exit 3; and the 258,560-byte contained-user-fault image `35a3dece4e64463bc9df7ef73c83ec5f5fff3b0daedd7176f77f1c2ef5525484` exits 0.

Exact implementation commit `3fd9ef7535d7536ed084144e4f697cda548bf35c` passes GitHub [Verify run 30745623111](https://github.com/eworker-inc/Windvale/actions/runs/30745623111). Windows and digest-pinned Debian 12 each pass all 67 Seed tests, all 25 OS tests, and the complete non-Fast verifier. Seed elapsed time is 229.159 seconds on Windows and 196.686 seconds on Linux. Both qualification logs emit the same 56 SHA-256 values in exact order. This qualifies Decision 0098 and Probe 29 as the latest cross-host OS baseline. It proves neither a dynamic namespace nor arbitrary counts, independent resource lifetimes, ownership transfer, page/root reuse, scheduling, JIT publication, Hyper-V, physical hardware, nor .NET retirement; QEMU execution remains Windows-only evidence.

## Qualified ABI-17 frame admission and Probe-30 root reuse

Qualified Decision 0099 advances the current target to `x86-64-wvb-baseline-v17` while retaining context version 7, service-table version 5, WVB 1.6, WVO 1.0, and ABI 16's 64-parameter convention. The hard frame envelope is now 2,048 16-byte cells. Exact compiler preflight passes the former `Compilerˉsourceˉwirˉcompileˉblock` local-count failure and reaches `WVN2004` in `Compilerˉbodyˉparseˉprimary`: the function requests at least 2,049 combined local/value slots. The improved diagnostic records the exact function, requirement, and limit. This is progress toward native compiler execution, not completion of it.

Qualified Decision 0100 advances memory to `WVKMEM07`, protected processes to `WVPROC09`, resource records to `WVRES004`, and firmware to Probe 30. Generation-1 process reference `65538` and generation-2 reference `131074` may use the same physical root but cannot share logical identity. After the first terminal cleanup, an exact LIFO operation accepts only the 42-page allocator tail, restores cursor/free state from `63/0` to `21/42`, zeroes every byte, and immediately reallocates the identical root. Both resource records preserve grant history across the release; the second grant advances it to `2`, and stale generation-1 process/resource evidence is rejected.

The focused Windows suite passes all 25 OS tests. It covers zero/non-tail release rejection, full zeroing and same-address reallocation, both generation planners/grants/cleanups, stale-generation rejection, deterministic artifacts, and the complete retained malformed-input suite. Pinned Windows QEMU 11.0/Q35/TCG passes all four Probe-30 scenarios with `process-reuse=pass`: normal `5034c01a98f20344d96fa091fd9a55a303e72669d746a4b83df2900eed93992f` (261,120 bytes, exit 0), invalid opcode `bb57ebf7e50eb56bf3d42d91b2213ed5b262554416fdf76609142eccba44cc55` (261,120 bytes, exit 3), general protection `d56fe572fb7a7ff724f7b7c26aa5299a6c5cee4c203f009b63d651c1d3cd8fcc` (261,120 bytes, exit 3), and contained user fault `78dfa73a80a05021273cb44587f6b957d16d4cd4ebaec487f7b8a8f5427846ca` (261,632 bytes, exit 0).

Exact implementation commit `4a077ab9ebaf2108201927eef3095e87ef2ed907` passes GitHub [Verify run 30749304867](https://github.com/eworker-inc/Windvale/actions/runs/30749304867). Windows and digest-pinned Debian 12 each pass all 67 Seed tests, all 25 OS tests, and the complete non-Fast verifier. Seed elapsed time is 221.700 seconds on Windows and 201.079 seconds on Linux. Both qualification logs emit the same 56 SHA-256 values in exact order. This qualifies Decisions 0099 and 0100, ABI 17, and Probe 30 as the latest cross-host native/OS baselines. QEMU execution remains Windows-only evidence, and neither decision claims native compiler execution, a general allocator, scheduling, JIT publication, Hyper-V, physical hardware, or .NET retirement.

## Qualified Probe-31 exact-WVB portability proof

Qualified Decision 0101 advances admission to version 3, interpreter profile to 5, kernel memory to `WVKMEM08`, paging to `WVKPAG04`, protected processes to `WVPROC10`, and firmware to Probe 31 while retaining ABI 17/context 7/service table 5, WVA seam 8, admission bridge 2, retained bridge 10, `WVRES004`, and `WVBR002`.

The admitted resource is the exact 493-byte canonical WVB compiled from `Examples/Seed/Sum-Data.wv`, SHA-256 `6f3a272d37dd8893995c7f85c236414ed2864bf59de2f3775c08afd426013f8c`. Its immutable `[3,5,8,13]` data, internal `Add`, loop, locals, branches, and calls execute for exactly 203 guest instructions and return `29` through the reference runtime, the shared Windows/Linux native backend, and both protected Windvale OS client generations. The AOT interpreter consumes exactly 93,181 native instructions at dynamic depth 4; its largest frame is 1,883 slots under the unchanged ABI-17 limit of 2,048. Each client uses a 256-byte record arena inside its existing RW/NX data page and consumes exactly 240 bytes.

The focused Windows suite passes all 25 OS tests. Pinned Windows QEMU 11.0/Q35/TCG passes all four exact Probe-31 scenarios: normal `30acb028e44b6d12bc4d0e4d34232d86a43b83b40f070d3a48b7c56e505bc0bc` (531,456 bytes, exit 0), invalid opcode `dec5a39be132a3e6f140425547097b450a38a7b62e6ac3fa3d20f7d3c457587b` (531,456 bytes, exit 3), general protection `f0e9daacfa479945afec952692f69e3911b285e478f9ae8d12a3e14f0c091960` (531,456 bytes, exit 3), and contained user fault `795cb85aa599d2ead4e228bd0eb3da5ad28ecd8970955b294ab09f72c3f7ade7` (531,968 bytes, exit 0). Normal and contained-fault paths execute the canonical program in both generations, validate exact record-arena use, clean both typed aliases, reuse the root, exchange result `29`, and power off cleanly.

Exact implementation commit `f3eca7c8dab290e3916fbf33dcabc41d685a91bb` passes GitHub [Verify run 30753663882](https://github.com/eworker-inc/Windvale/actions/runs/30753663882). Windows and digest-pinned Debian 12 each pass all 67 Seed tests, all 25 OS tests, and the complete native CLI qualification gate. Seed elapsed time is 210.128 seconds on Windows and 210.185 seconds on Debian. Both logs emit the same 56 SHA-256 values in exact order, including the same canonical `Sum-Data.wvb` identity. This qualifies Probe 31 as the latest cross-host native/OS baseline and completes Phase 12's first same-module proof. QEMU execution remains Windows-only evidence.

This evidence does not qualify arbitrary WVB loading or verification, a dynamic namespace, independent resource lifetimes, general allocation, scheduling, JIT publication, broader traps, Hyper-V, physical hardware, or .NET retirement. A second real program, independently lived resource, or third runnable must choose the next boundary.

## Qualified Probe-32 second exact WVB

Qualified Decision 0103 advances admission to version 4, interpreter profile to 6, kernel memory to `WVKMEM09`, protected processes to `WVPROC11`, and firmware to Probe 32 while retaining ABI 17/context 7/service table 5, WVA seam 8, admission bridge 2, retained bridge 10, paging 4, `WVRES004`, and `WVBR002`.

The admitted resource is the exact 815-byte canonical WVB compiled from the existing cross-compiler fixture `Tests/Fixtures/Source-Wvb/Function-Only.wv`, SHA-256 `9ccfed0509e84bfc63979c6dc13170c14762efbdaa448b4c5894325f31aa7761`. Stage 0 and the Windvale-written compiler produce byte-identical bytes. Its four functions exercise `bool`, `u8`, `u32`, and `i32` constants and comparisons, local mutation, forward branches, and internal calls for exactly 199 guest instructions and result `6`. Profile 6 derives valid instruction boundaries before accepting branch targets.

The AOT interpreter consumes exactly 189,114 native instructions at dynamic depth 5; its largest frame is 1,900 slots under unchanged ABI 17's 2,048-slot cap. New verified-WVO call-graph preflight counts every reachable frame, return address, and the entry shim's saved `r15`, rejects recursion, and derives exactly 58,800 native stack bytes. Fifteen pages (61,440 bytes) are the smallest whole-page envelope; the former 13-page configuration page-faulted under pinned QEMU, and fourteen pages are arithmetically insufficient. The execution-scoped record arena is 1,024 bytes in the existing data page and consumes exactly 528 bytes for eleven 48-byte records; a measured 512-byte configuration exhausted after 480 bytes before the next record.

The local changed-files verifier completes a zero-warning Release build and all 67 Seed tests in 218.262 seconds. The focused Windows suite passes all 25 OS tests. Pinned Windows QEMU 11.0/Q35/TCG passes all four exact Probe-32 scenarios after the serialized process identity was corrected to `WVPROC11`: normal `a2c69181b55178b0e23c9b1012239a8fb1c8a53e2ffb089c8c59f45fa9dd0a6a` (714,752 bytes, exit 0), invalid opcode `04d801d25c0b5876fccb796d14b0c1ba14123f468200d5fb28728033867a5df5` (714,752 bytes, exit 3), general protection `b5abc555c11d3585b73e0d3e5391bbf33ca864933d8f0514265fce355273f799` (714,752 bytes, exit 3), and contained user fault `0233c10f323aaa3ee30dbe710b11c05f4d85a08fd88d6a52705c02f4b271347b` (715,264 bytes, exit 0).

Exact implementation commit `da938979ae9fe59e5f752bdb81359ded58a0e6ac` passes GitHub [Verify run 30758910402](https://github.com/eworker-inc/Windvale/actions/runs/30758910402). Windows and digest-pinned Debian 12 each pass all 67 Seed tests, all 25 OS tests, and the complete native CLI qualification gate. Seed elapsed time is 251.909 seconds on Windows and 200.120 seconds on Debian. This qualifies Decision 0103 and Probe 32 as the latest cross-host OS baseline. QEMU execution remains Windows-only evidence.

## Qualified ABI-18 typed block slots and WVKMEM10 compaction

Qualified Decision 0105 advances the backend to `x86-64-wvb-baseline-v18` while retaining WVB 1.6, WVO 1.0, execution context 7, service table 5, the ABI-16 64-parameter call convention, and the ABI-17 2,048-cell physical frame ceiling. Machine IR preserves globally canonical semantic value IDs and exact types, records a separate canonical physical map, and reuses exact-type ranges only across blocks whose operand stacks are independently verified empty. The selector reconstructs the map, rejects cross-block operands, and the fragment decoder retains descriptor alias/provenance checks across later cell redefinitions.

Exact compiler preflight clears the former slot-2,049 failure and reaches `WVN2003`: `Compilerˉcompileˉsourceˉwvb` uses the next unsupported native opcode, `Bytesˉfromˉu8`. For the retained Probe-32 interpreter, `Executeˉmain` keeps 1,236 semantic values but needs only 82 physical value cells and a 745-cell actual frame. The verified deepest call path consumes exactly 23,824 bytes, making six pages minimal. The deterministic interpreter WVO is 418,372 bytes, SHA-256 `c712a8cf7aa674b89e01e8fbc1632eeb6414fd1fde0bc4ead763e45c38037bb1`; the client uses 102 code pages, a 113-page reclaimable allocation, and a 134-page `WVKMEM10` arena. Probe 32's exact WVB, 199 guest instructions, result `6`, call depth, `WVPROC11` shape, paging, resource, and serial contracts remain unchanged.

Focused Windows verification passes all ten native compiler cases. The complete local Standard gate passes a zero-warning Release build, all 68 Seed tests including the 160.501-second golden contract, and all 25 OS tests; the complete Seed suite takes 216.323 seconds. Pinned Windows QEMU 11.0/Q35/TCG passes all four retained scenarios: normal `b8f0e656066b1e4f28edc4124eca6eea18130a0d6c0f4a9018e8ae817a0fa985` (531,456 bytes, exit 0), invalid opcode `0322ce3d3a9fecfa5c84809d8594f4f3ea643aaff2776f8d25668f1d723b9b54` (531,456 bytes, exit 3), general protection `1a0bd9f37c595d4170bd05fe83cc05dc344d2223674a01812f253ceb77893e40` (531,456 bytes, exit 3), and contained user fault `68319856b2913b3c857012d3fd38f147cf2a2307afacc9ffc8c8a33c005d0cf9` (531,968 bytes, exit 0).

Exact implementation commit `484c228c666e57bd6d3dc67d60c855f528cff3bf` passes GitHub [Verify run 30762156220](https://github.com/eworker-inc/Windvale/actions/runs/30762156220). Windows and digest-pinned Debian 12 each pass zero-warning Release builds, all 68 Seed tests, all 25 OS tests, and the complete native CLI qualification gate. Seed elapsed time is 237.931 seconds on Windows and 196.849 seconds on Debian; the golden contract takes 177.494 and 147.236 seconds respectively. This qualifies ABI 18, `WVKMEM10`, Decision 0105, and compacted Probe 32 as the latest cross-host native/OS baseline. QEMU execution remains Windows-only evidence.

## Qualified bounded straight-line WebAssembly backend

Decision 0106 extends target `wasm32-browser-v1-experimental` to a bounded one-function straight-line `i32` lowerer written in portable Windvale source. It accepts locals, constants, discard, checked addition/subtraction/multiplication/negation, and a final return while preserving execution ABI 1 result, `WVR3007`, and exact attempted-instruction evidence. The retained success artifact is 432 bytes with SHA-256 `15f2d58746ff2b0ae33a0de05e2781949c9d908fab46dd4072bfe3b2fa42b0bb`; the checked subtract, multiply, and negate overflow artifacts retain SHA-256 `757d26c2cf404cabcf5b78d2c998bc7ddc78ec4531e4571630ae2c1b5c8d7925`, `e924c7507a363a7b019935622abfbd4bf4ac8445cd37a0412130ce8e5c83d51a`, and `3f098efd63c68d8c62a4f6b373507e12c21808ff01120d165c9dc85a047e99e2`.

Exact implementation commit `a2285f5a0c09598ec701691bdbf0af9080e8cf0c` passes both host jobs in GitHub [Verify run 30762541741](https://github.com/eworker-inc/Windvale/actions/runs/30762541741). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 68 Seed tests, all 25 OS tests, and the complete native CLI qualification gate. The WebAssembly test recompiles the Windvale backend and fixtures, requires exact backend/input/output digests, independently parses the emitted Wasm, compares status/result/instruction tuples with the reference runtime, and covers malformed and bounded failure cases without output.

The run-level conclusion is `cancelled` only because a later `main` documentation push activated `cancel-in-progress` after both host jobs had finished: Debian completed successfully at 19:09:53 UTC and Windows at 19:11:07 UTC. Their exact job conclusions remain `success`; no host qualification step failed or was cancelled. Node.js 24.18.0 separately validates and executes all six ABI artifacts on Windows. This qualifies deterministic Windows/Linux construction and differential semantics, not a permanent WebAssembly target.

## Local disposable-worker integration

Decision 0107 integrates the qualified `.wv` backend into the browser playground. The reusable engine recompiles the embedded core/tool composition once, requires backend WVB SHA-256 `b47a6f5b89ac0d58dc6cafd6489b1fb12f1a0b9b161c09e8d2ca5a438993076a`, preserves selector rejection without output, and reproduces the retained 432-byte Wasm SHA-256 `15f2d58746ff2b0ae33a0de05e2781949c9d908fab46dd4072bfe3b2fa42b0bb` from 301-byte WVB SHA-256 `f7d360cf4d717d2cce93eda4f2c814960c39f1dd04bd0f74c44f55066730d655`.

On 2026-08-02, the focused Seed playground test passed the exact backend, unsupported-fallback, deterministic-repeat, WVB, and Wasm assertions. A Release Blazor build completed with zero warnings or errors. A Chromium-based in-app browser then ran the `WebAssembly worker` example twice; each fresh worker validated the import-free module and returned ABI `1`, status `0`, result `42`, and 30 instructions equal to the .NET reference path. The browser console contained no warnings or errors.

Exact implementation commit `f3b96052b964832fb5fc60ed3d076b42e8b78e9d` passes GitHub [Verify run 30763774038](https://github.com/eworker-inc/Windvale/actions/runs/30763774038). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 68 Seed tests, all 25 OS tests, and the complete native CLI gate. The cross-host Seed case recompiles the embedded `.wv` backend, reproduces the exact WVB/Wasm identities, verifies unsupported fallback without publication, and compares deterministic repeats. GitHub [Deploy homepage run 30763774003](https://github.com/eworker-inc/Windvale/actions/runs/30763774003) publishes the static playground from the same commit successfully. This qualifies the reusable engine and packaging integration; cross-browser qualification and worker containment for compilation, verification, lowering, and fallback execution remain open.

## Local standalone direct-artifact integration

Decision 0110 adds `/playground/wasm-demo/` as an ordinary HTML, CSS, and JavaScript route beside the editable playground. It embeds the exact qualified profile-3 bytes as deployment data, checks their 432-byte length and SHA-256 `15f2d58746ff2b0ae33a0de05e2781949c9d908fab46dd4072bfe3b2fa42b0bb`, and reuses Decision 0107's disposable worker. The route contains no Blazor bootstrap or .NET framework request; Stage 0 still owns artifact construction and qualification.

On 2026-08-02, `npm run verify:wasm-demo` reconstructs and validates the import-free artifact under Node.js and executes it twice with ABI `1`, status `0`, result `42`, and 30 instructions. A zero-warning Release build and publication retain the nested entry point and shared worker, and the focused Seed browser-playground engine case passes. A Chromium-based in-app browser runs the route to the same tuple with no warning or error; its seven observed static assets contain the page stylesheet, logo, analytics bootstrap, application module, artifact data, worker host, and worker, with no `_framework`, Blazor, or .NET asset. This local step does not by itself establish cross-browser or public-deployment qualification.

Exact implementation commit `e9480bca814318fb6fdcab0c4f3f1db699a01e6f` passes GitHub [Verify run 30767375189](https://github.com/eworker-inc/Windvale/actions/runs/30767375189), including the complete Windows and digest-pinned Debian 12 qualification jobs. GitHub [Deploy homepage run 30767375190](https://github.com/eworker-inc/Windvale/actions/runs/30767375190) passes the independent demo verifier and publishes successfully. The public `windvale.ca` route then reports the exact ABI tuple with no `_framework`, Blazor, or .NET asset and no browser warning. This qualifies the static packaging and deployed Chromium runtime boundary; cross-browser qualification remains open.

## Local bounded WebAssembly call-graph evidence

[Decision 0120](../Decisions/0120-Bounded-WebAssembly-Call-Graph.md) adds profile 6 for two through eight `i32`-returning functions, zero through two `i32` parameters, profile-3 scalar bodies, and direct calls only to a lower canonical ordinal. The order rule excludes recursion and cycles and statically caps dynamic depth at eight. The generated module uses real private WebAssembly functions behind the unchanged ABI-2 exports, with one shared instruction count, limit, and status across every callee.

The retained three-function source compiles to 399-byte WVB SHA-256 `502f5e9394248db4e21b49a3a98173917c2ff6f9a8252bef606a7a6c845d6482`. The deterministic 1,185-byte import-free Wasm SHA-256 is `d92667752762a992bdb626e34b83b78ee9c531f167b911737dfbf5f6443f3518`. The reference runtime, independent target decoder, and Node.js agree on `0/42/66`, one-instruction-short `3011/0/65`, and reset across a repeated successful run. A separate callee-overflow fixture produces 301-byte WVB SHA-256 `9e2b2a747287ff49ffce4d34f888b557a48064062e75ff5147bfc0224b54dca2` and 737-byte Wasm SHA-256 `4e936e5c4b077d1bce8719f5cc5c974961088f1171ed00158f9ac251f7652bd7`; overflow in `Calculate` propagates through `Main` as `3007/0/14`. Corrupt forward/self targets and arity mismatches fail as `Unsupportedˉcode` without output. The eight-function boundary is accepted and independently reconstructed, while nine functions fail without publication.

The standalone verifier reconstructs and executes the embedded artifact without a .NET asset. On 2026-08-02, a Chromium-based in-app browser loads the updated ordinary HTML page, runs both fresh workers, and reports the exact artifact identity, ABI `2`, both retained tuples, and zero .NET/Blazor requests. Focused Windows verification is green. Cross-host and cross-browser qualification remain pending.

## Local WebAssembly calls-with-control evidence

[Decision 0121](../Decisions/0121-WebAssembly-Calls-With-Structured-Control.md) composes the profile-6 call graph with profile-5 sequential structured control. The selector admits compiler-produced `bool` locals, independently reconstructs every region and call, and keeps one ABI-2 count, limit, and status across callers, callees, loop iterations, and conditional routes.

The true-route fixture compiles to 716-byte WVB SHA-256 `cf519c2d636d6e7b22b54afacf632bf5e514982e030d5c8b799a5585e7f39120` and deterministic 2,729-byte Wasm SHA-256 `3be50be3c2436638973eb68743f9fdd2e00df9816e50e498b432ff36468c3a77`. The false-route fixture produces 722-byte WVB SHA-256 `77e65ba692c8abc87dbac4dfeba174f3afc9191ac784b47a65becae8f0df2752` and 2,729-byte Wasm SHA-256 `35d75c30ef03dbb693a976cfaa31405ce90ecca4d393c5e93de8953fcf4658da`. The reference runtime, independent target decoder, and Node.js agree respectively on `0/42/196` plus `3011/0/195`, and `0/42/153` plus `3011/0/152`. An out-of-range branch target and a nested call/control program fail without publication. A fresh Chromium-based in-app browser reproduces `0/42/196` and `3011/0/195`, reports no warning or error, and observes no `.NET`, Blazor, or `_framework` asset. Cross-host and cross-browser qualification remain pending.

## Local WebAssembly linear-memory and UTF-8 evidence

[Decision 0123](../Decisions/0123-Versioned-WebAssembly-Linear-Memory-And-Utf8-Buffers.md) adds profile 8 and execution ABI 3 for the exact compiler-produced `bytes -> bytes` and `text -> text` identities. The emitted import-free modules expose one fixed, non-growable 129-page memory, a reserved first page, disjoint 4 MiB input and output regions, exact output kind and length, and the retained instruction counter. Text input is rejected by a generated strict UTF-8 state machine before WVB execution.

The bytes fixture compiles to 179-byte WVB SHA-256 `3d751ca734faed1832b4d33a9f0cfc605b695f3ae8156e3d431504798869c8d9` and deterministic 435-byte Wasm SHA-256 `b5f87bd47be7a0ce0bb6755de4ecea8bc311c9412ee28d6091092e7aa4c184f5`. The text fixture compiles to 178-byte WVB SHA-256 `c19463d24d65c1bc46dca48dcda8541491b53b7289483afe4508685f30e0fbda` and deterministic 791-byte Wasm SHA-256 `c3635b8df4ed9d471faad7e653e975662099c0a2336639586915ce50b768542d`.

The reference interpreter agrees on arbitrary bytes, Unicode text, and four charged WVB instructions. The independent target decoder reconstructs the exact WVB type and instruction shape plus Wasm types, fixed memory, ordered exports, globals, wrapper, meter, UTF-8 validator, and memory copy. Node.js round-trips empty, ordinary, Unicode, arbitrary-byte, and exact-4-MiB inputs; proves memory growth rejection; checks `3011/0/3` exhaustion and `3008/0/0` range failure; and compares curated malformed boundaries plus 20,000 deterministic generated byte strings with its independent fatal UTF-8 decoder. `npm run verify:wasm-demo` independently checks the embedded text artifact, memory contract, success, exhaustion, and malformed input without loading .NET. A Chromium-based in-app browser round-trips the default input and a 62-byte edited multilingual value with exact `0/4` success and `3011/3` exhaustion, zero .NET/Blazor requests, and no console warning or error. Cross-host and cross-browser qualification remain pending.

## Local WebAssembly runtime-value evidence

[Decision 0128](../Decisions/0128-Bounded-WebAssembly-Runtime-Values.md) adds profile 9 over unchanged execution ABI 3. The Windvale-authored selector now validates one straight-line `bytes -> bytes` function with `i32`, `bool`, `u8`, `u32`, and `bytes` locals; definite local initialization; a typed stack no deeper than four; byte lengths, slices, reads, widening, little-endian builders, concatenation, and one final return. Internal packed descriptors and a checked monotonic output arena remain private to generated code.

The representative fixture compiles to 914-byte WVB SHA-256 `6436f97c0e9abf131cc3a503c4449104706aa66eb0292a282a978fb7a5c5e100` and deterministic 4,878-byte Wasm SHA-256 `7bd5d2b0bc256503cd07dc300e528da38f8a09bcfec4c2b1007c1994db1b88f4`. The reference runtime and Node.js agree on its 19 output bytes and 155 charged instructions. Budget 154 returns `3011/154` with no output, while empty input reaches range status `3008` after 26 charged operations.

The focused 718-byte concatenation artifact SHA-256 `94533e9d01bdfcc606a3225ac28c774ecadd3cc0e0eccb02a7dba4f3fdb4ccb2` succeeds with an exact 4 MiB output and returns `3015/7` one input byte past the per-value boundary. The 797-byte u16 artifact SHA-256 `f312812fedae4c8dd45ffcb022301c1e85d7bdad4c71906a771cfc95333cde41` proves success, `3016/10` narrowing rejection, and `3008/7` range failure. The 921-byte arena artifact SHA-256 `0e37802a606ee67abd467ddc5da84f0d18807bb86b8bf497c4bdf0a41fa5a089` distinguishes aggregate `3018/14` from value-size `3015/14` at the input-capacity boundary.

The independent C# decoder verifies the source profile and completely consumes the emitted Wasm section, memory, global, ordered-export, local-type, opcode, meter, output-publication, and bulk-memory evidence. A not-yet-initialized local load is rejected without output. Node.js rebuilds and executes all four new artifacts, checks fixed-memory non-growth and invalid lengths before metering, and retains deterministic exact hashes. The focused Seed WebAssembly test and the 21-artifact WebAssembly verifier pass locally on Windows. Cross-host and browser execution evidence for profile 9 remain pending.

## Local Windvale-native WebAssembly WVB-envelope evidence

[Decision 0131](../Decisions/0131-Windvale-Native-WebAssembly-Wvb-Envelope-Verifier.md) adds profile 10 over unchanged execution ABI 3. The runtime-value selector admits checked unsigned addition and subtraction, unsigned comparisons, terminator-aligned jumps and branches, early bytes returns, and compiler-produced nested control. It lowers each validated WVB basic block through one private program-counter local and a single Wasm dispatch loop while preserving exact dynamic WVB instruction charging.

The first measured consumer is `Wvb-Envelope-Verify-Main.wv`. It compiles to 2,837-byte WVB SHA-256 `1362b2707a4ff442a1458e3f821e01108bb948858db21e022bfee05869c2fb86` and deterministic 14,902-byte import-free Wasm SHA-256 `f493777450b720ef786b60502528819969ad9e0322aa55a9c0259f6de20850fc`. The reference runtime and Node.js return `[1]` for its own canonical WVB after exactly 2,206 instructions; budget 2,205 returns `3011/2205` with empty output.

The companion checked-`u32` fixture has 447-byte WVB SHA-256 `d6ba02dfe12efdcb7c2f8ed6664551a776e79e3ff2c30134dc7e3642ee7ce743` and deterministic 1,893-byte Wasm SHA-256 `f645c0ff095eb06c825fea056659545cc258d857da55fc9dfd1a928812373f61`. It returns little-endian `42` after 57 instructions, `WVR3007` for addition overflow after 37 and subtraction underflow after 47, and `WVR3008` for a truncated read after seven.

Bad magic, an 11-byte truncated header, a hostile first-section length, and one trailing byte return `[0]` after 112, 92, 460, and 2,201 instructions. The independent C# decoder reconstructs the source opcode and target profile, terminator alignment, Wasm local layout, dispatch loop, conditional routes, branch depths, program-counter traffic, exact static meter count, output publication, fixed memory, ordered exports, and bulk-memory use. A branch target changed to point inside its own operand is rejected before output. The focused Seed WebAssembly test and the 23-artifact-plus-input Node.js verifier pass locally on Windows. Exact descendant `ea1aa89` passes Windows and Debian qualification including this portable envelope-verifier path; browser execution evidence for profile 10 remains pending.

## Local Windvale-native WebAssembly WVB-structural evidence

[Decision 0134](../Decisions/0134-Windvale-Native-WebAssembly-Wvb-Structural-Verifier.md) adds profile 11 over unchanged execution ABI 3. The single-runtime-function ceiling is now 2,047 nonparameter locals, 32,768 code bytes, and 100,000 decoded instructions; generated Wasm is bounded to 524,288 bytes. A one-pass byte mask makes decoded instruction boundaries constant-time control-target evidence, and the encoder accumulates one basic block before appending it to the complete function. The preexisting profile artifacts retain exact bytes.

`Wvb-Structural-Verify-Main.wv` compiles to 19,755-byte WVB SHA-256 `72da44ba1292ed3ef4ac62c239dd937862636229a7d60302305a7dd19ac27376`. Its function has 1,096 parameter-plus-local types, 4,062 instructions, and maximum stack two. The selector emits deterministic 113,385-byte import-free Wasm SHA-256 `46fe579fb7082dd4b0dd981e09f6b953127e52c9c6993d7885ca130725762677`. The reference runtime and Node.js return `[1]` for the verifier's own WVB after exactly 1,446,276 instructions; budget 1,446,275 returns `3011/1446275` with empty output.

The verifier completely consumes bounded module, capabilities, data, functions, code, exports, and types payloads. The 1,651-byte data/text, 1,781-byte nominal-types, and 849-byte hosted-capabilities fixtures return `[1]` after 103,696, 94,466, and 28,803 instructions. Invalid module profile, capability count 33, data count 4,097, function count 4,097, unknown code opcode, invalid export target, and truncated nonempty Types payload each return `[0]`.

The independent C# decoder completely consumes the large generated Wasm local and opcode stream and checks its exact meter, dispatch, output-publication, fixed-memory, and export evidence. The focused Seed WebAssembly test and the 24-artifact-plus-three-positive-input Node.js verifier pass locally on Windows. This is structural evidence only: UTF-8/name grammar, canonical ordering and identity, operand indices, type/local flow, control joins and reachability, declared stack agreement, and authorization remain for the semantic verifier. Cross-host and browser execution evidence for profile 11 remain pending.

## Local descriptor-bearing WebAssembly call evidence

[Decision 0139](../Decisions/0139-Descriptor-Bearing-WebAssembly-Call-Graph.md) adds profile 12 over unchanged execution ABI 3. The selector admits two through eight acyclic `bytes -> bytes` functions using the profile-11 runtime and terminator-aligned control operations. Each private Wasm function has target type `(i64) -> i64`; the existing packed descriptor remains private generated-code policy. Shared mutable globals carry status, instruction count, instruction limit, and the monotonic arena across every caller and callee while the exact ten-export public ABI remains unchanged.

`Runtime-Calls-Main.wv` compiles to 764-byte WVB SHA-256 `a44c8bdbf9983a7929a769d5ca2e0b60323d72cf96b04e31450d9757bb15729a`. Its three functions exercise nested calls, a call within conditional control, slicing, concatenation, and empty-input byte construction. The selector emits deterministic 4,086-byte import-free Wasm SHA-256 `5ee04d5b3b33399dce61709135709f0d0ebb7d6374e14759d83986859806eadd`. The reference runtime and Node.js both return `[9, 9, 8]` from `[9, 8]` in exactly 127 instructions; budget 126 returns `3011/126` with no output. Empty input returns `[0, 0]` in 63 instructions, and a repeated success proves wrapper and arena reset.

The independent C# decoder checks decreasing source call ordinals and consumes both Wasm types, all four function declarations, fixed memory, eleven globals, ordered exports, wrapper locals, and all private local layouts. A call operand changed to target its own caller is rejected without output. The focused Seed WebAssembly test passes, and the 25-artifact-plus-three-positive-input Node.js verifier rebuilds, validates, instantiates, and executes every output locally on Windows. The changed-scope Windows verifier also completes a zero-warning Release build and passes all 77 Seed tests, including the golden compiler contract; it is development feedback rather than conformance or cross-host qualification. Cross-host and browser execution evidence for profile 12 remain pending.

## Local modular WebAssembly WVB canonical metadata/reference evidence

[Decision 0144](../Decisions/0144-Modular-WebAssembly-Wvb-Canonical-Metadata-And-References.md) retains profile 12 and execution ABI 3 while adding the first eight-function WVB semantic phase. `Wvb-Semantic-Verify-Main.wv` separates identifier, structural, module/capability, data, function, code/export, type, and orchestration ownership. It validates canonical names and ordering, exact capability catalog signatures, strict text UTF-8, nominal declaration identities, local/function/capability/data/type operands, exact branch boundaries, export-to-function identity, record-to-enum field identity, and enum member/value uniqueness.

The source compiles to a 70,016-byte WVB with SHA-256 `09a665dcfbf8fe70d9b830be8376b1c1353a5ef09ff10de7b0183e535036fa64`. Its eight functions occupy exactly 65,536 aggregate code bytes. The hosted Windvale backend completes deterministic lowering in 129,151,253 instructions and emits 440,093-byte import-free Wasm SHA-256 `a2ef01881a4d381154a0e3feb0cb74cb0cdb3a53631cae1206d2fc03bcabe2fa`.

The reference runtime and Node.js return `[1]` for the data/text, nominal-type, and hosted-capability modules after exactly 1,122,085, 912,951, and 113,457 instructions. One fewer instruction on the data/text case produces `WVR3011` with empty output. Both engines return empty success output for thirteen structurally valid corruptions: module identifier, portable capability declaration, capability signature, data ordering, text UTF-8, function ordering, nominal function shape, text-constant data kind, branch boundary, export identity, cross-kind type identity, enum backing value, and record-to-enum field target. The Stage 0 oracle independently rejects every same byte sequence.

The independent C# decoder consumes the wrapper and eight private Wasm functions, both function types, fixed memory, eleven globals, exact ten-export ABI, and every private local layout. The focused Seed WebAssembly case and complete 26-artifact-plus-input Node.js gate pass locally on Windows. Changed-scope verification on base `dcc694a` produces a zero-warning Release build and passes all 80 selected Seed tests, including the golden compiler contract; it remains development feedback rather than conformance evidence. After rebase onto `c5e72bb`, the focused WebAssembly test passes again against the updated reference runtime. Allocation-free exact branch-boundary rescans are quadratic on branch-heavy input: the verifier's own unusually large WVB exceeds 500,000,000 verification instructions and is retained as optimization pressure rather than acceptance evidence.

This is not the complete mandatory semantic verifier. Typed operand-stack flow, definite local initialization, call value flow, record-field receiver identity, joins, reachability, maximum-stack agreement, authorization, cross-host construction, and browser execution evidence remain pending.

[Decision 0146](../Decisions/0146-Expanded-Descriptor-Bearing-WebAssembly-Call-Graph.md) adds profile 13 over unchanged execution ABI 3 because the canonical semantic verifier reached exactly all eight functions and 65,536 aggregate code bytes in profile 12. The per-function local, code, instruction, stack, signature, control, and decreasing-call-ordinal rules remain unchanged. The bounded aggregate becomes sixteen functions, 131,072 code bytes, 400,000 decoded instructions, and 1,048,576 generated Wasm bytes; profile-12-sized inputs retain the 524,288-byte output ceiling.

A test-derived ninth semantic phase produces a 70,086-byte WVB with 65,567 aggregate code bytes and SHA-256 `1b88f7cdbaf6d00fcde3ac6dfa09f38ff721e90edf283018378e5004a91340b4`. It lowers in 130,307,230 instructions to 440,333 import-free Wasm bytes with SHA-256 `8601f90946c0738ad6ce20c236e219f66e4ee4b2b143242a430c203918b1259a`. The independent C# decoder consumes the wrapper plus nine private functions; a seventeen-function graph is rejected without output. Node.js agrees with the reference runtime on the data/text, nominal-type, and hosted-capability inputs at 1,122,092, 912,958, and 113,464 instructions and on one-instruction-short exhaustion.

The original 440,093-byte profile-12 semantic artifact remains byte-identical. The updated standalone selector core WVB is `14be02c61ddfa0b8bcfab5804de8edf7cefa4dffe8c8b0d24762aee63b728222`; the composed backend WVB is `fddf873732a2979c4a5fac25c02d10f91dcc71d7920b77293aed62354bb3e0f7`. Profile 13 supplies capacity only: executable type/control-flow verification and authorization remain pending.

## Qualified ABI-19 one-byte construction

Qualified Decision 0108 advances the implementation to `x86-64-wvb-baseline-v19` while retaining WVB 1.6, WVO 1.0, execution context 7, service table 5, the 64-parameter convention, ABI 18's canonical typed block-slot map, and the 2,048-cell physical frame ceiling. Verified `Bytesˉfromˉu8` now lowers from `U8` to a one-byte execution-arena-backed descriptor. The independent decoder reconstructs its checked allocation, length-one descriptor, scalar source, exact byte store, runtime failure edge, and distinct source/result cells. The retained four-byte encoder now rejects the same malicious scalar/result alias.

The focused borrowed-bytes test executes `0u8` and `255u8` through the reference interpreter, W^X JIT, and linked WVO/AOT to the same result and rejects changed allocation/store widths plus both encoder aliases. Exact compiler preflight clears `Bytesˉfromˉu8` and now reaches `WVN2003` for `Bytesˉfromˉu16ˉlittle` in `Compilerˉcompileˉsourceˉwvb`.

The Windows Development gate passes a zero-warning Release build, all 67 regular Seed tests, and all 25 OS tests. The complete local Standard gate passes all 68 Seed tests including the 171.677-second golden contract and all 25 OS tests; the Seed suite takes 228.705 seconds and the complete command takes 243.5 seconds. Pinned Windows QEMU 11.0/Q35/TCG passes all four retained Probe-32 scenarios with the exact qualified ABI-18 images: normal `b8f0e656066b1e4f28edc4124eca6eea18130a0d6c0f4a9018e8ae817a0fa985` (531,456 bytes, exit 0), invalid opcode `0322ce3d3a9fecfa5c84809d8594f4f3ea643aaff2776f8d25668f1d723b9b54` (531,456 bytes, exit 3), general protection `1a0bd9f37c595d4170bd05fe83cc05dc344d2223674a01812f253ceb77893e40` (531,456 bytes, exit 3), and contained user fault `68319856b2913b3c857012d3fd38f147cf2a2307afacc9ffc8c8a33c005d0cf9` (531,968 bytes, exit 0).

Exact implementation commit `a35c3484c8d18498cc611354dc04554c513fc15c` passes GitHub [Verify run 30764320109](https://github.com/eworker-inc/Windvale/actions/runs/30764320109). Windows and digest-pinned Debian 12 each pass a zero-warning Release build, all 68 Seed tests, all 25 OS tests, and the complete native CLI qualification gate. Seed elapsed time is 242.730 seconds on Windows and 203.702 seconds on Debian; the golden contract takes 182.201 and 151.697 seconds respectively. This qualifies ABI 19 and Decision 0108 as the latest shared native/OS baseline while retaining Probe 32's exact firmware identities. QEMU execution remains Windows-only evidence.

## Qualified bounded exact-compiler record arena

[Decision 0112](../Decisions/0112-Bounded-Exact-Compiler-Record-Arena.md) measures the boundary exposed by Decision 0111 before revising it. The exact compiler consumes 1,480,096 bytes of immutable-record storage and 4,340,388 bytes of dynamic text/byte storage while compiling `Tests/Fixtures/Source-Wvb/Function-Only.wv`. The implemented host record-arena capacity is 2 MiB, leaving 617,056 bytes of measured headroom; the existing text capacity remains 16 MiB.

Under that ordinary host capacity, the 4,556,121-byte ABI-20 fragment passes independent decoding and W^X publication, returns zero, emits the exact success report, emits no diagnostics, and publishes an 815-byte WVB byte-identical to the independent Stage 0 compiler result. A separate workload still exceeds the revised arena and reaches `WVR3017`. No source, WVB, WVO, context layout, service table, ABI, generated machine byte, OS arena, or lifetime rule changes: immutable records remain checked, monotonic, and execution-scoped.

Focused Windows tests pass for exact compiler execution and the retained interpreter/JIT/AOT record boundary. Windows Development passes a zero-warning Release build, all 67 regular Seed tests, and all 25 OS tests in 74.1 seconds wall time. Windows Standard passes all 68 Seed tests and all 25 OS tests in 241.2 seconds wall time; Seed in-process time is 227.737 seconds, including 169.396 seconds for the golden compiler-reproduction contract.

Exact implementation commit `bbec1aee901d6471b3ff0e56e65f656a4cd53ed1` passes GitHub [Verify run 30769250223](https://github.com/eworker-inc/Windvale/actions/runs/30769250223). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 68 Seed tests, all 25 OS tests, retained planner reproduction, and the complete native CLI gate. The exact compiler case takes 1.678 seconds on Windows and 1.414 seconds on Linux. Windows Seed takes 227.437 seconds with a 169.266-second golden contract; Linux Seed takes 192.121 seconds with a 141.888-second golden contract. The complete jobs finish in 8m46s and 7m14s respectively. QEMU was not rerun because ABI 20, every generated machine byte, and all OS source/artifact inputs remain unchanged.

## Qualified exact-compiler record-lifetime evidence

[Decision 0115](../Decisions/0115-Exact-Compiler-Record-Lifetime-Pressure.md) applies the exact 12-module, 677,073-byte bootstrap inventory to the qualified native compiler. The ordinary 2 MiB record arena reaches `WVR3017` before stdout, diagnostics, or output publication. Diagnostic-only 64 MiB and 256 MiB capacities also exhaust, the latter after consuming 268,435,440 bytes. The capacity override used for measurement is not retained.

A new opt-in reference-runtime profile completed the successful 6,700,562,174-instruction Stage 1 execution and produced the exact 599,868-byte Stage 2 compiler with retained SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066`. The 40 busiest functions construct at least 77,821,091 declared record fields, corresponding to at least 1,245,137,456 bytes under ABI 20's 16-byte field cells. `Compilerˉsourceˉtokenˉmake` alone constructs 46,621,918 fields across 3,330,137 tokens, equivalent to 745,950,688 current native bytes.

The exact compiler declares 49 records and 22 enums; its widest record has 34 fields and no record contains another record. The conformance suite pins those facts and the ordinary-capacity failure. `--report-function-record-fields` reports semantic field constructions per function without changing default runtime allocation or output, so later ABIs may change storage while preserving comparable workload evidence.

Exact integration commit `05e5ef1069eff5283f4f1c46923f40905e04c5db` passes GitHub [Verify run 30771491421](https://github.com/eworker-inc/Windvale/actions/runs/30771491421). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 70 Seed tests, all 25 OS tests, and the complete native CLI gate. The bounded full-bootstrap case takes 897 ms on Windows and 678 ms on Linux. Windows Seed takes 238.394 seconds with a 172.632-second golden contract; Linux Seed takes 199.414 seconds with a 147.241-second golden contract. The complete jobs finish in 8m48s and 7m29s respectively. QEMU was not rerun because ABI 20, every generated machine byte, and all OS source/artifact inputs remain unchanged.

## Qualified nominal native record-storage plan

[Decision 0117](../Decisions/0117-Nominal-Native-Record-Storage-Plan.md) retains the exact nominal declaration index for every native function return, parameter/local binding, and semantic value. The selector validates those shapes independently across constants, local flow, record construction/fields, enum comparisons/names, calls, void calls, and returns. An independent test reconstructs the same identities from verified WVB. The additional machine-IR metadata does not change ABI 20, serialized formats, or selected bytes: the exact compiler remains 4,556,121 bytes with SHA-256 `8e74707df03a535e3ef68cfcfc8da6fa68fda29ccf4344e272fc50c8a5845bab`.

The deterministic Stage 0 storage planner analyzes control-flow liveness for persistent record locals and definition-to-last-use liveness for block-local record results. The exact compiler contains 308 record parameters, no assigned record parameter, 8,097 record locals, and 14,948 record semantic values mapped to 4,484 existing physical record slots. Across all 328 functions, 137,512 declared local-field cells compact to 9,291 persistent cells and the 88,669-cell coarse physical-slot bound compacts to 7,463 peak-live result cells. There are 206 record-returning functions with 3,222 total return-field widths. The 49 records remain non-nested and no wider than 34 fields.

`Compilerˉsourceˉwirˉcompileˉblock` is the largest projected frame. Its 1,178 existing cells plus 196 persistent record cells, 114 peak-live record-result cells, and one hidden return-destination cell total 1,489 cells (23,824 bytes), below the unchanged 2,048-cell native frame ceiling. This is planning evidence rather than an ABI-21 implementation: ABI 20 still allocates record fields monotonically and the exact full bootstrap still reaches `WVR3017`.

Focused Windows verification passes the nominal interpreter/JIT/WVO-AOT agreement case, the exact native file-output/compiler-plan case, and the retained full-bootstrap `WVR3017` boundary. Windows Development completes a zero-warning Release build, all 69 regular Seed tests, and all 25 bounded OS tests in 79.7 seconds wall time; Seed in-process time is 61.331 seconds.

Exact implementation commit `57416d0f93c803ef4218b6c0206798f2fd4f362c` passes GitHub [Verify run 30773327094](https://github.com/eworker-inc/Windvale/actions/runs/30773327094). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 70 Seed tests, all 25 OS tests, and the complete native CLI gate. The focused nominal/storage case takes 52 ms on Windows and 32 ms on Linux; the exact native compiler-plan case takes 1.933 and 2.014 seconds; the retained full-bootstrap boundary takes 737 and 669 ms. Windows Seed takes 225.610 seconds with a 167.252-second golden contract; Linux Seed takes 199.654 seconds with a 146.882-second golden contract. The complete host jobs finish in 8m32s and 7m25s. QEMU is not rerun because ABI 20, every generated machine byte, and all OS source/artifact inputs remain unchanged.

## Implemented deterministic native record-storage offsets

[Decision 0118](../Decisions/0118-Deterministic-Native-Record-Storage-Offsets.md) turns Decision 0117's region totals into complete immutable maps. Each record local and semantic value now has either an absolute projected-frame cell offset or `-1` for no owned backing. The canonical layout retains ABI-20 handles, then persistent local fields, block-reused result scratch, and an optional caller-result pointer. One width-first, identity-tied first-fit allocator serves both independently derived interference graphs.

Independent test code reconstructs CFG local liveness, definition-to-last-use result liveness, result/operand coexistence, region bounds, exact region ends, and pairwise separation. Planning a separately lowered copy produces identical arrays. The exact compiler publishes map digest `aff287fba46a840e454e4cc7bf4751d3152474caf09331a526f3730ba280816e`; static scratch allocation equals the 7,463-cell aggregate live lower bound without fragmentation. The largest function remains exactly 1,489 cells under the unchanged 2,048-cell ceiling.

The focused nominal and exact compiler-plan tests pass on Windows with zero build warnings. Windows Development completes a zero-warning Release build, all 69 regular Seed tests, and all 25 bounded OS tests in 81.2 seconds wall time; Seed in-process time is 65.078 seconds and the exact compiler-plan case takes 2.135 seconds. The selected compiler remains 4,556,121 bytes with SHA-256 `8e74707df03a535e3ef68cfcfc8da6fa68fda29ccf4344e272fc50c8a5845bab`. ABI 20 still executes through the monotonic arena and the retained full-bootstrap boundary remains `WVR3017`.

Exact implementation commit `060cf481d9783170cde4cf2cdd7ddab3cee1028e` passes GitHub [Verify run 30774669075](https://github.com/eworker-inc/Windvale/actions/runs/30774669075). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 70 Seed tests, all 25 OS tests, and the complete native CLI gate. The nominal record case takes 93 ms on Windows and 33 ms on Linux; the exact compiler-plan case takes 2.637 and 2.174 seconds; the retained full-bootstrap boundary takes 1.037 seconds and 623 ms. Windows Seed takes 252.041 seconds with a 186.165-second golden contract; Linux Seed takes 176.775 seconds with a 128.564-second golden contract. The complete host jobs finish in 9m15s and 6m31s. QEMU is not rerun because ABI 20, every generated machine byte, and all OS source/artifact inputs remain unchanged.

## Local Windows console application evidence

[Decision 0119](../Decisions/0119-First-Windows-Console-Application.md) adds target `windows-x64-console-v1` without changing WVB, WVO, the flat linker, ABI 20, context 7, or selected native bytes. The Stage 0 CLI compiles `Examples/Seed/Sum-Data.wv`, verifies its canonical WVB, selects the shared native fragment, reproduces it through WVO and base-zero flat linking, and packages an import-free PE32+ with an exact 67-byte entry stub and fixed loader-zeroed arenas.

The result is 5,120 bytes with SHA-256 `c6c4568f0a47e36ce8fdb145f4c3de3ce9a28bb2fb1935add75d44e48a2ac805`. Windows loads the file directly and reports process result `29`. The focused test also executes the nominal-record and dynamic-byte fixtures to `42`, proving the 2 MiB record- and 16 MiB dynamic-value arena pointers, while checked overflow becomes process result `1`. Hosted `Hello-Windvale.wv`, descriptor `Main`, and a changed native fragment publish no PE.

The independent verifier recovers the exact native bytes and `Main` offset and rejects truncated, oversized, trailing, noncanonical, and targeted corruptions across DOS, COFF, optional-header, section, startup, context, padding, and relocation boundaries. Bounded random hostile inputs remain contained by the target exception boundary.

The complete local Windows qualification command finishes in 549.4 seconds. It produces a zero-warning Release build, passes all 71 Seed tests in 281.905 seconds including the 206.219-second golden compiler contract, passes all 25 OS tests, publishes the Linux-x64 framework-dependent CLI, and completes the native CLI gate including exact application hash and direct Windows execution. The prior change-aware gate independently passes the same 71 Seed tests. Debian construction, report comparison, and GitHub qualification remain pending, so this evidence does not yet claim cross-host qualification.

## Local Linux console application construction evidence

[Decision 0122](../Decisions/0122-First-Linux-Console-Application.md) adds target `linux-x64-console-v1` over the same verified fragment preparation, WVO production, and base-zero flat-link reproduction as the Windows target. The adapter emits a sectionless static-PIE ELF64 with separate read-only, executable, and writable loads, a non-executable stack declaration, an exact Windvale version note, and direct `mmap`/`exit` startup syscalls. Its writable extent carries the same ABI-20 context and fixed 2 MiB record plus 16 MiB dynamic-value arenas.

On Windows, canonical `Sum-Data.wv` deterministically produces 8,304 bytes with SHA-256 `8e4eede684330e1797a7ff4d512ffe52684f1257cdbd97aa3d5ea06a13bea88c`; an external file classifier recognizes it as a statically linked, sectionless x86-64 ELF shared object. Independent PE and ELF verification recovers identical native bytes and the same `Main` offset. The focused Linux-container test passes with zero build warnings and covers deterministic repetition, all writer rejection classes, all eight ELF verifier diagnostic classes, targeted field/startup/context/padding mutations, and bounded random hostile inputs.

Windows Development completes a zero-warning Release build, all 73 regular Seed tests, and all 25 bounded OS tests in 79.9 seconds wall time; Seed in-process time is 61.964 seconds. The paired Windows application test passes in 305 ms after the shared preparation refactor, and Linux construction, verification, malformed-input, and PE/ELF parity pass in 14 ms.

The retained Linux-only branches directly execute sum, nominal-record, dynamic-byte, and checked-overflow fixtures, and the Linux qualification script requires exact bytes, mode `0755`, and result `29`. This checkout has no Linux execution environment, so those direct-process assertions and the dual-host Qualification gate remain pending; this section records construction and verification evidence only.

## Local paired console-container hardening evidence

The paired startup contracts now preserve successful results `0` through `255` and map every other successful `i32` or native failure to process result `1`. Canonical `Sum-Data.wv` remains 5,120 PE bytes with updated SHA-256 `486b758bac7d62456114f31a41754a3b4eb061db150cae12fbcb2b3ba17b3bdf`; the ELF remains 8,304 bytes with updated SHA-256 `cb7ece2e53b3d432406d9064ac8343901de261d22f34bcd5f3607da5ce71a9f6`. The PE directly executes shared boundary results `-1`, `0`, `1`, `255`, `256`, and `2,147,483,647` as exact process results `1`, `0`, `1`, `255`, `1`, and `1`. The equivalent Linux branches are retained for Debian execution.

Executable CLI output is written and flushed under a unique sibling path, prepared with exact mode `0755` on Linux, and then published through one replacement. An injected failure after the temporary write preserves the original requested output and leaves no temporary sibling; the successful replacement and Windows executable mode-independent path also pass.

The zero-warning Development build executes 74 regular tests. Seventy-three pass, including Linux construction/verification in 15 ms and atomic publication in 200 ms; the sole failure is a stale PE padding-corruption coordinate newly occupied by the seven-byte result check. Correcting only that test to derive the first padding byte from the declared startup length is followed by a zero-warning focused rerun: the complete Windows container case and all direct result boundaries pass in 1.058 seconds. The failed Seed phase prevents that Development invocation from reaching the unchanged OS suite, and it is not repeated solely to restate unaffected evidence. Fresh dual-host Qualification remains pending.

## Local paired WVA console-startup evidence

[Decision 0124](../Decisions/0124-Paired-Wva-Console-Startup-Templates.md) expresses both complete process-entry stubs in ordinary WVA instructions while retaining the independent C# writer and untrusted-container verifier boundaries. The Windows object has a 98-byte code section and typed `relative-i32` fields at offsets 10, 17, 32, and 54; its WVO SHA-256 is `e27b79aa4f55554a89abddb08dc765b19f2b6cd484a1ef7ef3878990941fcce9`. The Linux object has a 158-byte code section and fields at 64, 74, 89, and 112; its WVO SHA-256 is `e592d5d63048587f775ae2802aac3c40c747a62865f38059c094e7d3f95ed028`. Each object exposes only its startup export plus the same four semantic imports for execution context, native Main, record arena, and text arena.

The existing console tests now assemble their corresponding source, validate the complete object and relocation shape, instantiate the four final addresses, and compare every startup byte with the actual container. This evidence is deliberately folded into the existing PE and ELF cases rather than repeating application construction in another test. A zero-warning focused Release run passes both cases in 1.178 seconds on Windows: PE direct execution and all six shared process-result boundaries pass in 1.161 seconds, while ELF construction, independent verification, typed-relocation agreement, malformed-input coverage, and PE/ELF native recovery parity pass in 14 milliseconds.

The WVA encodings move the PE native-image offset from 80 to 112 and the ELF offset from 144 to 160 without changing either aligned file length. Canonical `Sum-Data.wv` produces 5,120 PE bytes with SHA-256 `5947c00a81f4cf94651d42d619f3173a622448d042f4fa20e3042940d4a56c77` and 8,304 ELF bytes with SHA-256 `8af8b46c290965cfc4475d882ac2d5fbdb0ffe4c493a19883a19c2683a319ec4`.

Windows Development completes a zero-warning Release build, all 74 regular Seed tests, and all 25 bounded OS tests in 97.9 seconds wall time on the rebased candidate. Seed takes 76.360 seconds; the integrated Windows and Linux container cases take 799 and 24 milliseconds. The qualification-only golden contract and direct Linux execution are intentionally outside Development, so fresh dual-host Qualification remains pending.

## Implemented ABI-21 frame-owned direct records

[Decision 0133](../Decisions/0133-Frame-Owned-Direct-Native-Records.md) consumes the exact Decision 0118 maps in the single shared selector used by host execution, both version-1 console containers, and Windvale OS. Direct record fields occupy deterministic persistent or block-reused frame backing; handles carry backing pointers; internal calls pass those pointers; and returns copy into caller-owned destinations. Assigned parameters receive owned copies before ordinary operations. Record-valued fields remain rejected.

The independent fragment verifier decodes nominal tags, complete field copies, frame ranges, local ownership copies, borrowed record references, caller-owned destinations, direct-field returns, and record call-graph agreement. Caller and callee stack decoding tests the complete 32-byte borrowed-descriptor form before the shared 16-byte record-pointer prefix. Corrupt planned construction backing fails as `WVN3030`. A 131,072-construction loop returns `131071` with zero record-arena use and the nominal interpreter/JIT/WVO-AOT result remains `42`.

The exact 328-function compiler retains map digest `aff287fba46a840e454e4cc7bf4751d3152474caf09331a526f3730ba280816e` and the 1,489-cell maximum. Its ABI-21 fragment is 16,905,513 bytes with SHA-256 `29a8b354e185fad4b4d8967ee8e263ce68cb9939373d91fb1e7919be887c8569`. The native compiler emits the exact canonical 815-byte function-only module and existing success report with no diagnostics, consumes 4,340,388 dynamic text/byte bytes, and consumes zero record-arena bytes. The full 12-module bootstrap clears `WVR3017` and deterministically reaches the retained 16 MiB dynamic-value boundary as `WVR3018` before output.

The measured explicit-copy fragment requires a synchronized 32 MiB code ceiling below the retained 34 MiB final-image ceiling. The 7,189-byte Windvale publication core has SHA-256 `f2c315c4c52099b8682396358563eef2eb9dceecf1feb84ce5bef5f8465bdeba`; the regenerated 7,105-byte retained bridge has SHA-256 `b21e1136fc9087f530391127a1e1400e7248fa1831a51f00d86d467cf5133cb0`. Stage 0 and Windvale planners accept exactly 32 MiB and reject 32 MiB plus one.

The paired Windows and Linux console targets rebuild through the same ABI-21 preparation boundary rather than retaining ABI 20. ABI 21 leaves scalar native selection unchanged; the integrated WVA startup templates produce the canonical 5,120-byte PE / SHA-256 `5947c00a81f4cf94651d42d619f3173a622448d042f4fa20e3042940d4a56c77` and 8,304-byte ELF / SHA-256 `8af8b46c290965cfc4475d882ac2d5fbdb0ffe4c493a19883a19c2683a319ec4`. Nominal-record container execution consumes zero bytes from their retained compatibility record arena.

The shared OS rebuild also consumes the same projected frame plan rather than retaining ABI 20. Probe 32's `Executeˉmain` grows from 745 to 755 frame cells and its independently measured deepest call path from 23,824 to 24,240 bytes, still inside the minimal six-page stack. The interpreter WVO is 445,684 bytes / SHA-256 `3840f10bacf8b7b498f28646b947a53841baf00241cd21bc94423ab5a43e8e31`; the normal linked client is 445,085 bytes / `95a80e8998571dfb1cb8af7e2d14fd40804f648ce73e136e7aa00265934d7f30`. The client needs 109 RX pages and a 120-page root, so `WVKMEM11` supplies a 141-page arena. Its retained 1,024-byte context record arena ends at exactly zero bytes used instead of 528.

Immediately before the Decision 0131 WebAssembly and Decision 0132 console-verification rebases, the integrated native/console/OS candidate completed Windows Qualification in 518.6 seconds. It produced a zero-warning Release build, passed all 77 Seed tests in 262.591 seconds including the 179.808-second golden compiler contract, passed all 31 OS tests, and completed the native CLI gate. Decision 0131 changes no native, console, or OS implementation/artifact input. After its rebase, focused WebAssembly, console, native-record, and all 31 OS checks passed. Decision 0132 retains the exact console bytes and changes no native or OS input; after its rebase, all four console checks and the native-record check pass. All four pinned Windows QEMU 11.0/Q35/TCG scenarios pass with exact rebuilt identities: normal `d394eb13ae93b71450bb9a3fd42e3dee0aaa17c58c6c920f031eacaf9deb5e8d` (555,520 bytes, exit 0), invalid opcode `ad77085ef6a9dbbd56dd938c3492748f363fb99c5719341387a6e87a75624ade` (555,520 bytes, exit 3), general protection `3a651d555f2c334c53b67385ac1fb731a513deb058e742ba5cbc58a985970de2` (555,520 bytes, exit 3), and contained user fault `198e01840c888d344e40ef985c574e33c77cdafccf36cdb56f9f9218bf74e8ca` (556,032 bytes, exit 0).

Exact commit `ea1aa89ba204ead633f8340c61b2bacc716881fd` passes GitHub [Verify run 30783457203](https://github.com/eworker-inc/Windvale/actions/runs/30783457203). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 77 Seed tests, all 31 OS tests, the golden compiler contract, and the native CLI gate. Windows Seed takes 241.987 seconds with a 169.303-second golden contract; Linux Seed takes 216.976 seconds with a 150.484-second golden contract. Complete jobs finish in 8m58s and 7m44s. This qualifies ABI 21, its shared console/OS consumers, the portable WebAssembly envelope verifier, and the portable PE/ELF verifier as one committed cross-host baseline.

## Local exact-compiler dynamic-value profile

[Decision 0136](../Decisions/0136-Exact-Compiler-Dynamic-Value-Pressure.md) adds an opt-in reference-runtime and CLI profile for every allocation-bearing operation admitted by ABI 21. It reports constructed values and their flat result bytes by function and class without changing default runtime allocation, output, bytecode, or native execution.

The canonical 12-module Stage 1 run succeeds in exactly 6,700,562,174 instructions and reproduces the exact 599,868-byte Stage 2 compiler with SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066`. It constructs 1,852,773 profiled values representing 902,262,268 flat bytes. `bytes.concat` contributes 962,611 values and 899,106,127 bytes, approximately 99.65% of the total; the next largest class is `bytes.from_u32_little` at 3,020,804 bytes.

The leading row is `Compilerˉsourceˉwirˉmergeˉfunction`: five escaping concatenated payloads per successful merge accumulate 315,298,984 flat bytes across 1,640 values. `Compilerˉsourceˉwirˉemit` adds 265,306,656 bytes across 451,269 concatenations, and `Compilerˉcompileˉsourceˉwvb` adds 102,543,288 bytes across 671 concatenations. This rules out a fixed capacity increase and shows why callee-only reset is insufficient, while not yet claiming a peak-live-byte count or selecting ownership, chunking, or collection.

The focused profiler test passes after a zero-warning Release build in 0.521 seconds. The successful full-bootstrap profile completes locally on Windows in 426.148 seconds. Cross-host qualification is pending; no OS input changed, so QEMU is not rerun for this diagnostic-only slice.

## Local exact-compiler dynamic-value lifetime

[Decision 0141](../Decisions/0141-Exact-Compiler-Dynamic-Value-Lifetime.md) follows each conceptual flat backing through verified operand stacks, local frames, calls, returns, borrowed byte/text views, and direct records. It counts unique reachable backing storage and separately retains popped input plus new-output overlap at allocation operations. The profiler is opt-in and leaves default execution unchanged.

The canonical 12-module Stage 1 run again returns zero after exactly 6,700,562,174 instructions and reproduces the exact 599,868-byte Stage 2 compiler with SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066`. Decision 0136's 1,852,773 constructed values and 902,262,268 constructed bytes reduce to a 17-backing, 9,030,829-byte ideal live peak. The allocation-operation peak is identical and occurs at `Compilerˉsourceˉwirˉdirectory` / `bytes.concat`; retained backings and bytes are both zero after completion.

The peak leaves 7,746,387 bytes, approximately 7.39 MiB, inside the unchanged 16 MiB native arena before metadata and fragmentation. Two local Windows Release executions reproduce the exact result and artifact identity in 512.450 and 517.510 seconds. The bounded Foundation CLI fixture deterministically reports 8,388,672 constructed bytes, a 6,291,475-byte/five-backing peak, and zero retained roots. The focused dynamic tests pass after a zero-warning build; QEMU is not rerun because this diagnostic slice changes no OS input or artifact.

After linear integration of Decisions 0137 through 0139, change-aware Windows verification completes a zero-warning Release build and passes all 80 selected Seed tests in 314.943 suite seconds. The exact golden compiler contract takes 219.181 seconds and the warm lifetime case takes three milliseconds. The new WVB 1.7 and WebAssembly paths do not execute in the canonical WVB 1.6 lifetime profile, so the separate multi-billion-instruction measurement is not repeated after that additive integration.

Exact implementation commit `f6952a1b2cf18222de77b0260060dcff3a9ed4bc` passes GitHub [Verify run 30788097110](https://github.com/eworker-inc/Windvale/actions/runs/30788097110). The complete Windows and pinned-Debian verifier jobs both pass, including the deterministic bounded lifetime report. This cross-host qualifies the support and ordinary fixture; the multi-billion-instruction exact compiler profile remains Windows-local evidence.

## Local bounded exact-compiler allocator replay

[Decision 0143](../Decisions/0143-Bounded-First-Fit-Dynamic-Arena-Replay.md) replays the same typed lifetime trace through one fixed 16,777,216-byte arena. Each backing receives a 16-byte in-band header and the complete block is aligned to 16 bytes. Address-ordered first fit splits the low end of the first sufficient span, and release coalesces adjacent spans immediately. Allocating-operation inputs remain occupied until their result is placed. The opt-in diagnostic policy leaves ABI 21 and ordinary reference execution unchanged.

The canonical Stage 1 compiler again returns zero after exactly 6,700,562,174 instructions and reproduces the exact 599,868-byte Stage 2 compiler with SHA-256 `9673bf3331763181f443ec67b7a513bc66daa718969f7f6b0d197a4186071066`. All 1,852,773 allocations succeed and 1,852,722 reuse previously addressed storage. Peak payload is 9,030,829 bytes; headers and alignment raise peak charge by 387 bytes to 9,031,216 across 17 blocks. First-fit reaches address 10,700,368, leaving 6,076,848 bytes above its maximum extent. External fragmentation peaks at 7,324,224 bytes across at most 86 free spans. Completion retains zero blocks and charged bytes and restores one fully coalesced arena. The Windows Release replay takes 622.891 seconds.

The bounded Foundation CLI fixture performs 35 allocations, reuses 12, peaks at 6,291,600 charged bytes across five blocks, reaches address 8,389,040, and retains nothing. A focused 64-byte fixture proves split, adjacent coalescing, and reuse for a later 64-byte request. At 48 bytes it admits the first 32-byte block, rejects the next against a 16-byte largest span as `WVR3018`, and still recovers all storage; a 63-byte misaligned arena is rejected before execution. No WVB/WVO bytes, source semantics, native ABI, generated machine bytes, OS source, or guest artifact changes, so QEMU is not rerun.

Change-aware Windows verification completes a zero-warning Release build and passes all 81 selected Seed tests in 350.412 suite seconds. The golden compiler contract takes 246.518 seconds and the allocator/coalescing case takes four milliseconds. This is proportional local development evidence rather than dual-host qualification.

Exact implementation commit `c5e72bb12f6c7d1ec54033d299403a1128eb1bef` passes GitHub [Verify run 30790280704](https://github.com/eworker-inc/Windvale/actions/runs/30790280704). Windows and digest-pinned Debian 12 both pass the complete repository verifier, including the bounded allocator fixture. This qualifies the replay support and ordinary fixture across both hosts; the exact multi-billion-instruction replay remains Windows-local evidence.

## Local exact-compiler native descriptor ownership plan

[Decision 0147](../Decisions/0147-Native-Descriptor-Ownership-Plan.md) attaches a bounded version-1 ownership plan to native machine IR and verifies it through a separately implemented reconstruction before x86-64 selection. The action plan distinguishes static and hosted borrows, allocations, aliases, local and direct-record replacement, last-use release, call borrowing, caller result acceptance, callee transfer, and normal-return frame cleanup. Terminal native failure retains the ABI-21 whole-arena teardown policy.

The exact 328-function compiler produces 186,557 deterministic actions: 435 acquisitions, 172 static or hosted borrows, 34,772 retains, 144,983 releases, 3,546 call borrows, 1,660 accepted returns, and 989 transferred returns. Its carriers cover 293 descriptor parameters, 3,190 descriptor locals, 524 descriptor-bearing record-parameter fields, 9,287 descriptor-bearing record-local fields, 6,182 descriptor values, and 17,898 descriptor-bearing record-value fields. Source parameters are immutable, so assigned parameter counts are zero. The largest function is index 204, `Compilerˉsourceˉwirˉcompileˉblock`, with 48,242 actions. The complete action-map SHA-256 is `8681cfd9d8c96e3d5dc70c2b97f62795c2e29b632fb66065f2dea8ca102b0511`.

A focused fixture covers allocation, static borrowing, slicing, mutable descriptor replacement, two descriptor-bearing direct-record fields, call borrowing, record/descriptor returns, and field selection. Repeated lowering is identical, independent reconstruction agrees, the unchanged ABI-21 fragment executes to result zero, and corrupt action/count plans fail as `WVN2903`. The exact compiler plan contains both static and host-snapshot borrows. ABI 21, WVB/WVO, native fragments, host containers, OS inputs, and QEMU artifacts remain unchanged; native execution still reaches `WVR3018` until a successor ABI consumes the plan.

After rebase onto the modular WebAssembly verifier and capability-bearing platform-library candidates, focused ownership and exact-compiler checks pass again. Change-aware Windows verification completes a zero-warning Release build and passes all 83 selected Seed tests in 324.221 suite seconds; the golden compiler contract takes 213.423 seconds. Following the later Decision 0146 WebAssembly call-graph rebase, the focused ownership and exact-compiler checks pass once more with a zero-warning build. This is development feedback over the integrated tree, not dual-host qualification.

## Qualified bounded exact-compiler publication

[Decision 0111](../Decisions/0111-Bounded-Exact-Compiler-Fragment-Publication.md) attributes the exact compiler's 4,556,121 selected bytes before revising a security boundary. Function code accounts for 4,555,263 bytes across 328 functions and 191,632 machine-IR operations; alignment and immutable data account for only 858 bytes. The 48,578 zeroed frame slots currently emit 1,360,840 bytes, so eliminating all frame initialization would still leave 3,195,281 bytes. This rules out a local encoding change as an honest way to retain the 1 MiB whole-fragment ceiling.

The implemented hard fragment limit is 8 MiB, below the retained 34 MiB publication-image ceiling. ABI 20, target bytes, WVB, WVO, context 7, and service table 5 do not change. The exact compiler code remains 4,556,121 bytes with SHA-256 `8e74707df03a535e3ef68cfcfc8da6fa68fda29ccf4344e272fc50c8a5845bab`, passes the independent fragment decoder, and enters live W^X publication. Execution against a real source file then deterministically reaches `WVR3017` at the retained 1 MiB immutable-record arena before stdout, diagnostics, or output publication. The separate 4 MiB WVO/object and flat-linker limits remain unchanged.

The Windvale-written publication planner retains `WVPQ 1`, `WVPL 1`, canonical placement, and the 34 MiB image limit while accepting exactly 8 MiB and rejecting 8 MiB plus one. Its 7,189-byte portable core has SHA-256 `19e111490cba6f3dcae963169be82c8033d267ea505c30850502ae36fb36e13c`; its regenerated 7,105-byte retained bridge has SHA-256 `5ad896d92368dcadc61f358d51f5786408d9f1dc977efa5f522f99230f3ed51e`. An 8 MiB plus one fragment fails independently as `WVN3005`.

Windows Development passes a zero-warning Release build, all 67 regular Seed tests, and all 25 OS tests in 67.4 seconds wall time. Windows Standard passes all 68 Seed tests and all 25 OS tests in 219.6 seconds wall time. The Seed suite takes 206.326 seconds, including 153.641 seconds for the qualification-only golden compiler contract.

Exact implementation commit `e139e4e391192c115a1cabd92a1e0ab3bd71d31d` passes GitHub [Verify run 30768107059](https://github.com/eworker-inc/Windvale/actions/runs/30768107059). Windows and digest-pinned Debian 12 each complete a zero-warning Release build, all 68 Seed tests, all 25 OS tests, exact retained-planner reproduction, and the complete native CLI gate. Windows Seed takes 227.915 seconds with a 164.428-second golden contract; Linux Seed takes 207.347 seconds with a 153.987-second golden contract. The complete jobs finish in 8m29s and 7m38s respectively. QEMU was not rerun because ABI 20, every generated machine byte, and all OS source/artifact inputs remain unchanged.

## Qualified ABI-20 two-byte construction

Qualified Decision 0109 advances the implementation to `x86-64-wvb-baseline-v20` while retaining WVB 1.6, WVO 1.0, execution context 7, service table 5, the 64-parameter convention, ABI 18's canonical typed block-slot map, and the 2,048-cell physical frame ceiling. Verified `Bytesˉfromˉu16ˉlittle` now lowers from `U32` to a two-byte execution-arena-backed descriptor only after an unsigned 65,535 upper-bound proof. Detail 12 maps a larger value through packed status 5 to `WVR3016` before arena mutation.

The focused borrowed-bytes test passes on Windows. It executes valid values zero and 65,535 through the reference interpreter, W^X JIT, and linked WVO/AOT, checks the first invalid value 65,536 under interpreter and both native publication forms, reproduces deterministic fragment and WVO bytes, and rejects corrupt range constants/branches, allocation/store widths, failure details, and scalar/result aliases. Exact compiler preflight clears the last observed unsupported operation, completes x86-64 selection, and reports `WVN2902`: 4,556,121 selected bytes against the retained 1,048,576-byte fragment limit.

The Windows Development gate passes a zero-warning Release build, all 67 regular Seed tests, and all 25 OS tests. The complete Standard gate passes all 68 Seed tests, including the golden compiler-reproduction contract, and all 25 OS tests in 215.085 seconds. All four pinned Windows QEMU 11.0/Q35/TCG scenarios pass with the exact retained Probe-32 identities: normal `b8f0e656066b1e4f28edc4124eca6eea18130a0d6c0f4a9018e8ae817a0fa985`, invalid opcode `0322ce3d3a9fecfa5c84809d8594f4f3ea643aaff2776f8d25668f1d723b9b54`, general protection `1a0bd9f37c595d4170bd05fe83cc05dc344d2223674a01812f253ceb77893e40`, and contained user fault `68319856b2913b3c857012d3fd38f147cf2a2307afacc9ffc8c8a33c005d0cf9`.

Exact implementation commit `a63ca0fe1d9d66882ead68ed28bcd7cd6a067f23` passes GitHub [Verify run 30766123518](https://github.com/eworker-inc/Windvale/actions/runs/30766123518). Windows and digest-pinned Debian 12 each pass a zero-warning Release build, all 68 Seed tests, all 25 OS tests, and the complete native CLI gate. Seed elapsed time is 247.807 seconds on Windows and 202.369 seconds on Debian; the golden contract takes 188.544 and 150.427 seconds respectively. This qualifies ABI 20 and Decision 0109 as the latest shared native/OS baseline while retaining Probe 32's exact firmware identities. QEMU execution remains Windows-only evidence.
## Prior Stage 0 linker qualification (WVB 1.5)

Commit `9c4b9f5` was archived and transferred with matching SHA-256 `44565d0f6366eb418dc1a4555d1f7b355df63ec2ea836440de54639087fc335a`, then verified in the uniquely named disposable directory `/tmp/windvale-linker-9c4b9f5-20260730` on the isolated E-Worker QA host. The archive contained the canonical WVA fixture as 403 bytes plus the linker project, provider fixture, and shell verifier. The host ran Debian GNU/Linux 12 x64 with .NET SDK `10.0.302`. The verification did not use E-Worker release, configuration, service, or durable-data paths. The Debian report was retrieved with SHA-256 `b84cd3a180e8cb167dca3cf909457de91a8643f19911628c426c6e52c6fe5fc0`; the provider WVO, linked image, and canonical map were retrieved separately and each matched the Windows artifact byte for byte; then the resolved exact temporary directory and transferred archive were removed.

An initial QA attempt at `57f2544` exposed that `.wva` lacked an explicit archive line-ending attribute: the Windows working file was LF while the exported Debian fixture was CRLF, producing semantically equal scans but different byte-count reports. Commit `e5fd109` added `*.wva text eol=lf`; its archive lists the canonical fixture as 403 bytes, and the complete qualification was rerun from that replacement commit. This was treated as a reproducibility defect rather than normalizing away the evidence.

```sh
Tools/Verify/Verify-Seed.sh
```

The Release build completed with zero warnings and errors. All 31 conformance tests passed, preserving every prior language, bytecode, runtime, WvDump, WVO, and Windvale-assembler case while adding complete Stage 0 linker coverage. The linker tests exercise actual-address alignment from aligned and unaligned bases, all section kinds, materialized BSS and padding, local/export/import resolution, both relocation kinds, semantic input order, locale independence, every WVL failure family reachable from external input, aggregate and map limits, relocation/address overflow, 200 deterministic hostile objects, full-image reconstruction, and exact image/map digests. The real CLI flow assembled both inputs, linked them, checked every essential map record and exact artifact digest, rejected an undefined import without output, preserved an existing image on rejection, surfaced a native missing-parent failure without output, and preserved every prior native-host case on both systems.

The Windows and Linux reports were then compared with the test runner:

```powershell
dotnet run --project Tests/Windvale.Seed.Tests/Windvale.Seed.Tests.csproj --configuration Release --no-build -- --compare-reports artifacts/seed-conformance-windows-x64.json artifacts/seed-conformance-debian-x86_64-9c4b9f5.json
```

The comparator confirmed equality between Microsoft Windows `10.0.26200` x64 and Debian GNU/Linux 12 x64 on .NET `10.0.10`. Both hosts produced:

- Module format `1.5`, object format `1.0`, assembly format `1`, and link format `1`.
- `Sum-Data.wvb`: `64134dfd779b353c5e501c9c23337a0c3849bfef2c97a63a07913705b0f10c6b`, result `29`.
- `Hello-Windvale.wvb`: `43d565c304cf2e2f5d886ee30b1fabf0b2fbfb0c8cd28bd932d85d5add0bf504`, output `Hello from Windvale` plus LF, result `0`.
- `Read-Wvb-Header.wvb`: `0cdf05f6c9e1fb1db0d5ab449207870b5e47cc248f187cd43cd9a5c3c9eee995`, result `1`.
- `Wv-Dump-Core.wvb`: `2957fc5523ae3ca16cf1aaeb9104c14a3342a0aefde9ac591bb689f744f1467f`, result `0`, with the complete hosted Sum report equal.
- `Wvo-Object-Core.wvb`: `a5d574ea646946b159d95bd7e51434bfcbf7545083a54541438a79a2e5e999df`, result `0`, output `Wrote WVO 1.0 bytes=189` plus LF.
- `Sample.wvo`: `006fd80183da7fbc71d3c6d63b65e6f3551765508fe9dba6f38ba80e002eb28a`; the directly retrieved Windows and Debian files were byte-for-byte equal.
- Assembled `Hello-Object.wvo`: `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`; the directly retrieved Windows and Debian CLI outputs were byte-for-byte equal.
- `Wva-Assembler-Core.wvb`: `7dbcf042f011adab5a04670973fc17b6b63d50fb08c09e8e54c3a4adb2c00825`, result `0`; the directly retrieved Windows and Debian modules were byte-for-byte equal.
- Hosted assembler object: `992c298a4f9b68dec27b7203a2770f2a37ef2016ea45e88d33ee21994060fe85`; the directly retrieved Windvale-written Windows and Debian objects were byte-for-byte equal to each other and to Stage 0.
- Hosted assembler report: `wvasm 1`, then `assembly status=valid object-bytes=218 sections=2 symbols=3 relocations=2 offset=403 line=22 column=1` on both hosts.
- `Console-Provider.wvo`: `486134e34bb32abadd233d1c3303acd9c313aa69d3874cafdce0fcb61b6e72ab`; the directly retrieved Windows and Debian objects were byte-for-byte equal.
- `Hello-Linked.bin`: 24 bytes, `0e02d447ec379e8bc8be373694d6ca14fdde0125550cbd34ee05b3ecc63ffe9a`; the directly retrieved Windows and Debian flat images were byte-for-byte equal.
- `Hello-Linked.wvmap`: 1,721 bytes, `31bc6a8e90d5f3049ae3e2eb0735a901923186d6a03ed40f22762b557b2ba5f4`; the directly retrieved Windows and Debian maps and the complete report-embedded maps were byte-for-byte equal.

This qualified roadmap gate 6F: the separate Windvale Linking 1 contract and C# Stage 0 flat-image oracle. At that point, the next Phase 6 gate was the Windvale-written verified-bytecode linker producing exact oracle images and maps through explicit hosted boundaries. The Windvale assembler remains qualified at `a689617`, semantic inspector at `cc57bf9`, lexical scanner at `e5fd109`, WVA 1 Stage 0 oracle at `3bfc6bb`, Phase 5 at `f87a5fa`, Phase 4 at `a829fc8`, Phase 3 at `1f4b48a`, the Phase 2 WVB 1.3 slice at `e6c51c6`, WVB 1.2 at `a4b0f5d`, and WVB 1.1 at `60fd261`; none is a compatibility promise or supported obsolete input format. Future bytecode, assembly, object, link, compiler, verifier, runtime, host-adapter, or report-contract changes must regenerate both native reports rather than inheriting this evidence automatically.

## Local Windvale-owned console-application verification evidence

[Decision 0132](../Decisions/0132-Windvale-Owned-Console-Application-Verification.md) moves completed PE/ELF acceptance into portable Windvale while retaining the existing detailed C# parsers as independent recovery oracles. One logical application is presented as a first value of at most 4 MiB and at most 8,304 trailing bytes. The verifier regenerates the canonical sparse construction recipe, checks every literal and implicit-zero byte, skips only its one opaque native span, and returns fixed `WVCV 1` evidence plus the exact recovered native image through a named write.

The portable core is 46,074 bytes with SHA-256 `326e2ecfc1f4dd8bd24f71fff4a9db960de2519d9aae17afbcd6a005c2e7c94d`; its retained hosted bridge is 46,150 bytes with SHA-256 `74542907a1b7a90d6d13ee157e7a9e7a4e60e83c042a5486e2f0ab3113ad6013`. The combined ownership test recompiles both source sets, checks exact embedded identity and only `file.read_bytes`/`file.write_bytes`, proves deterministic evidence, rejects malformed evidence and noncanonical chunks without output, and recovers exact 4 MiB native images from both maximum completed applications. The maximum Linux case uses the full 8,304-byte second chunk.

The existing PE and ELF malformed-input corpora now require rejection by both the portable and independent C# verifiers, avoiding a duplicate application corpus. Both live writers require target, native bytes, and entry to agree under both implementations. Canonical output is unchanged: 5,120-byte PE SHA-256 `5947c00a81f4cf94651d42d619f3173a622448d042f4fa20e3042940d4a56c77` and 8,304-byte ELF SHA-256 `8af8b46c290965cfc4475d882ac2d5fbdb0ffe4c493a19883a19c2683a319ec4`.

Windows Development completes in 90.6 seconds on the merged candidate with a zero-warning Release build, all 76 regular Seed tests in 74.315 seconds, and all 31 bounded OS tests. The combined verifier, Windows container, and Linux container cases take 484, 1,430, and 144 milliseconds. The affected combined case also passes independently in 1.320 seconds after the upstream replay.

Exact descendant `ea1aa89` passes GitHub [Verify run 30783457203](https://github.com/eworker-inc/Windvale/actions/runs/30783457203) on Windows and digest-pinned Debian 12. Both hosts pass all 77 Seed tests, all 31 OS tests, the golden compiler contract, and the native CLI gate with zero-warning Release builds. Windows Seed takes 241.987 seconds and Linux Seed 216.976 seconds; their golden cases take 169.303 and 150.484 seconds. This closes the fresh dual-host qualification requirement for the paired portable verifier and live Windows/Linux writers.
