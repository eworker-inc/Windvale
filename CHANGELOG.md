# Windvale changelog

Windvale has not issued a stable release. Development history and qualification evidence are recorded in Git, `Documents/Decisions/`, and `Documents/Project/Seed-Verification-Evidence.md`.

Once releases begin, Windvale will use `v0.y.z` tags while public contracts remain experimental. A `0.y` change may revise an experimental contract without backward compatibility, but release notes must identify affected formats and migration expectations. A future `1.0.0` release requires an explicit stability and support decision.

## Unreleased

### Added

- Completed the predictable-development-feedback milestone. Content-addressed linked-image/map reuse joins project, object, and current-host application checkpoints for all six portable database targets while all eight behaviors and cold paired-target qualification remain intact. The direct all-hit Windows owner now takes 87,800 ms instead of 402,638 ms, a 78.19% reduction; one 33-entry declaration machine-checks the three measured owner closures and five checkpoint families. Exact warm GitHub feedback passes end to end in 1m42s on Windows and 1m15s on Linux, while qualification jobs remain explicit and cache-independent.
- Added a 29,674 ms pinned WebAssembly engine checkpoint that reuses the checked-in browser package instead of repeating the last recorded 1,619,500 ms WVB/Wasm construction path. Complete cold reconstruction and the broad engine matrix remain the explicit qualification owner.
- Split the 733,980 ms Seed native front-door owner into a 13,900 ms pinned identity/admission smoke and a separately named complete reconstruction owner. Ordinary feedback still checks all 18 artifact identities and admits all six WVB modules, while explicit qualification retains the 105-artifact, 185-assertion reconstruction.
- Archived the frozen managed Stage 0 implementation outside `main`, retained its exact qualified release and checksums through a small in-tree recovery pointer, and added a fail-closed guard against tracked managed source or direct managed operational entry points returning.
- Documented separate tag families for immutable recovery archives, qualified native-only baselines, and `v0.y.z` product releases; `v0.1.0` remains reserved for the first inspectable product preview rather than the .NET-removal checkpoint.

- Added repeated copy-on-write updates to an existing depth-two Windvale Database tree. Portable branch rewrites replace or split any routed leaf while preserving untouched siblings, commit batches enforce unique obsolete-page predecessors, hosted mutation explicitly copies borrowed provider pages, and the Windows fixture covers all five interruption points for both initial and repeated publication without .NET or C#.
- Added measured four-way Windows/Linux native-retirement sharding without dropping or caching any owner. Manifest version 2 pins every assignment across 52 suites and the corrected 3,287-case inventory, coordinators retain the sequential and focused modes while reporting per-owner timing, and GitHub requires all eight cold shards through the unchanged final gate.
- Added the first durable multi-page Windvale Database generation: deterministic variable-key leaf splitting, a two-child branch root, a reusable 63-data-page commit batch, and a provider-backed reader with 32-level, snapshot, graph, and inherited-range bounds. The focused native owner now builds, publishes, and reopens the depth-two tree across all five interruption points without .NET or C#, while the lowerer rejection goldens retain ABI-23's structured diagnostics and ordinary-front-door compiler promotion remains explicit.
- Moved the canonical capability-free console-AOT smoke in both broad Seed qualification scripts onto paired native lower, object-verification, flat-link, and version-1 packaging front doors. The current Windows helper reproduces the established 5,120-byte PE and 8,304-byte ELF and executes the PE to result 29; together with the preceding front-door transfer, eleven managed invocations have left each host script while the broad harness and general execution transfer remain explicit.
- Moved the broad Seed qualification smoke for four representative Project 1 builds, WVB verification/inspection, and malformed-project preservation onto paired native helpers. This removes nine managed invocations from each host script while leaving the still-managed runtime, packaging, harness, and broad qualification work explicit; the new example manifests are colocated with their source rather than added at repository root.
- Replaced the standalone Windows WebAssembly verifier's managed compiler and backend execution with native source/WVB front doors plus one manifest-bound paired WVB-to-Wasm launcher. The complete strict Node.js engine, record-arena, and compiler-probe route now passes without a normal .NET invocation, reducing the direct managed-entry inventory to three broad normal gates and nine explicit recovery commands; independent Linux execution and grouped qualification remain.
- Exposed Probe 40's fifteen logical object components as fourteen ordered verified WVO containers, keeping the managed recovery link byte-identical while measuring the retained native linker's complete resolved-link resource blocker precisely. The already-large probe source now delegates the inventory/link boundary to a focused file.
- Cut the Probe 40 recovery workflow over to the retained native UEFI packager. Stage 0 now emits only its already-verified linked payload and entry evidence for this path; the native tool reconstructs the exact established normal EFI while managed object production and linking remain explicit follow-up work.
- Retained paired native-built Windows/Linux UEFI packager candidates with digest-bound launchers and a three-case `.NET-free` retirement lane. The shared hosted-WVB chain now accepts an explicit cross target, while the UEFI lane composes real native linker output, exact deterministic EFI construction, and invalid-entry destination preservation without a managed container constructor.
- Connected the digest-bound native flat linker to canonical UEFI packaging through a focused hosted Windvale tool. The packager consumes exact linked bytes and entry evidence, invokes the independently verified portable constructor, preserves existing output on rejection, and rebuilds through a pinned Project 1 front door; retained host containers and complete Probe 40 composition remain explicit follow-up work.
- Extended native console-packager reconstruction through deterministic WVO output. Cohesive packager, recipe-verification, target-header, and PE-section helpers keep the existing bounded arena while both ordinary and segmented WVBs now lower through the pinned .NET-free application; the same source rebase makes the console verifier and publisher WVB projects natively buildable while their broader unsupported lowering surface remains explicit.
- Added exact .NET-free source reconstruction for the ordinary and segmented console packagers. Canonical Project 1 inventory order lets the digest-bound native front door rebuild, compiler-align verify, and atomically publish both existing WVB identities; a focused two-case lane retains host-container construction as the separate remaining Stage 0 seam.
- Added bounded native construction of maximum-size version-1 console applications. A focused Windvale recipe streamer emits one exact 4-MiB chunk, one bounded remainder, and a `WVCS 1.0` manifest without joining the PE or ELF; a fixed two-case lane matches complete Stage 0 application identities and then revalidates both chunks through the independent native verifier. The host containers remain explicit Stage 0-constructed candidates pending native host-container reconstruction and cross-host promotion.
- Added a fixed .NET-free segmented console-size lane. A dedicated read-only Windvale verifier owns two immutable input snapshots, preserves the exact Windows and Linux maximum-plus-one boundaries, and checks exact portable rejection ordering without weakening either case to a small malformed application.
- Added portable Windvale admission for format-2 hosted PE/ELF applications and a fixed .NET-free native evidence lane. Two valid containers and the exact thirteen managed mutations now pass through the digest-bound atomic publisher with exact recovery, rejection, preservation, and scratch-cleanup requirements; Stage 0 remains the explicit constructor and recovery oracle.
- Added fixed .NET-free random-containment evidence for the exact legacy Seed sequence. One compact corpus preserves 500 source, 1,000 WVB, and 500 WVO values plus Stage 0 provenance; focused native lanes contain every value, preserve all inputs and assembler destinations, and keep compiler, host-process, source, and binary responsibilities in reviewable modules.
- Added a fixed .NET-free hostile-size WVO admission test. The first byte beyond the ordinary 4-MiB object/value limit is frozen in a compact archive; native verify, inspect, link, and publish paths all fail at the bounded file-snapshot boundary while preserving inputs, outputs, destinations, and publication scratch invariants.
- Added a fixed .NET-free console-container mutation corpus. Ten exact PE and nine exact ELF truncation, structural, padding, context, relocation, and trailing-byte cases retain their Stage 0 diagnostic provenance while the native publisher requires rejection, input/destination preservation, and zero scratch.
- Added a fixed .NET-free WVA differential corpus. The exact 200-case Stage 0 mutation sequence is frozen in one compact archive; the native assembler agrees on all 199 rejection codes and reproduces the sole accepted 243-byte WVO while preserving every source and rejected destination.
- Added a fixed .NET-free WVO differential corpus. The exact managed sequence and oracle decisions are frozen for 128 one-byte mutations plus 128 arbitrary values; the native verifier agrees on all 32 accepted and 224 rejected cases while preserving every input.
- Added a fixed .NET-free console-container hostile-input corpus. Two portable xorshift32 families freeze 128 PE and 128 ELF candidates with explicit zero and maximum-size boundaries; the native publisher requires exact rejection, unchanged input and destination, and zero scratch for all 256 cases.
- Added a fixed .NET-free native linker hostile-input corpus. A portable xorshift32 specification, compact digest-bound archive, and per-input manifest replace live framework PRNG generation for 200 zero-through-511-byte values; every public linker call requires exact `WVL1002` plus input and destination preservation.
- Added one manifest-driven .NET-free native retirement-suite coordinator for Windows and Linux. Its current digest-bound plan fixes 27 command lanes, 3,127 cases, exact child summaries, channel behavior, and fail-fast selection; exact filters preserve focused local checks while the unfiltered suite remains reserved for the final grouped gate.
- Added a verified Windvale-native WVO export renamer and used it to compile, lower, and bind the Probe 40 admission source in the ordinary build. The frozen object seed falls to nine WVOs while Stage 0 remains an explicit recovery and differential lane.
- Added a native Probe 40 Windvale-source producer. The ordinary image build compiles and lowers the canonical native-probe source to its exact 7,306-byte WVO, reducing the digest-bound Stage 0 object seed from eleven objects to ten without changing the final EFI identity.
- Added a focused .NET-free unsafe-WVB rejection matrix. Five compact immutable instruction-stream mutations cover unknown opcode, truncated operand, invalid local and jump indices, and post-return instructions; both digest-bound verifier and inspector launchers require exact phase reports and preserve every input without a runtime mutator.
- Added a separate .NET-free native linker map-limit command. Two mechanically specified WVO fixtures are retained in one 21,046-byte compressed archive rather than large generated WVA sources; five valid inputs carry exactly 16,384 definitions, require the exact `WVL1012` report, and preserve every input plus the existing output in about one second locally.
- Added a deterministic mixed-value WebAssembly layout for all 417 exact compiler functions. `text`/`bytes` and wide scalars use Wasm `i64`, primitive scalars and bounded nominal handles use `i32`, source local ordinals remain unchanged through run-length declarations, and the established scalar/control Wasm output remains byte-identical.
- Expanded the fixed .NET-free native linker rejection command from three to ten cases, covering every externally driven `WVL1001` through `WVL1010` family with exact reports and destination preservation. Five compact source/object fixture pairs exercise aggregate limits, duplicate exports, unresolved and kind-mismatched imports, layout overflow, and absolute/relative relocation overflow without rebuilding the linker or adding a large generated source.
- Completed the exact portable compiler's direct 32-bit scalar WebAssembly family in a focused source module. Unsigned divide/remainder and bitwise/shift operations now execute in the native engine; zero divisors return `WVR3032`, invalid shift counts return `WVR3033`, and exact instruction budgeting still takes precedence without publishing a failed result.
- Added deterministic instruction budgeting and checked `i32`/`u32` arithmetic to the general direct-WebAssembly dispatcher. The engine-valid three-function success module returns 42 in exactly 116 WVB instructions, budget 115 returns `WVR3011`, and a separate checked-add overflow module returns `WVR3007` in exactly 14 instructions without publishing a result; descriptor/nominal storage, call-depth containment, exact-compiler emission, and browser replacement remain pending.
- Added a fixed .NET-free native WVO read-only rejection matrix covering all thirteen stable WVO 1.0 status families. Ten compact fixtures plus three reused structural fixtures require identical exact reports from both digest-bound verifier and inspector launchers, empty standard output, and byte-for-byte input preservation without reconstructing either application.
- Added the first general scalar/control direct-WebAssembly dispatcher outside the monolithic backend. A capability-free Windvale adapter now turns a multi-block WVB fixture with locals, backward jumps, false branches, comparisons, and returns into a deterministic 254-byte module that Node validates and executes to the Stage 0 result of 42; metering, checked arithmetic, descriptor and nominal operations, exact-compiler emission, and browser replacement remain pending.
- Added a fixed .NET-free native WVA assembler rejection matrix covering `WVA1001` through `WVA1011`. Ten compact fixtures and one temporarily generated one-byte-over-limit input require exact report hashes, empty standard output, and complete destination preservation without rebuilding the already-qualified assembler or adding a very large source file.
- Classified every operation reachable in the exact browser compiler into explicit typed-direct, descriptor, nominal, call, or dispatcher-control WebAssembly lowering ownership. A capability-free Windvale adapter maps the independently extracted 58-opcode exact set to 464 deterministic bytes and fails closed on an unknown opcode; direct emission and browser replacement remain pending.
- Added a focused .NET-free native WVB-to-WVO lowerer rejection test. Fixed malformed-WVB and valid-but-unsupported-function inputs require exact native status hashes, empty standard output, byte-for-byte destination preservation, and zero private lowerer work without rebuilding the lowerer or rerunning the AOT chain.
- Added a general Windvale-native basic-block directory for direct compiler WebAssembly. Reachable functions now carry deterministic fallthrough, jump, false-branch, and return successors outside the monolithic backend; a portable self-analysis contract independently agrees on 88 reachable functions, 15,085 instructions, and 1,764 blocks, rejects an in-instruction branch target exactly, and a linear valid-path boundary pass cuts the focused test from 26.5 to 14.2 seconds. Direct compiler Wasm emission and browser replacement remain pending.
- Added a focused .NET-free native publisher rejection test shared by console-application and WVO publication. Both digest-bound launchers require exact phase reports, empty standard output, complete destination preservation, and zero `.wvpublish-*` scratch files without rebuilding either publisher.
- Added a focused .NET-free native console-packager rejection test over a dedicated six-byte image fixture. Entry-at-end, non-decimal entry, and empty-image cases require exact host-target report hashes, empty standard output, and byte-for-byte preservation of an existing destination without rebuilding or relinking the AOT chain.
- Added a focused .NET-free native linker-rejection test that reuses pinned WVO fixtures and the digest-bound linker launcher. Invalid base, missing entry, and malformed object cases require exact report hashes, empty standard output, and byte-for-byte preservation of an existing destination without rerunning the source-to-AOT chain.
- Expanded the digest-bound .NET-free native test plan to 26 cases with one accepted WVO plus fixed bad-magic, truncation, and trailing-byte rejections. Complete input and verifier-report hashes replace a live C# oracle while preserving exact cross-host behavior.
- Added a general Windvale-native executable graph for direct compiler WebAssembly. The exact compiler resolves arbitrary `Main` index 2 into 417 function entries, 2,991 ordered call targets, and 397 reachable functions without the old sixteen-function mask or call-direction restriction; this is lowering input, not yet emitted compiler Wasm or a browser speed claim.
- Added a bounded Windvale-native atomic publisher for complete WVO 1.0 objects. Complete portable admission now lives in one focused module shared by the inspector and new `WVPO 1` Windows/Linux candidates; the accepted-subset lowerer writes a private candidate and reaches durable replacement without loading .NET, while Stage 0 host-container construction and grouped qualification remain explicit.
- Added a bounded Windvale-native atomic publisher for completed version-1 console applications. The new `WVPA 1` Windows/Linux candidates validate `.exe`/`.elf` bytes through the portable verifier, reuse the qualified native sibling/write/reread/replace transaction, and make the digest-bound console packager publish privately constructed candidates without loading .NET; Stage 0 host-container construction and grouped qualification remain explicit.
- Added immutable Windvale-native function and nominal-type directories plus a compact 36-value typed stack for direct compiler WebAssembly. Sharded exact evidence validates all 417 compiler functions and all 2,991 direct calls at maximum stack 34, and an in-range incompatible target is rejected with exact function, offset, and opcode diagnostics; the deployed browser interpreter remains unchanged.
- Replaced the browser compiler's 100,000-instruction self-warmup with a digest-pinned 20,000-instruction Windvale loop. The smaller guest triggers the same optimized WebAssembly interpreter tier without rescanning the 920 KiB compiler during warmup, reducing the measured exact local source-to-WVB path from about 90 seconds to about 65 seconds while preserving compiler output and execution evidence.
- Added lossless `u32.from_u8` lowering through the existing typed instruction-state owner. A focused `0u8`/`255u8` fixture reproduces Stage 0's complete WVO through both Windvale adapters without a runtime service or failure path; full-tool self-lowering advances to function 36's `u32.remainder` frontier.
- Added bounded `bytes.from_u16_little` lowering through the shared ABI-22 dynamic-byte arena. The focused maximum-value fixture reproduces Stage 0's complete WVO through both Windvale adapters, including the `WVR3016` narrowing-failure branch; full-tool self-lowering advances to function 29's `u32.from_u8` frontier.
- Added checked `u32.add`, `u32.subtract`, and `u32.multiply` lowering as one typed ABI-22 arithmetic family. A focused high-value fixture and an overflow vector reproduce Stage 0's complete WVO through both Windvale adapters; full-tool self-lowering advances to function 26's `bytes.from_u16_little` frontier.
- Added bounded `bytes.from_u32_little` lowering through the shared ABI-22 four-byte construction path. The focused fixture checks a high-bit unsigned value and reproduces Stage 0's complete WVO through both Windvale adapters without a runtime service; full-tool self-lowering remains fail-closed at the earlier `u32.multiply` frontier.
- Added bounded `bytes.from_i32_little` lowering through the existing ABI-22 dynamic-byte arena. The focused byte-construction fixture checks exact signed little-endian round-trip behavior and reproduces Stage 0's complete WVO through both Windvale adapters without a runtime service; full-tool self-lowering clears functions 1 and 2 before reaching function 3's `u32.multiply` frontier.
- Added bounded `bytes.from_u8` lowering through the existing ABI-22 dynamic-byte arena. A focused `255u8` fixture reproduces Stage 0's complete WVO through both Windvale adapters without a runtime service; full-tool self-lowering now advances to `bytes.from_i32_little`.
- Added ABI-22 service-backed `u32.format` to the Windvale-written native x86-64 lowerer. A focused maximum-value fixture requires decimal `4294967295` and reproduces Stage 0's complete WVO through both Windvale adapters; full-tool self-lowering now advances to `bytes.from_u8`.
- Admitted bounded enum parameters and returns through ABI 22's existing 32-bit scalar path while preserving exact nominal identity in the call directory and assigning returned values to the enum slot group. The focused enum fixture now crosses `Keep(Weather) -> Weather`, and full-tool self-lowering advances to the first unsupported `u32.format` instruction.
- Replaced the native record planner's single-operand event with bounded per-instruction record-use lists, allowing direct calls to consume all record arguments admitted by ABI 22's existing 64-parameter directory. A compact four-record helper reproduces Stage 0's complete WVO through both Windvale adapters.
- Compacted native record-local liveness to a bounded directory of 256 record locals instead of indexing every scalar compiler temporary, expanded the retained record planner to 1,024 blocks and 8,192 instructions, and stopped fixed-point iteration once stable. A synthesized 129-record-local, 130-block, 1,032-instruction helper reproduces Stage 0's WVO without adding a large source fixture.
- Expanded the Windvale-written native x86-64 lowerer's measured per-function envelope to fewer than 2,048 combined parameters/locals, 32,768 code bytes, and 8,192 decoded instructions while retaining the exact 2,048-cell native frame check. A compact synthesized 1,025-local, 10,246-byte, 2,050-instruction module crosses all three former prototype ceilings and reproduces Stage 0's WVO byte for byte without adding a giant source fixture.
- Extended the Windvale-written native x86-64 lowerer to ABI 22's bounded 64-parameter call contract. The first four scalar, record, or descriptor arguments retain their register representations; later arguments use exact 16-byte outgoing stack cells, and caller-owned descriptor/record result addresses account for the temporary reservation. A focused call-argument module replaces duplicated register-only emission, and a mixed six-parameter descriptor fixture reproduces Stage 0's complete WVO byte for byte through both Windvale adapters.
- Added bounded `text` and `bytes` helper parameters and returns to the Windvale-written native x86-64 lowerer. Descriptor cells pass by address through the first four register positions, caller-owned result cells use `RAX`, and callee-side checkpoint handling preserves borrowed values or compacts arena-owned returns. A focused descriptor-call fixture returns 42 across interpretation and native execution while both Windvale adapters reproduce Stage 0's complete WVO byte for byte.
- Expanded the Windvale-written native x86-64 lowerer's measured module envelope to 512 functions, 64 immutable data declarations, and 64 nominal types, covering the real hosted tool's 297/33/29 tables. One layout-owned generator replaces hard-coded function and data names beyond ordinal seven, and a 9-data, 9-type, 10-function fixture reproduces Stage 0's canonical WVO byte for byte.
- Added four bounded dynamic text operations to the Windvale-written native x86-64 lowerer. Existing ABI-22 services now lower text concatenation, UTF-8 validation/conversion, and quoting with exact runtime/invalid-UTF-8 failure branches; a focused vector reproduces Stage 0's exact 5,140-byte WVO. Dynamic bytes concatenation remains the next separate ownership-heavy boundary.
- Added bounded static borrowed descriptors to the Windvale-written native x86-64 lowerer. Up to eight immutable text, bytes, or i32 declarations now share exact `.rodata`, symbols, and relocations; descriptor locals, UTF-8 views, slicing, length, and little-endian reads reproduce Stage 0's exact 7,626-byte WVO. Descriptor instruction state moved into a focused source module after the monolithic emitter exceeded its retained frame limit; dynamic ownership and descriptor calls remain fail-closed.
- Completed the bounded Windvale-native `u32` and `u8` comparison families after measuring them as a shared blocker in all remaining compiler-produced fixtures. One reviewed typed-return vector now covers all eight comparisons across true and false routes and reproduces Stage 0's exact WVO; descriptors, nominal values, capabilities, and other scalar arithmetic remain fail-closed.
- Extended the Windvale-written native x86-64 lowerer with bounded `u8`/`u32` locals and operations plus typed scalar helper returns. The compiler-produced `Function-Only.wv` fixture now reproduces Stage 0's exact 6,041-byte `.text` and 6,216-byte WVO through the memory adapter, hosted tool, and generated native tool; unmeasured scalar operations remain fail-closed.
- Generalized the Windvale-written native x86-64 call directory to accept exported `Main` at any ordinal plus bounded forward, self-recursive, mutually recursive, and cyclic scalar calls under ABI 22's shared instruction/depth budgets. A complete signature pass and canonical symbol projection reproduce Stage 0's exact general-call WVO while preserving every retained object byte.
- Added bounded immutable i32 static data to the Windvale-written native x86-64 lowerer. Focused data and WVO modules now own exact payload admission, `.rodata`, `$data_0000`, and relative relocations; canonical `Sum-Data.wv` reproduces Stage 0's exact 3,288-byte object while code-only objects remain unchanged.
- Added nine digest-bound malformed-WVB fixtures to the .NET-free native test plan. `WVNT 4` reuses the fixed base64 input contract and exact native verifier oracle for eight typed-execution corruptions and one unreachable-control corruption without adding a mutator or another host-specific parser.
- Added five digest-bound malformed-WVB envelope fixtures to the .NET-free native test plan. `WVNT 3` decodes exact base64 inputs, verifies their complete identities, and requires the qualified native verifier to reject bad magic, version, UTF-8, truncation, and trailing data with exact exit and channel behavior on both host adapters.
- Expanded the Windvale-written native x86-64 lowerer from one optional helper to one through eight scalar/control functions. A focused function-layout module owns per-function offsets, signatures, and WVO symbols; the existing three-function calls/control fixture matches Stage 0 exactly, and all retained one/two-function WVO bytes remain unchanged.
- Extended native test orchestration with exact failure oracles. Digest-bound `WVNT 2` now requires deterministic exit, output channel, Windvale status, and guest-instruction reports for invalid UTF-8, out-of-range bytes, and truncated little-endian reads without consulting .NET during the run.
- Expanded the digest-bound native test plan from two scalar/control cases to five portable cases. The qualified native builder and pinned runner candidate now reproduce and execute fixed existing conformance identities for typed constants, a six-function graph, static text/bytes, immutable byte operations, records, enums, and nominal calls without invoking .NET; grouped Windows/Linux qualification remains deferred.
- Added the first current-host native source-to-AOT composition proof for the accepted scalar/control/direct-call subset. The qualified native source builder now feeds separate native lowerer, linker, and console-packager candidate processes; the exact PE/ELF product executes to 42 without a .NET child mapping. Fixed WVB, WVO, link-map, flat-image, and container identities are retained for the grouped Windows/Linux gate; no artifact promotion or ordinary AOT launcher is claimed yet.
- Added the paired `windows/linux-x64-wvb-to-wvo-v1` source candidate for the existing Windvale-owned metered scalar/control/direct-call lowerer. The hosted shell now lives under `Compiler/Windvale`, emits the fixed canonical WVO without loading .NET, and reuses the existing ten-service startup with no new assembly. Direct signed little-endian construction removes its text-formatting workaround while retaining every differential WVO byte; complete backend coverage and grouped qualification remain pending.
- Added the paired `windows/linux-x64-console-packager-v1` source candidate. One Windvale application now validates and materializes the existing sparse version-1 PE/ELF construction recipe, requires exact portable-verifier recovery, and publishes only after success while reusing the established hosted-tool startup with no new platform assembly. Its first complete-value boundary is explicitly 4 MiB; grouped dual-host qualification and artifact promotion remain pending.
- Added paired source candidates for the remaining standard binary front doors: `windows/linux-x64-wv-linker-v1` package the Windvale-owned linker without new startup assembly, while `windows/linux-x64-wvo-inspector-v1` move WVO verification and inspection into an architecture-owned read-only Windvale module with no file-write authority. Their focused current-host checks precede one grouped final Windows/Linux retirement gate; neither candidate is promoted or claimed cross-host qualified yet.
- Cross-host-qualified the first ordinary no-.NET project source-to-WVB path at exact commit `d2e71c1d6491153afb715674fc13ba2c6276326a`: checked-in digest-bound Windows/Linux build-driver and publisher applications compile a bounded `.wvproj`, independently admit the candidate, and atomically replace the requested WVB. The inventory records exact source projects, targets, bytes, digests, and prior qualification provenance; separate Stage 0 reconstruction scripts preserve the recovery lane. Both hosts pass all 100 Seed tests, all 39 OS tests, and the complete native CLI gate in GitHub Verify run 30974274271.
- Accepted the exact native WVB publication-step contract and cross-host-qualified its first standalone Windows/Linux profile at commit `9d36387867ebff80ee94c6f9f7996da4ef32a4a3`: the tool snapshots and re-verifies a caller-owned candidate through the shared Windvale verifier, exclusively writes and checks its own destination sibling, performs one atomic durable replacement, and distinguishes active progress, cleanup-required failure, known unchanged rejection, completion, and indeterminate completion without strengthening `file.write_bytes` or adding a parallel compiler/verifier. Extended native fault injection and concurrent-reader stress remain hardening work.
- Qualified the Decision 0213 WVB 1.11 semantic-freeze baseline at exact commit `524e84afb6e5bab6bbd95ebc0b9eeaf886af834b`: Windows and Debian each pass all 97 Seed tests, all 39 OS tests, and the complete native CLI gate; the exact native compiler, verifier, and project-aware build-driver applications run without loading .NET in their child processes. Forward source-language semantics now advance only through the Windvale compiler while C# becomes feature-frozen recovery and independent evidence.
- Accepted the Stage 0 semantic-freeze and native-front-door migration policy: the next dual-host-qualified WVB 1.11 state becomes the frozen C# source-compiler baseline, future language semantics move only through the Windvale compiler, existing C# remains correctable recovery and independent evidence, and the qualified native build driver plus verifier move into the ordinary source-to-WVB path before the complete .NET-retirement gate. No normal-path dependency is removed by this decision.
- Portable checked database storage geometry with zero-based `u64` page identities, offsets, lengths, and exclusive ends; typed invalid-size, overflow, and outside-storage outcomes; and explicit lossless `U64ˉfromˉu32` lowering through canonical WVB 1.11 opcode `0xBF`, both source compilers, verification, inspection, and the reference runtime.
- The first rights-limited hosted database consumption path: `Readˉonlyˉwvdb` composes `filesystem.directory_read_v1` with the portable experimental `WVDB 1` reader, assembles at most six exact chunks under the existing 16,416-byte ceiling, and preserves provider failures separately from malformed database bytes without claiming a durable format or writable storage.
- Consolidated all current bytecode production and general validation on canonical WVB 1.11, removed lowest-required-version selection and obsolete-format compatibility paths, made narrow native/WebAssembly/Foundation/OS consumers declare their actual 1.11 subsets, and completed `i64`/`u64` support in the Windvale-written compiler through constants, WVIR, formatting, operators, exact little-endian byte codecs, differential byte equality, and runtime execution.
- Bounded guest-record reclamation in the import-free Wasm-hosted interpreter: stable 512-slot handles, nominal extent metadata, conservative tracing across active locals, operand stacks, saved call frames, construction fields, and nested records, deterministic first-fit reuse, reset, and exact live-set `WVR3017`. The exact portable compiler clears the former guest instruction 37,085 record boundary and reaches an ordinary 100,000-instruction `WVXO 2` budget result; the separate 64 KiB guest text/bytes heap is next.
- Cross-host-qualified Probe-40 generation-safe non-tail memory-object reclamation under Decision 0196: `WVKMEM17` adds a fixed bitmap, one owner byte per arena page, and three `WVMEMO01` records; portable Windvale owns the complete policy invariant while exact WVA first-fits, preflights, zeroes, releases, and rebuilds the client object at the same root while the later directory object remains live. `WVPROC17`, paging 5, ABI 22, and Probe 39's private timer experiment remain unchanged; all 87 Seed and 39 OS tests pass on Windows/Debian and all five pinned Windows QEMU scenarios pass.
- Proposed an integrated successor-contract review set without claiming implementation: generalization beyond Probe 40's fixed memory objects, flat resource domains, atomic two-level clean spawn, bounded supervision, exact stream/terminal results, staged shell grammar, copied `LinkPort 1`, a minimal IOMMU-aware `virtio-net` profile, separate time/entropy/key/identity/authorization providers, immutable package and release generations, a concrete Windvale 0.1 gate, nominal payload variants, and bounded move-only builders. Decision 0198 remains proposed pending product review.
- Bounded reclaiming value storage in generated profile-16 WebAssembly: deterministic first-fit allocation, descriptor retain/release, split/coalescing, reset, and a 16 MiB cumulative stress workload reuse the fixed 4 MiB arena without imports or memory growth. Constant-time interpreter local-shape metadata reduces incremental compiler interpretation cost by about 83%; the exact portable compiler crosses the former 1,511/1,512 boundary and reaches guest instruction 37,085, where the separate 4 KiB guest-record arena returns exact `WVR3017`.
- Cross-host-qualified Probe-39 HPET/local-APIC preemption under Decision 0188: memory 16 and paging 5 add supervisor-only timer MMIO plus private thread/timer evidence; exact WVA operations own APIC admission, calibration, interrupt entry, `SWAPGS`, `IRETQ`, and one-shot rearm; four interrupts prove three involuntary switches across the existing directory, init, and client roots. The retained workload, public process contract, and ABI 22 remain unchanged; all 87 Seed and 39 OS tests pass on Windows/Debian and all five pinned Windows QEMU scenarios pass.
- Accepted the future simple Windvale remote-terminal protocol direction: provisional `WVTS/1` carries one bounded terminal session per authenticated secure connection, uses TCP plus current TLS 1.3 for its first real network carrier, separates mutual identity from capability authorization, keeps input control typed, preserves normal and diagnostic output, disables replayable early data, and tears the session down on disconnect. SSH, WebSocket, QUIC, multiplexing, resume, forwarding, file transfer, and richer terminal surfaces remain later adapters or revisions. No remote-terminal mechanism is implemented by this decision.
- Accepted future Windvale OS network-stack architecture: protocol parsing and policy stay in one bounded user-space network service behind an isolated NIC driver; the kernel remains protocol-blind and enforces interrupts, timers, IPC, DMA/IOMMU ownership, accounting, and teardown; applications receive semantic rights-limited resolve, connect, datagram, listen, and secure-connection capabilities. The public direction is standards-based and dual-stack, while the first device proof remains a small deterministic static-IPv4 `virtio-net` slice. No network mechanism is implemented by this decision.
- Accepted future Windvale OS console, shell, and CLI architecture: device drivers, terminal service, capability-restricted shell, and ordinary CLI applications remain separate; commands launch from immutable identity-bound plans with explicit streams, grants, and resource domains; the shell grammar stays small while verified Windvale programs own substantial automation. No terminal input, shell, pipeline, or CLI mechanism is implemented by this decision.
- Accepted language-syntax and operator evolution: local inference, typed constants without mutable globals, named records, structured loop and Boolean control, explicit module qualification, exhaustive enum match, later payload variants/results, bounded collections, scoped resources, checked same-type arithmetic, statement-only assignment, bounded content equality, numeric-only ordering, short-circuit Boolean logic, and measured unsigned bitwise expansion. The new language-design guide is architectural direction; the implemented Seed grammar and artifacts are unchanged.
- Accepted product-wide direction across Decisions 0178 through 0183: least-privilege GitHub stewardship with GitHub plus E-Worker-local recovery, incremental Stage 0 archiving, a deterministic capability-oriented language and typed code/data packages, canonical stack WVB with stack-independent WIR, demand-driven native growth, selected next OS mechanism contracts, an earlier experimental Windvale-native browser route with separate permanent-host/target gates, and immutable package/trust/evolution boundaries. These decisions claim no new implementation.
- Exact per-function frames and compact saved-call frames in the Wasm-hosted WVB interpreter. The capability-free compiler advances from one to 1,511 guest instructions, budget 1,512 is the pinned first enclosing allocation failure, and the existing direct-AOT selector is explicitly measured as incomplete for compiler static data, nominal values, recursion, and general calls.
- Compiler-scale execution entry in the Wasm-hosted WVB interpreter: candidate-adaptive frames and expanded function, parameter, stack, instruction, budget, and call-depth bounds admit the exact portable compiler and execute its first instruction. The full run now reaches a pinned outer `WVR3018` from immutable whole-frame replacement, selecting bounded reusable interpreter storage as the next boundary.
- Cross-host-qualified Probe-38 third protected service and ready/wait dispatcher under Decision 0176: `WVPROC17`/`WVKMEM15` isolate the immutable directory provider from init, route resource and directory traffic through separate `WVENDP01`/`WVCHAN04` pairs, and select every initial entry and explicit wake from three validated process/thread records. All 87 Seed and 38 OS tests pass on Windows and Debian; five pinned Windows QEMU scenarios pass.
- A capability-free Windvale compiler adapter with a versioned WVSS-to-WVB memory contract, plus `WVXI 2` / `WVXO 2` byte-array guest execution in the import-free Wasm-hosted interpreter. All three compiler-capacity verifier phases admit the exact portable compiler; a small `Main(bytes) -> bytes` program executes under Node.js, and the compiler's initial sixteen-function preflight rejection supplied the next capacity boundary.
- A compiler-capacity, three-artifact Windvale-native WVB verifier bundle in import-free WebAssembly: independent canonical, typed-execution, and control/reachability meters admit the exact 599,868-byte compiler without weakening verification, changing execution ABI 3, or claiming compiler execution.
- Accepted future virtualization and accelerator architecture: pinned QEMU/TCG remains the reproducible guest oracle; direct physical/root providers are preferred for qualification while nested acceleration remains optional developer evidence; a future Windvale VM host keeps privileged vCPU/memory/IOMMU enforcement in the kernel and machine/device policy in isolated services; GPU and AI-compute access explicitly distinguishes software, shared paravirtual, hardware-partitioned, and exclusive-passthrough modes. No implementation or qualification is claimed.
- Accepted process, service, and driver architecture: applications, helpers, services, drivers, runtimes, and future VMMs share one capability-oriented process/thread mechanism; clean spawn, flat resource domains, a minimal service manager, endpoint control planes, measured shared-memory data planes, staged preemptive scheduling, and exact driver teardown define the direction without claiming those general mechanisms are implemented.
- A cross-host-qualified Probe-37 kernel-owned service endpoint: `WVPROC16` resolves every existing IPC capability through one generation-safe `WVENDP01`, rebinds it across client-root reuse, and closes it exactly once on provider exit or contained fault while retaining ABI 22, `WVCHAN04`, and all five boot scenarios.
- Cross-host-qualified deterministic format-3 compiler containers and direct Stage 2 reproduction: the same exact ABI-22 compiler PE/ELF runs on Windows and Debian, consumes the canonical twelve-source inventory, reproduces the byte-identical 599,868-byte compiler WVB, and loads no named .NET runtime in the child.
- Cross-host-qualified public `windows-x64-console-v3` and `linux-x64-console-v3` compiler targets through the existing independently verified, durable, atomic artifact publisher, including project-manifest build and verified-WVB `aot` routing on Windows and Debian.
- Bounded record and enum values in the retained profile-16 Wasm-hosted WVB interpreter: typed eight-byte cells, deterministic nominal defaults, field/member operations, enum-name publication, a 4 KiB append-only record arena with exact `WVR3017`, and complete-verifier-first differential evidence without changing execution ABI 3 or earlier Wasm artifacts.
- Import-free `bytes.sha256_hex` lowering in WebAssembly profile 16, with explicit target-side padding, schedule, compression, and lowercase hexadecimal output; the complete-verifier-first Wasm-hosted interpreter now covers empty, boundary-padding, and multi-block SHA-256 inputs without JavaScript or Web Crypto imports.
- Cross-host-qualified Probe 36 service-failure containment: `WVPROC15`/`WVCHAN04` record one exact init fault, scrub the live copied-message channel, wake the blocked client once with transport result `-1`, revoke its resources, and reach clean shutdown; all 87 Seed and 38 OS tests pass on Windows/Debian and five pinned Windows QEMU scenarios pass without a compiler or ABI change.
- Explicit bounded large-native WVO/link admission for the measured ABI-22 compiler: the unchanged WVO 1.0 codec and one shared linker now admit a deterministic 17,147,219-byte compiler object and reproduce its exact 17,130,441 native bytes under a 20 MiB opt-in profile, while standard 4 MiB object/link behavior, ordinary Windvale byte values, and existing application/container bytes remain unchanged.
- Exact Windows and Linux compiler service bundles plus fixed `WVHA 1` candidate manifests: the measured compiler binds its canonical six capabilities to exactly ten required ABI-22 services, explicit platform adapters, table slots, placements, sizes, and digests without changing existing PE/ELF targets or claiming runnable format-3 containers.
- Bounded paired compiler runtime-data plans: fixed 4 KiB headers serialize the exact eight-billion-instruction limit and initialize context, service, output, and file tables over explicit argument/snapshot/arena/scratch regions below 512 MiB while leaving all runtime-derived pointers zero for verified startup binding.

- Invariant `i32`/`u8`/`u32` formatting and deterministic UTF-16-compatible text quoting in the Wasm-hosted WVB interpreter, including scalar extrema, short/control escapes, BMP and supplementary Unicode, complete-verifier-first execution, and an explicit remaining SHA-256 boundary.
- Bounded text and bytes values in the Wasm-hosted WVB interpreter: uniform descriptor cells, fixed call frames, a charged 64 KiB heap, static data, byte readers/builders/concatenation, text concatenation/conversion, strict UTF-8, exact resource failures, and complete-verifier-first differential evidence under Node.js.
- The first Wasm-hosted Windvale WVB interpreter: a separate import-free profile-14 artifact executes complete-verifier-approved scalar calls and control with exact guest/outer budgets, call-depth containment, checked overflow, reference-runtime agreement, and a twenty-nine-artifact Node.js gate.
- Cross-host-qualified `windows-x64-console-v2` and `linux-x64-console-v2` targets. They package hosted `console.write_line` programs with canonical WVA startups, the existing exact native output leaves, initialized ABI-22 runtime tables, and independently verified `WVHC 1` capability/service metadata while preserving version-1 bytes.
- The first capability-bearing static platform-library slice: profile-compatible imports require explicit transitive capability approval, portable `WVRS 1` policy now lives under `Libraries/Foundation`, and a `Libraries/Platform` adapter completes a live hosted store lookup without changing WVB or treating opaque host resources as filesystem paths.
- The first versioned rights-limited filesystem capability: `filesystem.directory_read_v1` binds one immutable directory snapshot, returns exact chunks of at most 3 KiB through a strictly validated `WVDR 1` envelope, and exposes typed Windvale results without native paths, handles, mutation, or kernel filesystem policy.
- Cross-host-qualified Probe 35 carries that capability through an isolated guest service: Windvale policy binds the exact immutable store/snapshot identities and both service exchanges, the kernel provides format-blind copied-message transport and generation-safe cleanup, and two rebuilt clients validate maximal directory reads before executing the retained interpreter.
- Earlier superseded candidate for source `i64`/`u64` scalars and conditional WVB 1.7 emission. Decision 0209 replaces its version selection and native-backend limitation with the single current WVB 1.11 contract described at the top of this section.
- Accepted bounded owned-value semantics as the prerequisite for dynamic sequences, builders, maps, database pages, caches, and resource values; allocating APIs must expose checked charges, budgets, publication, and lifetime rather than inherit host garbage-collection behavior.
- Opt-in reference-runtime dynamic-backing lifetime evidence through stacks, locals, calls, borrowed views, and direct records; the exact compiler's 902,262,268 constructed flat bytes reduce to a 9,030,829-byte ideal peak across 17 live backings inside the unchanged 16 MiB native capacity.
- A concrete opt-in 16-byte-header, 16-byte-aligned first-fit/coalescing replay that admits all 1,852,773 exact-compiler dynamic allocations inside the same 16 MiB arena, peaks at 9,031,216 charged bytes, reaches a 10,700,368-byte extent despite measured fragmentation, and recovers every block without changing ABI 21.
- A bounded native machine-IR descriptor ownership plan with a separately implemented reconstruction oracle; the exact 328-function compiler deterministically publishes 186,557 borrow, acquire, retain, release, call, return-transfer, and frame-cleanup actions while preserving ABI 21 and every generated machine byte.
- The first WVA-owned native descriptor allocator leaf: one digest-bound 2,989-byte x86-64 implementation performs bounded first-fit allocation, checked retain/release, address-ordered coalescing, and stale/corrupt-owner rejection; live W^X differential execution agrees byte for byte with an independent model while ABI 21 remains unchanged.
- A deterministic native descriptor-allocator emission schedule with separate reconstruction: all 180,190 exact-compiler calls now have physical direct/indirect owner locations, five execution phases, generated-versus-service ownership, bounded three-cell request scratch, and explicit instruction/call-budget preservation without changing the integrated ABI-22 bytes.
- Typed WVA 8- and 16-bit register families, byte/word register and RIP/SIB-memory operations, signed width-appropriate immediates, width-bounded shifts, condition-result materialization, zero/sign extension, and byte port I/O in both the Windvale assembler and independent C# oracle, with exact-byte, differential, boundary, and malformed-input coverage.
- The implemented-candidate one-page `WVRQ 1` / `WVRY 1` resource-service protocol, format-blind capacity-one exchange lifecycle, independent Stage 0 verifier/handler, and Windvale-owned live hosted lookup of a third configuration resource.
- The implemented-candidate `WVRS 1` read-only resource store: a deterministic bounded format, independent Stage 0 writer/verifier, portable Windvale validation and dynamic name lookup, and a hosted three-resource service fixture without path or filesystem semantics.
- Probe 34's immutable guest resource service: init alone maps an exact digest-bound `WVRS 1` page RO/NX, its WVA seam dynamically selects and copies the requested configuration resource, and `WVCHAN03` clears retained IPC state while recording terminal client exit/fault across the two-generation rebuild proof.
- Deterministic WVA signed-immediate ALU/test, signed multiply, bounded rotate/shift, and typed SIB base/index/scale/`disp32` memory operations in both the Windvale assembler and independent C# oracle.
- An expanded WVA x86-64 foundation with typed 32/64-bit general-purpose registers, definition-local labels and deterministic near fixups, register move/ALU/compare/test operations, conditional branches, stack and indirect-control operations, and RIP-relative symbol addressing in both the Windvale assembler and independent C# oracle.
- The [Windvale Community Source License 1.0](LICENSE.md), [E-Worker Inc](https://eworker.ca) stewardship, contributor agreement, and third-party notice policy.
- Vendor-neutral AI-authorship and attribution policy.
- Public contribution, governance, security, support, conduct, and project-identity policies.
- The public [`eworker-inc/Windvale`](https://github.com/eworker-inc/Windvale) repository, issue and pull-request templates, private vulnerability reporting, and public development workflow.
- Change-aware lightweight, website, and qualification CI scopes, with a targeted site/editor/Wasm/support gate that avoids unrelated Seed qualification for website-only changes.
- Windows and Linux repository verification workflows.
- Repository-owned Windvale syntax highlighting and Visual Studio Code language support for `.wv` source files.
- A GitHub-rendered visual progress dashboard with evidence-based roadmap indicators and a dated SVG phase map.
- Opt-in deterministic per-function instruction reporting in the C# reference runtime and Seed CLI.
- The accepted first x86-64 UEFI/QEMU boot environment and a path-independent dependency preflight over exact EDK II firmware inputs.
- The first verified WVA-to-Windvale kernel execution seam, linking an assembled machine shim to compiler-generated `.wv` Main on kernel-owned memory.
- Windvale-owned post-memory boot evidence and a bidirectional WVA seam for Main entry and console-byte output.
- The accepted Windvale-native interpreter/JIT/AOT direction and explicit qualification gate for retiring .NET from the normal Windows and Linux workflow.
- Qualified exact Windvale bytecode compiler self-reproduction: Stage 0 builds Stage 1, and Stage 1 builds a byte-identical Stage 2 from the same committed 12-module inventory on Windows and Debian.
- The cross-host-qualified Windvale Project 1 manifest, `windvale build` command, portable Windvale-written parser, and native hosted shell for selecting and inspecting one root plus explicit source dependencies without changing import, WVSS, or WVB semantics.
- The cross-host-qualified shared native path through ABI 14/context 6, including all eleven native service leaves, bounded borrowed and arena-backed values, deterministic interpreter/JIT/WVO-AOT agreement, and live Windvale-produced process-input leaves.
- A cross-host-qualified Windvale publication planner that owns every live executable-image extent and canonical runtime-service placement before the narrow Windows/Linux W^X adapter allocates memory.
- A cross-host-qualified Windvale publication-lifetime graph that gates allocate, copy, seal, invoke, and release operations while one internal C# owner contains raw Windows/Linux executable-memory authority.
- A cross-host-qualified probe-17 terminal invalid-opcode boundary with a kernel-owned vector-6 IDT, deterministic panic transcript, and separate pinned-QEMU success and fault images.
- The accepted capability-oriented Windvale OS architecture: a small kernel written primarily in `.wv`, a bounded `.wva` machine layer, isolated services, AOT system code, and no permanent C#/.NET dependency.
- A cross-host-qualified WVA-owned Q35 poweroff adapter, with exact assembler parity and clean pinned-QEMU exit after the successful kernel path.
- Cross-host-qualified WVA-owned normalized x86-64 entries for invalid opcode and general protection, one explicit ring-0 trap-frame prefix, and three deterministic pinned-QEMU boot scenarios.
- Cross-host-qualified kernel-owned x86-64 page tables with a low-1-GiB identity map, null-page guard, NX/WP enforcement, fixed read-only/executable boot window, WVA-owned CR3 mechanics, and continued success/fault evidence under the new root.
- Cross-host-qualified fixed in-guest WVB admission: an AOT Windvale verifier checks one exact embedded WVB 1.6 identity before the bridge can execute its separately AOT-compiled form, while malformed and changed inputs fail closed.
- The first protected Windvale OS process candidate: a separate CPL3 root/thread executes the admitted Windvale AOT module, uses a generation/rights-checked capability for bounded register IPC and syscall exit, contains one deliberate user fault, and preserves terminal kernel-fault behavior.
- The first Windvale OS init/resource-service candidate: a receive-only Windvale service and send-only admitted client execute under separate CPL3 roots, exchange one kernel-owned message through deterministic wait/wake coordination, and preserve service completion after a contained client fault.
- The first user-space Windvale OS bytecode interpreter candidate: an AOT-built portable Windvale interpreter executes the exact admitted WVB subset at CPL3, records its runtime/input identities and role-specific W^X bounds, and sends the interpreted result to the independent init service without linking the program's AOT derivative.
- A section-derived Windvale OS interpreter profile that validates the WVB envelope and seven ordered sections, derives code and metadata payloads, and executes a second compiler-produced module after its longer name moves the code section; the enlarged process, paging, and aligned-memory contracts retain explicit bounds and pinned-QEMU evidence.
- Cross-host-qualified runtime-supplied Windvale OS WVB: the hosted CPL3 interpreter fetches fixed `boot:main.wvb` through an exact WVA-owned ABI-16 leaf, while the admitted program occupies a separate RO/NX page and is absent from the interpreter WVB and linked RX image.
- The first cross-host-qualified Windvale-selected OS boot-resource grant: init owns the fixed admitted WVB, selects resource `1` in `.wv`, and requests one checked immutable RO/NX client alias before the unchanged user-space interpreter can read it.
- The first qualified Windvale OS terminal resource-borrow cleanup: ordinary client exit and contained client fault both remove the WVB alias and private service/resource publication while preserving init ownership and one historical grant.
- The first qualified typed Windvale OS resource pair: init atomically grants the admitted WVB plus a separate execution budget, WVA validates exact typed lookup, the Windvale interpreter charges each opcode, and terminal cleanup removes both aliases and their complete publication.
- ABI 17's bounded 2,048-cell native frame envelope, admitting the exact compiler past its former 1,049-local blocker and reporting the next lowered-value limit with the exact function and required slot.
- Cross-host-qualified ABI 18 typed block-scoped physical values, clearing the exact compiler's slot-2,049 blocker without raising the 2,048-cell limit and reducing Probe 32's retained interpreter WVO by 27.5% and deepest native stack by 59.5%.
- Cross-host-qualified ABI 19 one-byte construction with exact `u8` boundary behavior, independently verified arena/descriptor/store shape, scalar/result alias rejection, and exact compiler preflight advanced to `Bytesˉfromˉu16ˉlittle`; all four pinned-QEMU Probe-32 identities remain exact.
- Probe 30's first reclaimed process extent: exact LIFO release zeroes a terminal client's 42 pages, rebuilds generation 2 at the same physical root, generation-stamped resource records reject stale state, and both clients execute the same typed WVB/budget input.
- Qualified Probe 31's exact-WVB portability proof: the canonical 493-byte `Sum-Data.wv` WVB runs to `29` through Windows/Linux reference and native paths plus a protected Windvale OS interpreter, exercising immutable data, a loop, branches, locals, and internal calls in both rebuilt client generations.
- Cross-host OS-test execution in the regular non-Fast verifier, qualifying probe 24 with all 67 Seed tests and all 25 OS tests on Windows and digest-pinned Debian 12 while keeping the long golden/native CLI gates out of the focused inner loop.
- A permanent UTF-8 native-process verifier boundary and detached/redirected macron regression check, closing the recurring launcher-only decoding failure.
- Cross-host-qualified ABI 15/context 7 native whole-file output through exact Windows and Linux leaves, advancing the exact compiler WVB beyond its `file.write_bytes` admission blocker.
- Cross-host-qualified ABI 16 bounded internal calls: four fast register parameters plus verified 16-byte stack cells through the language's 64-parameter limit, advancing exact compiler preflight to its single 1,049-local function.
- Cross-host-qualified bounded exact-compiler publication: deterministic size attribution justifies an 8 MiB fragment ceiling under unchanged ABI 20, the 4,556,121-byte compiler passes independent decoding and W^X publication, and native execution reaches the retained 1 MiB record-arena boundary before output.
- Cross-host-qualified bounded exact-compiler execution: measured demand of 1,480,096 record bytes justifies a 2 MiB host arena, under which the native compiler emits the exact 815-byte Stage 0 WVB while retaining ABI 20, generated bytes, execution-scoped lifetime, and independently sized Windvale OS contexts.
- Cross-host-qualified nominal record and enum identities throughout native machine IR plus a deterministic record-storage planner; exact compiler liveness projects a 1,489-cell worst frame under the unchanged ABI-20 2,048-cell ceiling without yet changing record allocation or claiming full native reproduction.
- Cross-host-qualified deterministic absolute native record-storage maps for persistent locals and block-local results, independently checked against CFG and value lifetimes and pinned for the exact compiler without changing ABI 20 or selected machine bytes.
- The first narrow Windows host-executable target: `windvale compile --target windows-x64-console-v1` packages capability-free scalar programs as deterministic import-free PE32+ files with an independently verified startup/context boundary and direct Windows execution without loading .NET.
- The matching narrow Linux host-executable target: `windvale compile --target linux-x64-console-v1` packages the same verified scalar fragment as a deterministic sectionless static-PIE ELF, owns its stack through a bounded Linux `mmap`, and exits through direct syscalls without an interpreter, libc, or .NET.
- Paired portable process-result normalization and atomic executable publication: both native containers preserve only successful results `0` through `255`, map every other result or native failure to `1`, and publish a fully written and prepared sibling file through one replacement so failure leaves the requested output unchanged.
- WVA-authored Windows and Linux console startup candidates with exact instruction-level sources, four typed final-image relocations per host, and byte-for-byte differential agreement with the independent C# recovery writers.
- A versioned portable Windvale console-application planner that now supplies every live PE/ELF file and virtual placement to both Stage 0 writers, with a digest-pinned retained WVB, bounded evaluation, complete independent C# reconstruction, and malformed request/response evidence.
- Fixed sparse Windvale construction recipes that determine every Windows PE and Linux ELF byte without raising the 4 MiB value limit; both live writers validate and materialize the recipes, compare complete output with C# recovery writers, and retain their independent untrusted-container checks.
- Cross-host-qualified ABI 21 frame-owned direct records across the shared host-container/OS backend: the independent decoder verifies pointer/copy/call/return shapes, the exact compiler and rebuilt Probe 32 use zero record-arena bytes, both version-1 console targets use the same selector, full bootstrap pressure advances from `WVR3017` to `WVR3018`, and `WVKMEM11` bounds the larger 109-page OS client; complete Windows/Debian Seed and OS evidence plus all four Windows pinned-QEMU scenarios pass.
- Cross-host-qualified ABI 22 generation-owned dynamic byte buffers and verified function-return arena checkpoints across the shared backend. The exact native Stage 1 compiler reproduces the complete byte-identical 599,868-byte Stage 2 under a measured 64 MiB host arena after Decision 0147 ownership-plan agreement; the independent decoder rejects corrupt ownership/checkpoint stencils, and `WVKMEM13` composes the rebuilt 110-page client with Probe 34's resource service. Exact descendant `2591cd5` passes all 84 Seed tests, all 31 OS tests, the golden compiler contract, and the complete native CLI gate on Windows and digest-pinned Debian 12 in GitHub Verify run 30797770080; all four pinned-QEMU scenarios pass on Windows.
- The public [`windvale.ca`](https://windvale.ca/) project home, system-responsive light and dark themes, new Windvale visual identity, and same-origin [`windvale.ca/playground/`](https://windvale.ca/playground/) browser playground.
- Windvale-authored WebAssembly backend profiles: a portable `.wv` selector emits deterministic import-free modules from bounded WVB; execution ABI 1 preserves checked arithmetic, ABI 2 adds exact instruction limits for structured control and bounded calls, and ABI 3 adds fixed disjoint 4 MiB linear-memory buffers, strict guest-side UTF-8 transport, and a statically checked primitive/bytes runtime with reads, slices, little-endian construction, concatenation, and bounded monotonic allocation. Exact result or output validity, Windvale failure status, and instruction accounting are preserved without an engine trap.
- Playground integration for the bounded direct-Wasm subset: a digest-pinned `.wv` backend lowers canonical WVB, a disposable browser worker validates and executes the import-free module, and the UI compares result and instruction evidence with the .NET reference interpreter.
- A standalone `/playground/wasm-demo/` route that checks and executes the pinned 791-byte profile-8 text artifact without loading Blazor or .NET, accepts editable Unicode input, and independently validates ABI-3 memory and UTF-8 evidence while keeping Stage 0 artifact-production provenance explicit.
- A separate Windvale support page with configurable one-time Stripe tiers, explicit public-recognition consent, and a validated Cloudflare KV-backed supporter roll that keeps personal data out of repository history.

### Changed

- The x86-64 kernel exception terminal, its three marker records, and its bounded polled-COM1 byte loop now assemble from canonical WVA/WVO; the Stage 0 exception object retains only checked IDT descriptor construction.
- The Windvale-written lexer uses bounded keyword, identifier, and Unicode-whitespace dispatch while preserving the Seed lexical contract.
- Compiler folders now describe implementation roles: `Compiler/Windvale` contains the Windvale-written compiler, while `Compiler/Reference` contains the independent C# reference/recovery compiler.
- Assembler folders now describe implementation roles: `Assembler/Windvale` contains the Windvale-written assembler, while `Assembler/Reference` contains the independent C# Stage 0 reference/recovery assembler. Canonical WVA inputs remain under `Examples/Assembler`.
- Linker folders now describe implementation roles: `Linker/Windvale` contains the Windvale-written flat-image linker, while `Linker/Reference` contains the independent C# Stage 0 reference/recovery linker and its currently C#-only UEFI target adapter. Canonical WVA provider inputs remain under `Examples/Linker`.

### Current development status

- Windvale Seed, its runtime and bytecode foundation, the object model, assembler, linker, Foundation modules, complete portable compiler, and exact bytecode compiler self-reproduction have Windows/Debian qualification evidence.
- Complete hosted native compiler reproduction is cross-host qualified under ABI 22. Standalone compiler packaging, the remaining native tools, Windvale-native platform memory authority, complete execution-support lifetime ownership, .NET retirement, and a general Windvale OS runtime remain active or planned milestones rather than completed releases; qualified Probe 31 proves one exact nontrivial WVB across Windows, Linux, and Windvale OS but does not provide a general allocator, arbitrary loader, dynamic resource namespace, transferable capability system, scheduler, general interpreter/JIT, or complete kernel runtime.
